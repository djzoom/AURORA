# L15 — 高斯消元（Gaussian elimination） — 方程组 Ax=b 的消元过程，行阶梯形（REF）与解的存在性分类

**总体评级**: B- | **问题数**: 1 critical / 3 major / 4 minor

## 概述

本课数学正确性高——三元例题解 [1,2,-3]、增广矩阵 ER 消元步骤、秩判别三类情形均经验证无误，aurora.laviz 调用路径真实可用。主要缺陷有两处：cell-9（习题之前）已完整展示 `classify_system` 所需的全部 if-else 逻辑，习题价值被严重稀释；Markdown 小节"奇异矩阵实验"承诺让学生运行代码，却没有对应的代码单元格，形成一个不可运行的死角。

## 亮点

- 秩判别三分类的推理路线（步骤 1–4）清晰具体，用数字示范了 rA、rAb 与 n 的比较，适合初学者记忆。
- 三个验证例题（U、M、N）数学正确，覆盖三种情形，`assert` 断言形式简洁，预期值和实际值一眼可见。
- `aurora.laviz.mat_times_vec` 调用路径正确（`src/aurora/laviz.py` 函数签名匹配），可运行，Aurora 连接部分真实可信。
- 条件数参数实验（cell-23）设计良好，用 eps → 0 直观展示接近奇异时数值不稳定的趋势，超出标准教学内容，是加分项。
- "本课收束"正确指向 L16（行列式与逆矩阵），cross-reference 无误。

## 问题列表

### 🔴 Critical

**[Runability]** — cell-24（Markdown）承诺"把 A 的行列式设为 0，调用 `np.linalg.solve` 会触发 `LinAlgError`；再用 `lstsq` 处理同一矩阵"，并明确写"运行验证"，但 cell-24 之后直接跳到 cell-25（本课收束），中间没有任何代码单元格。学生按描述操作，找不到可运行的代码。

Fix：在 cell-24 之后插入代码单元格，用 `try/except np.linalg.LinAlgError` 捕获奇异矩阵错误，再对比 `np.linalg.lstsq` 在一致方程（`b=[3,6]`）和矛盾方程（`b=[3,7]`）两种情形下的返回值。

### 🟠 Major

**[Exercise Design — Solution Leak]** — cell-9（位于第 1 节，习题 4 之前）已完整展示 `classify_system` 的核心算法：计算 `rank_A` 和 `rank_Ab`，与 `A.shape[1]` 比较，按三个分支处理。学生只需将该 `if/elif/else` 块加上三个 `return` 语句即可通过所有断言。习题的认知负担被大幅提前消耗，学习价值严重流失。

Fix：将 cell-9 改写为仅打印 rank 数值（不含分支逻辑），或将完整分类逻辑挪到习题 4 的断言通过之后作为"参考对照"。

**[Exercise Design — Stub 规范]** — 桩函数（cell `adadf627`）结尾为 `return None`。当学生未实现函数直接运行测试时，收到的是无信息的 `AssertionError`，无法区分"任务未完成"与"逻辑错误"。

Fix：改为 `raise NotImplementedError("TODO: implement classify_system")`，使报错类型本身成为任务提示。

**[Exercise Design — 缺少超定测试]** — 三个验证例题（U、M、N）全是 3×3 方阵。但学习目标 5 和"为什么对 Aurora 重要"均明确提到 LPC 是超定系统（m > n）。无一测试用例覆盖 m > n 的情形，与课程主题脱节。

Fix：新增一个超定例题，例如 A 为 4×2 矩阵（4 方程、2 未知数），验证 `classify_system` 在有唯一最小二乘解时返回 `'unique'`，使 Aurora 连接落地。

### 🟡 Minor

**[Code Comment 不精确]** — cell `0352c35f` 的注释 `# ER3 + ER2` 标注的是 `2*aug[1] - 3*aug[0]`，实际是"先对行 2 乘以 2（ER2），再减去 3 倍行 1（ER3）"的合并步骤，以保持整数。"ER3 + ER2"表述会让学生误解操作顺序或标签含义。

Fix：改为 `# 整数消元：2×R2 − 3×R1（ER2 缩放后接 ER3）`，并在 Markdown 处一行说明为何融合两步。

**[Runability — 变量覆盖风险]** — cell-23 将 `A` 重新赋值为 2×2 矩阵，覆盖整节课使用的 3×3 矩阵 `A`。若学生之后重新执行依赖旧 `A` 的单元格，会得到静默错误结果。

Fix：在 cell-23 中将变量命名为 `A_cond`，避免污染 session namespace。

**[Redundant Imports]** — `import numpy as np` 出现在 cell-3、5cd23f1b、cell-9、cell-11、cell-23 共五处，而 cell `0352c35f` 反而没有显式导入（依赖 session 状态），风格不一致。

Fix：在首个代码单元格统一导入，其余删除重复导入；或至少在 `0352c35f` 中补上导入，使其可独立运行。

**[Exercise Spec 内外不一致]** — 习题 4（cell `ab06e2fb`）"参考输入输出"给的是 2×2 示例（`[[2,1],[1,3]]` 等），但测试套件（cell `089aafd5`）全部使用 3×3 矩阵，两处矩阵规模不一致，对照阅读时容易混淆。

Fix：将参考示例统一为 3×3，或在测试套件中补充一组 2×2 用例与参考示例呼应。

### 💡 建议

- "Aurora 连接"（cell 2aeb143d）提到正规方程 `(XᵀX)w = Xᵀy`，而引言提到 LPC 的 Yule-Walker 矩阵，两者都是同一结构的实例，可加一行说明帮助学生把两种提法统一。
- 考虑对代码实验单元格加"🔬 实验 N"编号，与编号学习目标对齐，方便学生自查完成度。

## 优先修复（前3项）

1. **[Critical] 补充奇异矩阵实验代码单元格**：在 cell-24 之后插入代码，用 `try/except` 演示 `LinAlgError`，再对比 `lstsq`——这是本课承诺但缺失的核心实践内容。
2. **[Major] 消除 cell-9 的解题逻辑泄漏**：将 cell-9 中的 `if/elif/else` 分类逻辑移除或挪到习题之后，保留学生独立推导的空间。
3. **[Major] Stub 改为 `raise NotImplementedError`**：一行修改，使未完成状态的报错信息清晰，与课程其他习题单元格保持一致风格。
