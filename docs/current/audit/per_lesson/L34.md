# L34 — Nyquist 定理与混叠（aliasing） — 6kHz 正弦波被 8kHz 采样后会变成什么

**总体评级**: B- | **问题数**: 1 critical / 3 major / 3 minor

## 概述

本课用"6 kHz 被 8 kHz 采样后折叠成 2 kHz"的具体案例演示 Nyquist 定理，概念清晰、Aurora 连接真实（`aurora.audio.sine` 和 `aurora.aviz.aliasing` 路径均经过确认）。但存在一个直接阻塞运行的缺陷：TODO 存根返回 `None` 导致下游单元格以 `TypeError` 崩溃，而非给出清晰的"未实现"提示，整个笔记本无法从头到尾运行。此外，核心实验缺少定量断言，"下一节"引用的课程内容与实际 L35 不符。

## 亮点

- **折叠公式推导扎实**：在 cell-1 和 cell-e3450e0e 中，用 `k = round(freq/sr)` 找折叠中心、再取绝对值的推理路线逐步展开，配合具体数值（6000 Hz → 2 kHz）让概念可验证。
- **双公式一致性**：notebook 中使用的两种等价公式——`abs(((f + nyq) % sr) - nyq)` 和 `abs(f - sr·round(f/sr))`——经程序验证在 sr=8000 全频域完全一致，数学无误。
- **Aurora 路径全部有效**：`from aurora.audio import sine`（`src/aurora/audio/__init__.py` 正确导出）和 `aurora.aviz.aliasing(6000, 8000)`（`src/aurora/aviz.py:42` 存在且逻辑正确）均可正常调用。
- **cell-9 和 cell-20 的探索式代码**：遍历频率范围打印视在频率，在练习前给学生建立直觉，是有效的脚手架。

## 问题列表

### 🔴 Critical

**[Runability]** — `predict_alias_freq` 存根返回 `None`，而不是 `raise NotImplementedError`。cell-6de0a05b 执行 `assert 0 <= alias <= sr/2` 时抛出 `TypeError: '<=' not supported between instances of 'int' and 'NoneType'`，而非 AssertionError；之后的 cell-8741bb40 因 `x_aliased`、`x_lowfreq`、`alias` 均未定义而 NameError。笔记本无法从头到尾运行。

**Fix**: 将存根改为 `raise NotImplementedError("predict_alias_freq: 请实现折叠公式 abs(freq - sr * round(freq/sr))")`，并在 cell-6de0a05b 中用 `try/except NotImplementedError` 包裹，给出友好提示后跳过后续绘图单元格（或用守卫变量控制）。

---

### 🟠 Major

**[Exercise Design]** — cell-6de0a05b 计算并打印了 `diff`（高频采样点与低频预测的最大差值），但没有断言 `diff` 接近 0。这是本课最核心的实验结论，却缺少定量的通过/失败标志；学生实现错误时看不到任何红色警告，只会看到一个数字。

**Fix**: 在 `print('采样点与低频版差异:', ...)` 之后加一行：
```python
assert diff < 1e-10, f"混叠验证失败：差值 {diff:.2e} 应 < 1e-10"
```

**[Course Continuity]** — cell-cell-22"本课收束"写道："下一节（窗函数）在同样的采样点数组上加汉宁窗，压制频谱泄漏，为 FFT 做准备。"但实际下一节是 **L35（欧拉公式遇见 FFT，旋转因子可视化）**；窗函数在 **L36** 才出现。跳过了整整一节。

**Fix**: 将描述改为"下一节（L35，欧拉公式与旋转因子）引入复数表示，为 DFT 推导奠基；L36 再引入汉宁窗压制频谱泄漏。"

**[Exercise Design]** — 练习没有边界情况测试：`freq=0`（应返回 0）、`freq=sr/2`（精确在 Nyquist，应返回 4000）、`freq=sr`（应折回 0）均未覆盖。这三个边界恰好是学生最容易弄错符号或取整方向的地方。

**Fix**: 在验证区块前增加：
```python
assert predict_alias_freq(0, 8000) == 0.0
assert predict_alias_freq(4000, 8000) == 4000.0
assert predict_alias_freq(8000, 8000) == 0.0
```

---

### 🟡 Minor

**[Exercise Design]** — 存根使用 `return None` 而非 `raise NotImplementedError`（课程设计准则要求"实质性练习必须 raise NotImplementedError"）。除触发 Critical 问题外，`return None` 还会掩盖学生"忘记返回值"的错误。

**Fix**: 改为 `raise NotImplementedError(...)` 即可（见 Critical 修复）。

**[Pedagogical Quality]** — 笔记本在 cell-9、cell-20 中使用 `abs(((f + nyq) % sr) - nyq)` 形式，而 TODO 描述和 `aviz.aliasing` 内部使用 `abs(f - sr·round(f/sr))` 形式。两者数学等价（已验证），但笔记本完全没有解释这两种写法为何等价，会让学生困惑"哪个才是正确公式"。

**Fix**: 在 cell-e3450e0e 的 💡 提示框中增加一句说明："这两种写法等价：`((f + Nyquist) mod sr) - Nyquist` 与 `f - sr·round(f/sr)` 表达的是同一个折叠操作。"

**[Pedagogical Quality]** — cell-cell-25（"小检查：从短序列开始"）出现在"本课收束"（cell-cell-22）之后，打乱了笔记本的叙事流。该单元格是初学者热身代码，应放在 cell-7 附近（课程第一个互动实验处），而非结语之后。

**Fix**: 将 cell-25 移动到第 2 节"实验入口"之后，cell-cell-7 之前或之后。

---

### 💡 建议

- **cell-cell-21 中的扫频代码**：目前作为 markdown 代码块呈现，需要学生手动粘贴。将其改为实际可运行的代码单元格（依赖学生完成 `predict_alias_freq` 后），能让参数实验真正成为课堂活动而非说明文档。
- **"下一节"加上课号**：将"下一节（窗函数）"改为"L36 窗函数"，方便学生和审阅者快速定位，整个课程中的交叉引用风格应统一。

## 优先修复（前3项）

1. **将 `return None` 改为 `raise NotImplementedError`，并在 cell-6de0a05b 中添加守卫**，防止 TypeError/NameError 级联崩溃，确保笔记本在未实现状态下可优雅运行。
2. **在 `diff` 打印后加 `assert diff < 1e-10`**，将核心混叠实验从"观察性展示"升级为"可验证的通过/失败测试"。
3. **修正"本课收束"中的下一节引用**，从"（窗函数）"改为"L35（欧拉公式与旋转因子）"，消除导航错误。
