# L89 — RAG 完整流水线 — 文档切片（Document Chunking）+ TF-IDF 检索 + 提示词（Prompt）构建 + 生成

**总体评级**: C+ → B-（修复后） → B（2026-06-30 重新评级）→ A-（2026-07-01 loop升级）| **问题数**: 2 critical / 2 major / 4 minor

## 修复记录（2026-06-29）
| 重新评级 2026-06-30：B- → B | NIE 桩 + aurora.llm 真实导入 + 数据流表 | 📋 已记录 |

| 问题 | 修复方式 | 状态 |
|---|---|---|
| SyntaxError in cell-10：orphaned template fragment 导致 `Answer:"""` 开启未闭合三引号字符串 | 删除孤立代码；在 docstring 中补充期望输出格式模板 | ✅ 已修 |
| 单元格顺序错误：format_rag_prompt 测试（原 cell-8）和端对端 RAG 测试（原 cell-9）在定义/依赖之前执行 | 重排 cells：stub 移至 任务2 markdown 后（新 cell-8），端对端测试移至 build_tfidf 之后（新 cell-11） | ✅ 已修 |
| chunk_text stub 崩溃 cell-5 导致 all_chunks 未定义 | 将测试调用包裹 try/except NotImplementedError；all_chunks 初始化移至 try 外并加兜底逻辑 | ✅ 已修 |
| format_rag_prompt 练习无规格说明 | 随 Critical-1 修复一并补充 docstring，说明期望输出格式 | ✅ 已修 |
| Minor: 错误的 L90 前向引用（Tool-calling Agent） | 更新为正确的"对话式 RAG——会话记忆、来源归因与 Podcast Q&A 流水线" | ✅ 已修 |

## 概述

The pedagogical concept — assembling chunk_text + TF-IDF retrieval + prompt formatting into a complete RAG pipeline — is well-chosen and well-scoped for this point in the course. However, the notebook is unrunnable top-to-bottom due to three compounding structural failures: a SyntaxError from a garbled partial-solution leak in the `format_rag_prompt` stub cell, two verification cells that appear before the function they test is defined, and a cell-5 crash that leaves `all_chunks` undefined for the rest of the notebook.

## 亮点

- The RAG architecture diagram in cell-1 is clear and correctly labels which aurora.llm functions handle each stage.
- The `aurora.llm` imports (`build_tfidf`, `cosine_retrieve`) reference real, correctly-named functions in `/Users/z/AURORA/src/aurora/llm/retrieve.py`; no phantom imports.
- The verification cell for `chunk_text` (cell 015b5290) uses a clean `try/except NotImplementedError` guard and tests multiple properties (return type, multi-chunk for long input, per-chunk word count, short-text pass-through).
- The `cosine_retrieve` score type and range assertions (`isinstance(score, float)`, `0.0 <= score <= 1.0`) are accurate given the implementation returns `float(scores[i])` on non-negative TF-IDF vectors.
- The "本课收束" summary table correctly accounts for all five pipeline stages.

## 问题列表

### 🔴 Critical

**[Runability — SyntaxError in cell-10]** — The `format_rag_prompt` stub cell contains leaked, garbled partial-solution code after the `raise NotImplementedError("TODO")` line. Specifically, the text `Answer:""".strip()` opens a triple-quoted string that is never closed, producing `SyntaxError: unterminated triple-quoted string literal` (confirmed via `compile()`). The cell cannot execute at all, meaning `format_rag_prompt` is never defined.

Fix: Remove everything after `raise NotImplementedError("TODO")` in cell-10. The orphaned lines (`Context:`, `{context}`, `Question: {query}`, `Answer:""".strip()`, `    return prompt`) are a mangled fragment of the solution and must be deleted entirely. If scaffolding hints are desired, add a docstring comment showing the expected template structure.

**[Runability — Cell ordering: two test cells precede their definitions]** — Two verification cells run before the things they test are defined:
- The `format_rag_prompt` test (cell 9f212137, notebook position [8]) runs before `format_rag_prompt` is defined (position [10]).
- The end-to-end RAG test (cell cbc38021, position [9]) references `matrix` and `vocab`, which are built at position [11], and `format_rag_prompt`, defined at position [10].

Both cells catch `NameError` silently, so students see `⬜ 未实现` when the real problem is broken notebook layout. Fix: Move the `format_rag_prompt` definition cell (cell-10) immediately after the "任务 2" markdown header (cell-7), before the test cell (9f212137). Move the end-to-end test cell (cbc38021) after cell-11 where `matrix` and `vocab` are built.

### 🟠 Major

**[Runability — chunk_text stub crashes cell-5, leaving all_chunks undefined]** — Cell-5 defines the `chunk_text` stub, then immediately calls it with a 200-word string (which exceeds `max_words=80`, hitting the `raise NotImplementedError`). Execution halts mid-cell; the `all_chunks = []` initialization and the knowledge-base loop that follows are never reached. Cell-11 subsequently fails with `NameError: name 'all_chunks' is not defined`, taking down `build_tfidf` and the entire demo section.

Fix: Wrap the in-cell test calls in `try/except NotImplementedError` (matching the pattern already used in cell 015b5290), and initialize `all_chunks` with a guarded fallback:
```python
try:
    chunks = chunk_text(long_text, max_words=80, overlap=10)
    print(f'200 词文本 → {len(chunks)} 个块')
except NotImplementedError:
    print('⬜ chunk_text 未实现')

all_chunks = []
for doc in KNOWLEDGE_BASE:
    try:
        all_chunks.extend(chunk_text(doc, max_words=60))
    except NotImplementedError:
        all_chunks.append(doc)  # fallback: use full paragraph
```

**[Exercise Design — format_rag_prompt exercise is underspecified]** — The only guidance for the TODO is the comment "构建包含上下文的提示词". Students have no idea what structure is expected. The test in cell 9f212137 checks that the query text and first retrieved doc snippet appear in the prompt, but gives no guidance on what template to produce. Any ad-hoc concatenation passes the test, making the exercise unchallenging.

Fix: Add a docstring showing the expected output structure, for example:
```
    Expected output format:
    Context:
    [1] <first retrieved passage>
    [2] <second retrieved passage>

    Question: <query>

    Answer:
```
This tells students what to build without giving away the implementation, and makes the test meaningful.

### 🟡 Minor

**[Course Continuity — Wrong L90 forward reference]** — The "本课收束" section states "下一步 L90：Tool-calling Agent——让模型自己决定什么时候调用哪个工具". The actual L90 title is "对话式 RAG — 会话记忆（Conversation Memory）、来源归因与 Podcast Q&A 流水线". Fix: Update the forward reference to match.

**[Exercise Design — Overlap correctness not verified]** — The chunk_text test in cell 015b5290 checks that chunks are not too long and that the 200-word input produces more than one chunk, but never verifies that consecutive chunks actually share `overlap` words. A student could implement a non-overlapping chunker and pass all tests. Fix: Add an assertion such as:
```python
if len(chunks) >= 2:
    end_of_first = set(chunks[0].split()[-overlap:])
    start_of_second = set(chunks[1].split()[:overlap])
    assert len(end_of_first & start_of_second) > 0, "连续块应有重叠词"
```

**[Exercise Design — Empty string edge case missing]** — The chunk_text tests do not include `chunk_text("")`. A student implementation may crash or return unexpected output on empty input. Fix: Add `assert chunk_text("") == [""]` or `== []` depending on the intended contract.

**[Pedagogical Quality — Learning objectives use unordered bullet list]** — The four learning objectives use `-` bullets rather than numbered items `1. ... 2. ... 3. ... 4. ...`. Numbered objectives are easier to cross-reference with notebook sections. Fix: Convert to a numbered list.

### 💡 建议

- Consider adding a brief explanation of why `overlap` helps — specifically that split-boundary context loss is a common RAG failure mode — before introducing the `chunk_text` exercise. One sentence is sufficient.
- The hardcoded test docs in cell 9f212137 use string scores like `0.85`/`0.72` which are Python `float`. This is fine, but worth noting that `cosine_retrieve` returns NumPy-derived floats cast via `float()` — the `isinstance` check in the end-to-end test will pass for both.
- The optional LLM cell (cell-12) is a good touch; a one-line comment explaining why Qwen2.5-0.5B is the suggested model (small enough to run locally) would improve pedagogical continuity with L87.

## 优先修复（前3项）

1. **Remove the SyntaxError from cell-10**: Delete the orphaned lines after `raise NotImplementedError("TODO")`. This is a hard blocker — the cell cannot execute.
2. **Fix cell ordering**: Move the `format_rag_prompt` definition before its test cell, and move the end-to-end test after `matrix`/`vocab` are built. Two test cells currently run before their dependencies exist.
3. **Guard cell-5 test calls with try/except**: Prevent the `NotImplementedError` from crashing the cell before `all_chunks` is initialized, which currently breaks every downstream cell that uses `all_chunks`.
