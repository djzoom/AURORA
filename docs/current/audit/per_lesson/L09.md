# L09 — 向量（vector）代数 — 加法、标量乘法与线性组合，NumPy 实现 + 几何意义

**总体评级**: B- | **问题数**: 1 critical / 3 major / 4 minor

## 概述

The notebook covers the declared scope (vector addition, scalar scaling, linear combinations, geometric intuition) with correct core mathematics and a clean narrative arc. However the single Aurora codebase connection contains three compounding factual errors (wrong function name, wrong file path, wrong output shape), and a critical cell-level mismatch exists where the section heading promises matrix-vector multiplication but the code underneath actually demonstrates linear combinations. Exercise coverage is too thin for a standalone lesson.

## 亮点

- Learning objectives are numbered and concrete; each maps to a specific code skill or concept.
- The pedagogical sequencing from "vector = numpy array" → operations → geometric visualization is logical and well-paced for early linear algebra.
- `assert np.allclose(louder, [0.3, -0.6, 0.9, -1.2])` arithmetic is correct (0.2×1.5=0.3, etc.), and the tolerance-based assertion is the right tool here.
- `aurora.laviz` import (`style`, `arrows2d`) exists in `/Users/z/AURORA/src/aurora/laviz.py` and the API call matches the function signatures — this cell will not error.
- The `本课收束` accurately summarizes what was built and correctly points toward dot product as the next topic.

## 问题列表

### 🔴 Critical

**[Accuracy / Aurora连接]** — Three compounding errors in the MFCC reference used throughout the notebook:

1. **Wrong function name**: The notebook refers to `extract_mfcc()` in two places (cell `4cfc2768` and cell `aaa6a9c9`). The actual function in `src/aurora/audio/mfcc.py` is named `mfcc()`.
2. **Wrong file path**: The notebook cites `audio_core/mfcc.py`. The real path is `aurora/audio/mfcc.py`.
3. **Wrong output shape**: The notebook claims `extract_mfcc()` "outputs a `(13,)` vector per frame." The actual `mfcc()` function returns shape `(n_frames, n_mfcc)` — i.e., the entire sequence, not a single-frame vector.

Fix: Replace every occurrence of `extract_mfcc()` with `mfcc()`, fix the path to `aurora/audio/mfcc.py`, and clarify that `mfcc()` returns `(n_frames, 13)` — indexing one row (e.g. `coeffs[i]`) yields the `(13,)` per-frame vector.

### 🟠 Major

**[Runability / Code-Description Mismatch]** — Cell `cell-8` markdown states: "把四个方向各异的向量喂给同一个矩阵 A，看 x 分量如何被 y 分量拉偏——非对角元素 A[0][1]=1.0 就是这个耦合的来源." But cell `cell-9` contains no matrix at all — it computes `a*e1 + b*e2` linear combinations over basis vectors. Students following the prose will be confused about what they are observing.

Fix: Either (a) rewrite cell `cell-8`'s description to correctly say "这段代码展示任意 2D 向量都是基向量 e1, e2 的线性组合" — which matches the code — or (b) replace `cell-9` with actual matrix-vector code. Option (a) is simpler and keeps L09 on scope.

**[Exercise Design]** — The notebook contains only one TODO exercise (`scale`). Vector addition and linear combinations — two of the three headline topics — have zero student practice. A student can read the notebook without writing a single new line of arithmetic code beyond `c * v`.

Fix: Add at least two exercises: (1) `def add_signals(a, b)` with an assert verifying a concrete mixing example; (2) `def linear_combination(coeffs, vecs)` that reduces a list of `(scalar, vector)` pairs, tested on the e1/e2 basis example already in the notebook.

**[Exercise Design / Runability]** — The `scale` stub (cell `463a40e9`) returns `None` instead of raising `NotImplementedError`. When a student runs the test cell without implementing the function, they get:

```
TypeError: unsupported operand type(s) for -: 'NoneType' and 'float'
```

inside `np.allclose`, which gives no signal that the function is unimplemented.

Fix: Change the stub body to `raise NotImplementedError("TODO: implement scale")`.

### 🟡 Minor

**[Exercise Design]** — The `scale` assert tests exactly one input (`[0.2, -0.4, 0.6, -0.8]`, scalar 1.5). Edge cases not covered: zero vector `np.zeros(5)`, zero scalar `c=0`, negative scalar `c=-1`, and a scalar with |c| > 1 vs < 1 (geometric meaning hinge on this). A student could special-case the provided example and still pass.

Fix: Add three additional `assert np.allclose(scale(...), ...)` lines in the same test cell covering at least `c=0` and `c<0`.

**[Course Continuity / Scope]** — Cells `cell-21` and `cell-22` introduce matrix-vector multiplication (`A @ v`) and discuss off-diagonal coupling — content that belongs to L12 (matrices). At L09 the student has not yet seen matrix notation formally. These cells have no TODO, no connection to the stated lesson objectives, and are not referenced in `本课收束`.

Fix: Remove or move these cells to L12. If kept, add an explicit "preview — we will explain this formally in L12" callout.

**[Course Continuity]** — `本课收束` says "下一节点积" without stating the L-number. Given that stale p-numbers are a known course-wide risk (prior audit findings), spell out "L10 点积 (`L10_dot_product.ipynb`)" for unambiguous forward navigation.

**[Runability]** — Cell `a365b328` uses `np` (via `np.array`) but contains no import statement. It depends on `np` surviving from a prior cell. Notebooks should be independently runnable by cell order; if a student runs only cells 2-4 out of order they will get `NameError: name 'np' is not defined`.

Fix: Add `import numpy as np` at the top of cell `a365b328` (or consolidate all setup imports into one top-level cell).

## 优先修复（前3项）

1. **Fix the MFCC Aurora connection (Critical)** — change `extract_mfcc()` → `mfcc()`, `audio_core/mfcc.py` → `aurora/audio/mfcc.py`, and clarify the return shape is `(n_frames, 13)` not `(13,)`. This is the only factual error that would mislead a student who explores the real source code.
2. **Fix cell-8 description to match cell-9 code (Major)** — the matrix-coupling narrative applied to code that actually shows basis-vector linear combinations will mislead every student who reads both. A one-line description rewrite resolves it.
3. **Change `scale` stub to `raise NotImplementedError` (Major)** — the current `return None` produces an opaque `TypeError` that hides the unimplemented state from the student.
