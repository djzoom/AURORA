# L20 — 分解图谱 — LU / QR / SVD 三种分解的几何意义对比

**总体评级**: B → B+（修复后）| **问题数**: 0 critical / 2 major / 4 minor

## 修复记录

| # | 问题类型 | 原状 | 修复操作 | 修复单元 |
|---|---------|------|---------|---------|
| 1 | 🟠 Major — Course Continuity | 六处陈旧标签 `p6`/`p7`/`p9`/`v3` 指向不存在课号 | 全局替换：`p6`→`L14`，`p7`→`L15`，`p9`→`L17`，`v3`→`L21` | cells 8, 12, 13, 14, 16, 21 |
| 2 | 🟠 Major — Code Design | cell 9 用硬编码 `L = np.array(...)` / `U = np.array(...)` 绕过 `_lu(A)` | 删除两行字面量，改为 `L, U = _lu(A)` | cell 9 |

## 概述

L20 covers four matrix factorizations (LU, QR, spectral S=QΛQᵀ, SVD) with a consistent visual-first approach through `laviz`. The mathematics is correct throughout — all `np.allclose` assertions verify numerically, the hand-rolled `_lu` function produces the canonical Doolittle decomposition, and the single coding exercise (`rank_from_svd`) has a clean scaffold. The main weakness is pervasive stale cross-reference labels (`p6`, `p7`, `p9`, `v3`) inherited from a previous organizational scheme; every back-reference in the notebook currently points to a label that does not exist. A secondary issue is that `_lu` is defined in one cell but then bypassed in the LU visualization cell, which uses hardcoded matrix literals instead.

## 亮点

- All four matrix identities are verified with `np.allclose` asserts before being passed to `show_factorization`, so the pedagogical visuals are guaranteed to match correct mathematics.
- `np.linalg.eigh` is correctly chosen over `eig` for the symmetric spectral theorem — pedagogically deliberate and numerically stable.
- The `rank_from_svd` exercise has a clear 3-step reasoning scaffold and covers four distinct cases: full-rank square, rank-1 outer product, rectangular full-row-rank, and a random 5×4 matrix cross-checked against NumPy.
- The `try/except NotImplementedError` wrapper lets the notebook run top-to-bottom even before the student implements anything — consistent with the course's runability discipline.
- The condition-number experiment (κ = 1, 10, 1000) concretely ties factorization theory to numerical solver instability.

## 问题列表

### 🔴 Critical

None.

### 🟠 Major

- **[Course Continuity]** — Pervasive stale cross-references appear in five cells: `78b37885` references `` `p7` 学的消元 ``, `7c5942eb` references `` `p9` 学的对角化 ``, `31c61440` says `p9 的同一个例子`, `e9bec99a` says `` `p6` 的 SVD ``, and both `70ec5c49` and `cell-21` say `下一本/节 \`v3\``. The current L-numbering scheme has no lessons with these labels. Correct mappings: `p6` → `L14` (eigen_svd), `p7` → `L15` (linear_systems), `p9` → `L17` (eigen_diagonalization), `v3` → `L21` (aurora_matrices). Every student who attempts to follow a back-reference is sent to a dead end. **Fix**: Global search-replace all five occurrences.

- **[Code Design]** — `_lu` is defined and called in `cell-7` to print/verify the decomposition, but cell `f8e57a48` then silently overwrites `L` and `U` with hardcoded literals instead of calling `_lu(A)`. The two approaches give contradictory signals about how LU is obtained (algorithmic vs. magic constants) and the function definition is dead weight in the visualization flow. **Fix**: Remove the hardcoded `L = np.array(...)` / `U = np.array(...)` lines from `f8e57a48` and replace with `L, U = _lu(A)`, keeping the existing `assert np.allclose(L @ U, A)`.

### 🟡 Minor

- **[Runability]** — `heatmap` is imported in the first cell (`from aurora.laviz import style, show_factorization, heatmap`) but never called anywhere in the notebook. **Fix**: Remove `heatmap` from the import, or use it in an Aurora mel-spectrogram demonstration cell.

- **[Runability]** — `import numpy as np` is repeated verbatim in cells `cell-5`, `cell-7`, `cell-17`, and `cell-20` after it is already available from the top-level cell. Redundant but harmless. **Fix**: Remove the four redundant import lines from body cells.

- **[Exercise Design]** — The `rank_from_svd` test suite omits a zero-matrix edge case. The logic `S[0]` on a zero matrix returns `0.0`, so `np.sum(s > 1e-10 * 0.0)` correctly returns 0, but this is non-obvious and worth asserting explicitly. **Fix**: Add `assert rank_from_svd(np.zeros((3,3))) == 0, '零矩阵应返回秩 0'` to the verification block.

- **[Pedagogical Quality]** — Only one coding exercise for a lesson covering four major factorization types. Cell `cell-18` describes a rank-1 SVD experiment ("把矩阵从满秩改成秩1") entirely in markdown with no runnable code beneath it. **Fix**: Convert the rank-1 observation into a code cell: create `np.outer([1,2],[3,4])`, call `show_factorization` with its SVD, and confirm that only the first singular value is non-zero. This directly illustrates learning objective 4 about effective rank.

### 💡 建议

1. The `rank_from_svd` hint says "设门限 `t = threshold * S[0]`" but NumPy's `matrix_rank` uses `S.max() * max(M,N) * machine_eps`. A one-sentence comment explaining the relative-threshold design choice would deepen the connection to the condition-number material shown earlier in the same lesson.
2. Consider adding a second exercise implementing Gram-Schmidt QR from scratch (`def gram_schmidt_qr(A)`). This fits Aurora's no-black-box ethos and directly applies the orthogonalization geometry explained in section 2.
3. Learning objective 5 promises "理解 Aurora mel 频谱矩阵的 SVD 低秩近似" but no Aurora-specific code appears in the notebook. Adding even a small synthetic mel spectrogram (e.g., from `aurora.audio`) followed by `np.linalg.svd` and a singular-value decay plot would fulfill this promise.

## 优先修复（前3项）

1. **Replace all stale p-number and v-number cross-references** — five broken links actively mislead students: `p6`→`L14`, `p7`→`L15`, `p9`→`L17`, both `v3`→`L21`. Mechanical find-and-replace in cells `78b37885`, `7c5942eb`, `31c61440`, `e9bec99a`, `70ec5c49`, `cell-21`.
2. **Use `_lu(A)` in cell `f8e57a48` instead of hardcoded literals** — eliminates dead code, unifies the algorithmic and visualization paths, and reinforces that LU is computed, not written by hand.
3. **Add the rank-1 SVD code cell** for cell `cell-18`'s described experiment, and add the zero-matrix assert to the `rank_from_svd` suite — two small additions that close the gap between what the markdown promises and what the student can actually run.
