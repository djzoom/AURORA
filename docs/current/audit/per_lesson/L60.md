# L60 — autograd 机制 — grad_fn 计算图，backward()，retain_grad 与梯度累积

**总体评级**: B- → B（修复后）| **问题数**: 1 critical / 3 major / 4 minor

## 修复记录（2026-06-29）

| 问题 | 修复方式 | 状态 |
|---|---|---|
| Critical: 标题承诺 retain_graph 原理但正文缺席 | 在 Section 3.5 新增 markdown + code cell，完整演示 `retain_graph=True`：双 loss 多次 backward、与 `retain_grad()` 的对比表、RuntimeError 对比示例，并加 assert 验证 | ✅ 已修 |
| Major: `a3_backward.ipynb` 旧文件名（5 处） | 全局替换为 `L56_backward_pass.ipynb` | ✅ 已修 |
| Major: 下一节引用 "M2-P3" 旧编号体系 | 替换为 "下一节（L61）" | ✅ 已修 |
| Major: 虚构的 "见 L64" 前向引用 | 改为"后续多任务训练章节将演示此用法" | ✅ 已修 |
| Minor (P0): stub `pass` 隐式返回 None | 改为 `raise NotImplementedError`；cell 11 检查格加 try/except 守卫 | ✅ 已修 |

## 概述
数学内容完全正确，代码可从头运行到尾无报错，手写 `Value` 引擎与 PyTorch autograd 对比的实验设计清晰直观。但课程标题承诺的核心主题"retain_graph 原理"在正文中几乎缺席（只出现于末尾一行脚注），实际内容覆盖的是 `retain_grad()`，两者是完全不同的 API；同时所有跨课引用均沿用已废弃的旧文件名，其中一处前向引用指向的 L64 不包含该主题。

## 亮点
- 计算图 ASCII 示意图（cell 3）直观，精确对应后续代码输出，leaf/non-leaf 对比有说服力。
- 梯度累积演示（cell 8）用两次 backward 的累积结果对比 `zero_()` 后的结果，场景真实，适合初学者形成肌肉记忆。
- cell 13 的 `Value` mini-engine 实现紧凑且正确，双图（梯度值对比 + 误差 log 坐标）呈现方式专业，tanh 饱和区梯度趋零的说明也到位。
- 验证格（cell 11）对 `None` 返回值有友好提示，不会直接崩溃。

## 问题列表

### 🔴 Critical

**[教学质量 / 标题与内容严重不符]** — 课程标题标注"retain_graph 原理"，但正文中 `retain_graph=True` 从未被演示、代码从未出现；实际教学的是 `retain_grad()`（保留非叶节点的 `.grad`），两者语义完全不同——前者保留计算图供多次 backward，后者保留中间节点梯度值。学生如果按照标题预期来学，将找不到任何对应内容。
**Fix**: 要么将标题改为"retain_grad 与梯度累积"以符合实际内容；要么新增一个独立 section，用具体代码演示 `loss.backward(retain_graph=True)` 的使用场景（如同时对同一图求 L1 和 L2 loss 的梯度），补全承诺的主题。

### 🟠 Major

**[课程衔接 / 跨课文件名过时]** — `a3_backward.ipynb` 在 cell 0（标题区）、cell 1、cell 9（练习提示）、cell 14（收束）共出现 4 次。根据仓库文件系统，该文件已重命名为 `L56_backward_pass.ipynb`（路径 `notebooks/6_deep_learning/L56_backward_pass.ipynb`），旧名已不存在。学生跟随引用将无法找到文件。
**Fix**: 全局替换为 `L56_backward_pass.ipynb`，并在 cell 0 的 Aurora 连接区补充正确 L-number 引用（`见 L56`）。

**[课程衔接 / 下一节引用使用旧编号体系]** — cell 14 写"下一节（M2-P3）"，但课程已迁移至 L-number 体系，下一节实际为 `L61_pytorch_nn.ipynb`（`nn.Module` 线性层）。"M2-P3"在当前目录结构中无对应文件。
**Fix**: 改为"下一节（L61）将构建第一个 `nn.Module` 线性层……"。

**[课程衔接 / 虚构的前向引用]** — cell 14 末尾写"`retain_graph=True`（用于多任务学习，见 L64）"，但检查 `L64_kws_train_eval.ipynb` 的全文，该文件不含 `retain_graph` 任何出现。这是一个捏造的跨课引用，指向错误目标。
**Fix**: 删除"见 L64"或修正为实际讲授 `retain_graph` 的课号；如果尚未有对应课，改写为"后续多任务训练章节将演示此用法"。

### 🟡 Minor

**[教学质量 / 缺少学习目标列表]** — 课程开头没有编号的"学习目标"段，只在 cell 0 有一句话的目标描述。其他课通常有明确的 3–5 条具体目标。
**Fix**: 在 cell 0 后插入 markdown cell，列出 3–4 条编号学习目标（例如：1. 能解释叶节点与非叶节点的区别…）。

**[练习设计 / stub 使用 pass 而非 NotImplementedError]** — cell 10 的 `verify_gradients()` 以 `pass` 结束，隐式返回 `None`。虽然 cell 11 对此做了防护，但语义上 `pass` 表示"空实现已完成"，而非"未实现"。
**Fix**: 改为 `raise NotImplementedError("请实现 verify_gradients")`。

**[代码可运行性 / 未使用的 import]** — cell 2 中 `import numpy as np`，但整个 notebook 中 numpy 从未被调用（Value 引擎用 `math`，验证用 `torch`）。
**Fix**: 删除 `import numpy as np`，或在 cell 13 的误差比较中显式用 `np.allclose` 替代手写 `abs(...) < 1e-6`。

**[代码可运行性 / 冗余 import 和死代码]** — cell 11 顶部 `import torch` 重复了 cell 2 的导入；同 cell 中定义的 `_ref_verify()` 从未被调用，既没有打印也没有与学生结果作对比，是纯死代码。
**Fix**: 删除重复的 `import torch`；要么删除 `_ref_verify()`，要么在验证通过后调用它并对比结果（`ref_da, ref_db = _ref_verify(); assert abs(da - ref_da) < 1e-9`）。

### 💡 建议

- cell 5 的 detach 代码示例展示"z.backward() 报错"，但只是注释说明，不会实际运行。可以用 `try/except RuntimeError` 捕获并打印 error message，让学生真正看到报错内容，强化直觉。
- 当前唯一练习（cell 10）只需 5 行代码，对 L60 这个位置的学生偏简单。建议新增第二个练习：让学生手写一个调用 `loss.backward(retain_graph=True)` 两次的示例，补全标题承诺的主题，并与 `retain_graph=False`（默认）的 RuntimeError 对比。

## 优先修复（前3项）

1. **补全 `retain_graph` 内容或修改标题**：标题与内容不符是最严重的问题。最快修复是将标题改为"retain_grad 与梯度累积"；完整修复是新增演示 `retain_graph=True` 的 section。

2. **替换所有 `a3_backward.ipynb` 为 `L56`**：4 处出现，全部指向不存在的旧文件名，任何按引用操作的学生都会遇到 404。

3. **修正 "M2-P3" 和 L64 引用**：下一节标注改为 L61，同时删除或修正虚构的"见 L64"前向引用，避免学生跟踪到错误目标。
