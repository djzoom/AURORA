# L16 — 行列式（determinant）与逆矩阵（inverse matrix）— det(A) 的几何含义（面积缩放），求逆与何时不可逆

**总体评级**: B- → B（修复后） → B+（2026-06-30 重新评级）| **问题数**: 1 critical / 3 major / 4 minor

## 修复记录（2026-06-29）
| 重新评级 2026-06-30：B → B+ | 多练习 + 余子式展开推导，达到 B+ 标准 | 📋 已记录 |

| 问题 | 修复方式 | 状态 |
|---|---|---|
| Critical: 两个 stub return None 导致 TypeError | 改为 raise NotImplementedError；调用处加 try/except NotImplementedError 守卫 | ✅ 已修 |
| Major: 奇异矩阵分支断言缺失 | cell 13 新增 singular=[[1,2],[2,4]] 的 det/inv 断言，含 NotImplementedError 守卫 | ✅ 已修 |
| Major: Aurora 连接引用不存在的 src/aurora/audio/features.py | 替换为概念性描述，注明为计划实现项，移除具体错误路径 | ✅ 已修 |
| Major: 学习目标 3/4 声称手动实现 3×3，代码区全用 numpy | cell 18-20 新增 det_3x3/inv_3x3 TODO 桩及 NotImplementedError 守卫断言 | ✅ 已修 |

## 概述

数学内容准确——2×2 行列式公式、余子式展开、伴随矩阵法逆矩阵以及补充例题数值均正确。教学叙事清晰，推理路线格式层次合理。但存在一个关键可运行性缺陷（None stub 导致 TypeError 而非 NotImplementedError），Aurora 连接指向了不存在的文件路径，奇异矩阵分支从未被断言测试，而且学习目标 3、4 声称要手动实现 3×3 行列式和逆矩阵，代码区却全部改用 numpy 完成。

## 亮点

- 补充例题 A = [[1,1,0],[1,2,1],[0,1,3]]，|A|=2、A⁻¹ = (1/2)[[5,-3,1],[-3,3,-1],[1,-1,1]] 数值均正确，`assert np.allclose(A @ inv_expected, np.eye(3))` 可通过验证。
- 行列式几何意义（有向面积）的叙述准确，"det 极小但非零时用条件数 kappa 而非 det 判断稳定性"是实践中正确的提醒。
- 参数实验 cell-20（A = [[a,1],[1,a]]，遍历 a∈{-2,-1,0,1,2,3}）直观展示了 a=±1 时奇异性，设计有趣。
- `aurora.laviz.matrix_4ways` 函数确认存在于 `src/aurora/laviz.py`（line 189），可视化不会报错。
- 学习目标 5 条编号完整，难度对 L16 在全程 99 课中的位置合适。

## 问题列表

### 🔴 Critical

**[可运行性]** — 两个函数 stub 均 `return None` 而非 `raise NotImplementedError()`。cell-11 在未实现前会在 `f'{got_det:.6f}'` 处抛出 `TypeError: unsupported format character`；cell-12 在 `np.round(None, 6)` 处同样抛 TypeError。学生看到的错误与练习意图无关，调试困难。

Fix: 将 stub 改为：
```python
def det_2x2(M):
    raise NotImplementedError("请实现：返回 a*d - b*c")

def inv_2x2(M):
    raise NotImplementedError("请实现：det=0 返回 None，否则返回 2×2 逆矩阵")
```

### 🟠 Major

**[练习设计 — 奇异情形断言缺失]** — 规格文档（cell-586d2ce0）明确说明 `M=[[1,2],[2,4]]` → `inv_2x2` 应返回 `None`，但整个 notebook 没有任何断言验证此行为。学生可以对奇异矩阵返回 `np.zeros((2,2))` 而通过所有现有测试。

Fix: 在 cell-12 后追加：
```python
singular = np.array([[1., 2.], [2., 4.]])
assert det_2x2(singular) == 0.0, "奇异矩阵的行列式应为 0"
assert inv_2x2(singular) is None, f"奇异矩阵应返回 None，实际得到 {inv_2x2(singular)}"
print("✅ 奇异矩阵 通过")
```

**[Aurora 连接 — 文件路径错误]** — cell-384b2424 中写道"Aurora 特征白化步骤（`src/aurora/audio/features.py`）"，但该文件不存在。`src/aurora/audio/` 下实际文件为 io.py / mel.py / mfcc.py / stft.py / transforms.py / windows.py。`src/aurora/music/features.py` 存在但其中没有任何白化（whitening）或 Mahalanobis 距离实现。这是学生可直接验证的假陈述。

Fix: 删除对具体文件路径的引用，改为概念性描述："Aurora 特征提取中若协方差矩阵奇异（det=0），需加正则项 ε·I 后才能安全求逆"，注明该功能为计划实现项。

**[练习设计 — 3×3 实现目标与代码不符]** — 学习目标 3 写"用余子式展开计算 3×3 行列式"，目标 4 写"用伴随矩阵公式求 3×3 逆矩阵"。但代码区 cell-580559c6 直接调用 `np.linalg.det` 求子式（minor），cell-17552ca7 直接调用 `np.linalg.inv`——两者均非手动实现。学生未被要求编写任何 3×3 的计算逻辑，目标与内容不符。

Fix: 添加 `det_3x3(M)` 和 `inv_3x3(M)` 的 TODO 桩，以 A = [[1,1,0],[1,2,1],[0,1,3]] 为验证案例，附 `assert abs(det_3x3(A) - 2.0) < 1e-9` 和 `assert np.allclose(inv_3x3(A), inv_expected)`。

### 🟡 Minor

**[课程衔接 — 缺少下节课 L 编号]** — "本课收束"（cell-22）写"下一节的特征分解"，未标明 L17，与课程其他 notebook 引用风格不一致。

Fix: 改为"下一节 L17（特征分解与对角化）会用 det=0 来识别奇异矩阵"。

**[练习设计 — cell-21 缺少代码 stub]** — cell-21 是纯 markdown，指导学生"构造 `A = np.random.rand(3, 3)` 多次、令 `A[1] = 2 * A[0]` 后对比 det 和条件数"，但其后直接接"本课收束"，没有对应的代码单元。描述性文字未被任何可运行代码承接。

Fix: 在 cell-21 之后插入代码 stub，包含注释框架和 `# TODO` 标记，让学生填写实验代码。

**[可视化 — 几何解释与图示不匹配]** — cell f0238891 的图示标题写"行列式 = 列张成的有向面积"，但 `matrix_4ways` 展示的是四格热力图（整体/元素值/列高亮/行高亮），并不可视化平行四边形或面积。且调用对象是 3×3 矩阵，对 3×3 矩阵行列式的几何意义应是体积缩放，而非"面积"。

Fix: 单独增加一个 2×2 单元，用 `matplotlib.patches.Polygon` 绘制两列向量张成的平行四边形，并在图中标注面积=det 值；`matrix_4ways` 可保留但移至另起一格，不挂在"有向面积"标题下。

**[代码重复]** — `import numpy as np` 在 cell-8、cell-10、cell-20 中重复出现，而 numpy 已在更早的 cell（310a578e）中导入。重复导入无害但可能误导学生认为每格必须自己导包。

Fix: 仅在 notebook 最顶部保留一次 `import numpy as np`，后续格删除重复 import。

### 💡 建议

- 在 cell-11 测试中补充对单位矩阵的测试 `assert det_2x2(np.eye(2)) == 1.0`，覆盖平凡情形。
- Aurora 连接可补充：`np.linalg.matrix_rank` 比单纯比较 det 与 0 更数值稳健；协方差矩阵加正则项 `ε·I` 后用 `np.linalg.solve(Sigma + eps*I, v)` 代替显式求逆是工程实践中更推荐的做法。
- 若保留 3×3 的 numpy 演示格，应将学习目标 3、4 的措辞从"实现"（implement）改为"理解并验证"（verify），避免目标与实际内容的错配。

## 优先修复（前3项）

1. **将两个 stub 从 `return None` 改为 `raise NotImplementedError()`**（cell-310a578e）— 否则学生在未实现前运行时看到的是无关的 TypeError，Critical 问题。
2. **添加奇异矩阵断言测试**（cell-12 之后）— `det_2x2([[1.,2.],[2.,4.]]) == 0` 和 `inv_2x2(...) is None` 是规格文档已承诺但从未验证的核心分支，Major 问题。
3. **修正 Aurora 连接文件路径**（cell-384b2424）— `src/aurora/audio/features.py` 不存在，学生查阅时会直接发现错误，损害 notebook 可信度，Major 问题。
