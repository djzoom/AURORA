# L57 — MLP 从零搭建 — 手写全连接层、激活函数、前向 / 反向完整实现

**总体评级**: B- → B（修复后） → B-（2026-06-30 重新评级） | **问题数**: 2 critical / 3 major / 4 minor → 已修 1 P1
## 修复记录（2026-06-29）
| 重新评级 2026-06-30：B → B- | 三个类桩均用 bare pass | 📋 已记录 |

| 问题 | 修复方式 | 状态 |
|---|---|---|
| P1: Neuron / Layer / MLP 各在独立 Cell，但 Neuron 和 Layer 缺少即时验证（只有 MLP 级别的综合测试） | Cell 9 后插入 Neuron 验证 Cell（4 个参数、__call__ 返回 Value）；Cell 11 后插入 Layer 验证 Cell（3 neurons、单输出返回 Value） | ✅ 已修 |


## 概述

本节整体结构合理，采用"演示 → 练习 → 验证"的推进节奏，数学内容（Value 自动微分、参数计数、梯度链路）完全正确。主要缺陷集中在两个方向：多处跨课引用使用了已废弃的旧命名方案（`p3_nn.ipynb`、`a3_autograd.ipynb`），以及 TODO 桩代码设计让学生调试体验较差（`pass` 代替 `raise NotImplementedError`，导致后续 `repr` / `len` 调用抛出语义模糊的 `AttributeError` / `TypeError`）。

## 亮点

- **数学完全正确**：`Value` 类中加法、乘法、幂次、tanh 的反向传播公式以及除法梯度验证（`dz/dx=0.5, dz/dy=−1.5`）均无误；参数总计 41 的手动推算表格清晰准确。
- **演示单元格（Cells 4、6、8）先于练习**：在要求学生实现 `Neuron/Layer/MLP` 之前，用裸代码手动演示了相同的运算，降低了概念门槛。
- **验证层次完整**：Cell 12 分三个断言检查前向输出类型、参数总数、反向传播梯度是否非零，覆盖了核心正确性。
- **注释中的"为何用 `other**-1` 而非 `Value(other.data**-1)`"**（Cell 2 `__truediv__`）：这条注释精准点出初学者最常见的节点连通性错误，教学价值高。

## 问题列表

### 🔴 Critical

**[课程衔接 / 引用]** — Cell 0 中的 Aurora 连接写道「`p3_nn.ipynb` 将用 PyTorch 复现相同的三层结构」，但该文件不存在；当前课程已全面重命名为 `L`-前缀，对应文件为 `L61_pytorch_nn.ipynb`。学生点击或搜索 `p3_nn` 将找不到任何内容。  
**Fix**：将 `p3_nn.ipynb` 改为 `L61_pytorch_nn.ipynb`。

**[课程衔接 / 引用]** — Cell 2 注释「来自 `a3_autograd.ipynb`」同样是废弃命名；对应当前文件为 `L54_value_autograd.ipynb`。  
**Fix**：将 `a3_autograd.ipynb` 改为 `L54_value_autograd.ipynb`。

### 🟠 Major

**[练习设计 / 桩代码]** — Cells 9、10、11 中所有 TODO 方法体均为 `pass`。当 `Neuron.__init__` 未实现时，`self.w` 从未赋值，而 `__repr__` 调用 `len(self.w)` 将抛出 `AttributeError: 'Neuron' object has no attribute 'w'`，而非清晰的"未实现"提示。同理，`parameters()` 返回 `None` 后 Cell 12 的 `len(params)` 将抛出 `TypeError`，绕过了精心设计的断言错误信息（`f"参数数量应为 41，实际 {len(params)}"`）。  
**Fix**：将所有实质性 TODO 方法体改为 `raise NotImplementedError`；或至少在每个方法的第一行加入占位返回（`return []` / `return None`），使断言能在正确的位置失败并给出有意义的消息。

**[代码可运行性]** — Cell 15（本课收束）写道「对应 Aurora 训练管道 `aurora/train.py` 里的网络主体」，但 `src/aurora/` 下不存在 `train.py`（现有子包为 audio、llm、music、speech、tts 等）。这是一个指向不存在文件的虚假 Aurora 连接。  
**Fix**：删除该错误引用，或改为正确的路径（若确实计划添加 `train.py` 则加注"（待实现）"）。

**[教学质量]** — 缺少正式的编号"学习目标"（学习目标）列表。Cell 0 只有一行 `**目标**`，而课程其他章节通常提供带编号的具体目标（e.g. "1. 能手写 Neuron.__call__ 并解释梯度回流路径；2. …"）。  
**Fix**：在 Cell 0 之后插入一个 Markdown 单元格，列出 3–4 条编号的具体学习目标。

### 🟡 Minor

**[练习设计]** — 验证断言不覆盖边界情况：`nin=1`（单权重神经元）、多次调用 `backward()` 而不清零梯度（梯度累积 bug）、以及列表 vs 标量输出的边界（`Layer` 在 `nout=1` 时应返回单个 `Value` 而非列表，此细节只在文档注释中提及，未被断言检验）。  
**Fix**：在 Cell 12 补充 `nout=1` 时 `isinstance(out, Value)` 的断言；补充"二次 backward() 后梯度翻倍"的警示性注释。

**[教学质量]** — Cell 7 和 Cell 15 提及梯度下降更新式 `p.data -= lr * p.grad`，但未提醒**每次更新后必须清零梯度**（`p.grad = 0.0`）。这是初学者最常犯的训练 bug，将在 L58 中直接使用，应在本节至少以注释形式提前预警。  
**Fix**：在 Cell 7 或 Cell 15 加一行注释说明梯度累积问题及清零的必要性。

**[练习设计]** — `Neuron` 仅通过 `nonlin=True/False` 支持 tanh 或线性两种激活，无法选择 `Value.relu()`（已在 Cell 2 实现但此后从未调用）。这对本节范围而言可接受，但缺少任何说明会令学生困惑。  
**Fix**：在 Cell 3 的 Markdown 中注明"本节只用 tanh，relu 的使用见 L58/L59"，或将 `nonlin` 参数扩展为接受字符串（`'tanh'`/`'relu'`/`'linear'`）。

**[课程衔接]** — `solutions/` 目录下有 L55、L56、L59–L64 的解答文件，唯独缺少 `L57_mlp_solutions.md`。  
**Fix**：补充标准解答文件，与其他练习课保持一致。

## 优先修复（前3项）

1. **修正两处旧命名引用**（Cell 0 的 `p3_nn.ipynb` → `L61_pytorch_nn.ipynb`；Cell 2 的 `a3_autograd.ipynb` → `L54_value_autograd.ipynb`）——学生无法找到被引用的文件，这是即时可见的破坏性错误。

2. **将 TODO 桩改为 `raise NotImplementedError`**（Cells 9、10、11 的所有 TODO 方法）——现有 `pass` 桩导致后续 `AttributeError`/`TypeError`，完全掩盖了断言消息，破坏了验证单元格的指导价值。

3. **删除或修正 `aurora/train.py` 引用**（Cell 15）——该文件在代码库中不存在，声称"对应 Aurora 训练管道主体"会误导学生去找一个不存在的文件。
