# L59 — PyTorch Tensor 基础 — 与 NumPy 互转、device、requires_grad

**总体评级**: B- → B（修复后） → B-（2026-06-30 重新评级） | **问题数**: 1 critical / 2 major / 4 minor → 已修 1 P2
## 修复记录（2026-06-29）
| 重新评级 2026-06-30：B → B- | mel_to_batch 赋值 arr=None/t=None/return None | 📋 已记录 |

| 问题 | 修复方式 | 状态 |
|---|---|---|
| P2: Cell 2 裸 `import torch` 无防护，PyTorch 未安装时立即崩溃 | 改为 `try: import torch … except ImportError: HAS_TORCH = False; print(⚠️)` | ✅ 已修 |


## 概述

L59 是一节扎实的 PyTorch 入门课，数学内容准确，梯度累加陷阱实验尤为精彩。核心问题在于工程层面：未使用的 `mfcc` 导入可能在某些环境中引发 NameError、唯一练习的存根返回 `None` 而非 `NotImplementedError`（导致测试输出令人困惑的 `AttributeError`），以及整节课只有一道 TODO 练习，大量涵盖的算子（view、cat、stack、einsum）缺乏动手机会。

## 亮点

- **梯度累加实验**（cell-13）设计精良：先展示"不清零会发生什么"，再展示"正确做法"，对比清晰，是同类教程中少见的主动揭陷阱写法。
- **Aurora 连接**（mel_to_batch）具体且真实：`(B, 1, T, n_mels)` 正是 Conv2d 期望的格式，和代码库实际路径对应，不是泛泛一句"用于神经网络"。
- **推理路线**（cell-9 三步骤）将 np.stack → from_numpy → unsqueeze 串成因果链，而非直接给实现，scaffold 质量高于课程平均水平。
- **数学全部正确**：x=3 时 dy/dx=8，3 次累加=24，no_grad 下 requires_grad=False，所有手算结论与代码一致。

## 问题列表

### 🔴 Critical

**[Runability] 顶层 `from aurora.audio.mfcc import mfcc` 导入后从未使用**
cell-2 导入 `mfcc`，但整个 notebook 没有任何地方调用它。`aurora.audio.mfcc` 在当前仓库中存在且可导入，故在主仓库环境中不会报错；但在学生克隆的干净环境（pip install 未完成）或测试环境中，这行代码会触发 `ImportError` 并阻断整个 notebook 运行，在任何后续内容执行之前就让学生卡住。
**Fix**: 删除 `from aurora.audio.mfcc import mfcc`。若确实想展示"MFCC 输出就是 mel_to_batch 的输入"这一联系，改用注释或在 cell-9 的说明文字中引用路径即可，不必实际导入。

### 🟠 Major

**[Exercise Design] 存根返回 `None` 而非 `raise NotImplementedError`**
cell-10 的三个 TODO 位都赋值 `None`，最终 `return None`。学生在未实现时运行 cell-11，得到的是 `AttributeError: 'NoneType' object has no attribute 'shape'`，而非明确的 `NotImplementedError`。错误信息与练习本身无关，对初学者尤其误导。CLAUDE.md 规范明确要求存根使用 `raise NotImplementedError`。
**Fix**:
```python
def mel_to_batch(mel_list):
    # ✏️ TODO: 用 np.stack 堆叠列表
    # ✏️ TODO: 转为 float32 Tensor
    # ✏️ TODO: unsqueeze channel 维度
    raise NotImplementedError("请完成 mel_to_batch 的实现")
```

**[Exercise Design] Section 3 算子组（view/cat/stack/einsum）缺少任何 TODO 练习**
cell-8 演示了 6 种算子，全是演示代码，学生无需动手。这些操作是后续 CNN/Attention 代码的基础，掌握与否直接影响 L60+ 的理解。仅 mel_to_batch 一道题对于一节完整课来说偏少。
**Fix**: 在 Section 3 末尾加 1–2 道填空题，例如：给定 shape (6, 4) 的 Tensor，用 view 改成 (2, 3, 4) 并用 einsum 实现 (4, 6) @ (6, 5) 的矩阵乘。

### 🟡 Minor

**[Runability] 变量名 `y2` 跨 cell 复用**
cell-8 定义 `y2 = torch.zeros(2, 3)`（用于演示 cat/stack），cell-13 重新定义 `y2 = x2 ** 2 + 2 * x2`。顺序执行不报错，但阅读时语义混乱，且若学生单独重运行 cell-13 而未运行 cell-8，`y2` 的含义会不一致。
**Fix**: 将 cell-13 中的变量重命名为 `y_accum`，与其语义（累加测试）对应。

**[Exercise Design] mel_to_batch 测试缺少边界情况**
cell-11 只测 shape 和 dtype，未覆盖：单元素列表（`[np.zeros((80,40))]`）、非零内容是否正确保留、输入为 float64 时输出是否正确转换为 float32。
**Fix**: 增加 `assert batch[0, 0, 0, 0].item() == 0.0` 内容校验，并加一行 `assert mel_to_batch([np.zeros((80,40))]).shape == (1, 1, 80, 40)`。

**[Pedagogical Quality] 学习目标未独立列出，粒度不足**
"目标"仅在 header 中以一句话带过（"掌握 torch.Tensor 的基础操作"），缺少编号的具体目标（对应 Section 1-5 各子项）。课程其他 lesson 通常有"学习目标"小节。
**Fix**: 在 cell-1 前插入：
```markdown
## 学习目标
1. 理解 Tensor 与 ndarray 的 dtype / device 差异
2. 能用 from_numpy / .numpy() 实现零拷贝互转
3. 掌握 view、unsqueeze、cat、stack、einsum 基本用法
4. 实现 mel_to_batch：numpy 列表 → CNN 输入 Tensor
5. 理解 requires_grad、梯度累加陷阱与 no_grad 上下文
```

**[Course Continuity] "本课收束"遗漏了 Section 3 的内容**
收束段落仅提及 mel_to_batch 和 requires_grad，未提 view/reshape/cat/stack/einsum，而这些操作在后续 L60+ 中会频繁出现。学生读收束时会误以为 Section 3 是次要内容。
**Fix**: 在收束首句补充："掌握了 view、cat、stack、einsum 等形状操作后，……"。

### 💡 建议

- **cell-4 共享内存演示**可更进一步：`t.numpy()` 反向转回也共享内存，可加一行 `t32.numpy()` 演示（并说明 float() 会复制，打破共享）。
- **Section 5 实验 A** 若能实际调用 `y_ng.backward()` 并 catch `RuntimeError`，比只打印 `None` 更有说服力，与课程"主动揭陷阱"风格一致。
- **solutions/ 目录**已实际存在（`L59_tensor_basics_solutions.md`），cell-9 的提示文字与事实相符，无需修改。

## 优先修复（前3项）

1. **删除未使用的 `from aurora.audio.mfcc import mfcc`**（Critical / Runability）—— 防止在干净环境中阻断 notebook 启动。
2. **将存根从 `return None` 改为 `raise NotImplementedError`**（Major / Exercise Design）—— 给学生清晰、可追溯的错误信息，符合 CLAUDE.md 规范。
3. **在 Section 3 后添加 1–2 道 TODO 练习**（Major / Exercise Design）—— view/cat/stack/einsum 是后续所有 CNN/Attention 代码的积木块，仅演示而不练习会导致学生在 L62+ 遇到形状 bug 时无从下手。
