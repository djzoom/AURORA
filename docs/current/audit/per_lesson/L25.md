# L25 — 梯度下降 — 用一条直线拟合数据，从损失函数到权重更新公式

**总体评级**: C+ → B-（修复后）| **问题数**: 1 critical / 4 major / 3 minor → B（2026-06-30）

## 修复记录（2026-06-29）

| 问题 | 修复方式 | 状态 |
|---|---|---|
| 🔴 cell-17/cell-19 重复代码单元（结构性遗留物） | 删除 cell-17（stray），将 cell-19 替换为多 lr 对比循环 | ✅ 已修 |
| 🟠 cell-11 答案泄漏（`next_x = x - lr * grad_value`） | 改为概念描述，移除具体公式 | ✅ 已修 |
| 🟠 cell-12 stub 返回 None 导致 TypeError | 改为 `raise NotImplementedError`；新增 cell-13 单步断言（try/except 守卫） | ✅ 已修 |
| 🟠 cell-16 Aurora 连接引用不存在的 `aurora/train.py` | 改为前瞻性描述（"Month 2 引入训练循环时将以 optimizer.step() 形式出现"） | ✅ 已修 |
| 🟠 参数实验章节承诺多 lr 对比但代码只用 lr=0.2 | cell-19 替换为 `for lr in [0.01, 0.2, 0.9, 1.1]` 对比循环 | ✅ 已修 |
| 🟡 Minor × 3（lr 收敛条件措辞、函数切换过渡、numpy import） | 本轮跳过 | — |

| 2026-06-30 | 第三轮修复 | B- | B | 补 5 个学习率收敛对比实验（log 曲线+bar图+断言） |
## 概述

L25 的核心数学内容正确——更新规则、MSE 偏导数、中心差分公式均无误——但存在一个严重的结构性错误：两个几乎完全相同的代码单元（cell-17 和 cell-19）出现在"本课收束"和"Aurora 连接"之后，使得课程在叙事上已经结束之后还有实验代码悬挂，是明显的复制粘贴遗留物。课程仅有一个 TODO 练习，且脚手架已将完整一行解答写入 markdown，实质上把练习变成了抄写；承诺展示不同学习率行为的"参数实验"章节从未兑现。Aurora 连接引用了不存在于代码库中的 `aurora/train.py`。

## 亮点

- **更新规则解释准确**："梯度指向函数上升最陡的方向；减去梯度就是朝下坡走"，精确解释了负号的来源。
- **直线拟合演示数学正确**：MSE 对 w 的梯度为 `2·mean(err·X)`，对 b 为 `2·mean(err)`，两处推导均正确；实验收敛到 w≈2.03, b≈0.89，接近真值 w=2, b=1。
- **TODO 说明中有具体数值样例**：`x=0, grad=-6, lr=0.1 → new x=0.6` 的具体计算是良好的规格说明。
- **收敛条件推导正确**：`|1-2·lr|<1` 对本例二次函数适用，逻辑链完整。
- `solutions/` 目录真实存在（`L25_gradient_descent_solutions.md`），提示不是空话。
- L26 引用正确（`L26_visual_calculus.ipynb` 文件实际存在）。

## 问题列表

### 🔴 Critical

**[Structure / Runability]** — 两个近乎完全相同的代码单元（`cell-17` 和 `cell-19`）出现在"Aurora 连接"和"本课收束"章节之后。两者均从 `x=-2.0`、`lr=0.2` 出发对 `f(x)=(x-3)²` 跑 8 步梯度下降，仅最后一行 print 字符串有微小差异，是明显的复制粘贴遗留物。课程叙事在 `cell-20`（本课收束）已经结束，之后还有实验代码，学生会困惑课程结构，且两个单元中的函数定义 `def f(x)` 会互相覆盖全局命名空间。Fix: 删除 `cell-19`，将保留的 `cell-17` 移入 Section 1 内适当位置；或完全删除两个单元，因为该演示在 `cell cell-17` 的 markdown 中已用文字覆盖。

### 🟠 Major

**[Exercise Design]** — 解答在 TODO 之前的 markdown 单元（`cell-11`）中已经完整泄露：bullet 点逐字写出"关键步骤：`next_x = x - lr * grad_value`"。学生只需将伪代码抄成 Python，没有任何推导或思考负担。Fix: 删除该 bullet 中的具体公式，改为"将移动量（lr 乘以梯度）从当前位置减去"这类概念描述，让学生自行形式化。

**[Exercise Design / Runability]** — `gd_step` stub 返回 `None` 而非 `raise NotImplementedError`。测试单元执行 `x = gd_step(x, 2*(x-3), lr)` 后 `x` 变为 `None`，下一次迭代执行 `2*(None-3)` 抛出 `TypeError: unsupported operand type(s) for -: 'NoneType' and 'int'`，错误信息与"尚未实现"完全无关，令学生困惑。Fix: 将 `return None  # ← 改这里` 改为 `raise NotImplementedError("请实现 gd_step")`。同时在测试单元前补加单步断言：`assert abs(gd_step(0.0, -6.0, 0.1) - 0.6) < 1e-9, "单步验证失败"`。

**[Accuracy / Aurora Reference]** — "Aurora 连接"章节（`cell-5b8da531`）写道"`gd_step` 对应 `aurora/train.py` 里 `optimizer.step()` 的核心逻辑"，但 `src/aurora/` 下没有 `train.py`（仅有 `audio/`、`llm/`、`mlops/` 等子包）。Fix: 删除该虚假路径引用，改为"当 Month 2 引入训练循环时，此更新规则将以 `optimizer.step()` 的形式出现"，或等 `train.py` 实现后再添加引用。

**[Pedagogical Quality / Exercise Design]** — "参数实验：lr 大小决定收敛还是震荡"（`cell-18` markdown）明确承诺展示 lr=0.9 的震荡行为和 lr=0.01 的缓慢收敛，但 `cell-17` 和 `cell-19` 都只用 `lr=0.2`，没有任何代码真正对比不同学习率。承诺和交付完全脱节。Fix: 在该章节增加对比循环，例如遍历 `lr in [0.01, 0.2, 0.9, 1.1]`，打印各 lr 下 8 步后的最终 x 值和收敛/发散状态。

### 🟡 Minor

**[Accuracy]** — Section 1 写"lr<1 时 |1-2·lr|<1，序列收敛"，但该结论对本例（Hessian=2）在 `0 < lr < 1` 时成立；当 lr 为负数时，lr<1 仍满足但序列发散（x 向远离极值点方向走）。Fix: 改为"0 < lr < 1 时"，或加注"以下假设 lr > 0"。

**[Structure]** — `cell-7`（对 `f(x)=x²`）和 `cell-9`（对 `f(x)=x²+2x`）演示数值微分，与本课主函数 `f(x)=(x-3)²` 不同，切换函数没有过渡说明。这两段内容属于 L22/L23（导数介绍）范畴，在梯度下降课中出现缺乏动机。Fix: 若保留，在该章节首行加一句"以更简单的 f(x)=x² 验证数值斜率与解析梯度吻合，同样规律适用于本课的 f(x)=(x-3)²"；或将函数统一为 (x-3)²。

**[Runability]** — 线性回归单元（`cell-3f5b9cce`）顶部只 `import matplotlib.pyplot as plt`，使用 `np` 依赖之前单元已导入的 numpy。若学生单独运行此单元，会得到 `NameError: name 'np' is not defined`。Fix: 在该单元顶部添加 `import numpy as np`。

### 💡 建议

1. 补充一个正式的编号 `## 学习目标` 小节（3-4 条）：本课目前仅有一句开头"目标"，与课程系列其他课不一致。
2. 在"本课收束"之前添加一个对 `gd_step` 的多步迭代封装练习（如 `gradient_descent(f_grad, x0, lr, n_steps) -> list`），使本课有两个递进难度的 TODO，而非只有一个近乎填空的单行练习。
3. 三个空 markdown 单元（`cell-2`、`cell-21`、`cell-22`）增加结构噪声，建议删除。

## 优先修复（前3项）

1. **删除重复单元并整理结构**：移除 `cell-19`，将 `cell-17` 移入 Section 1 内，消除"结论后还有实验代码"的叙事错乱——这是唯一的 Critical 问题。

2. **删除 cell-11 中泄露的解答公式**：将"关键步骤：`next_x = x - lr * grad_value`"改为概念描述，让 TODO 恢复作为真正练习的价值。

3. **修复 `aurora/train.py` 虚假引用**：一个不存在的文件路径会让学生在查找时立刻发现教材不可信；改为前瞻性描述或等模块实现后再添加。
