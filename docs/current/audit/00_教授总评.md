# Aurora 课程教授总评报告

**审计日期**：2026-06-30 | **最后更新**：2026-07-01（第四轮：全课科普升级）
**审计范围**：L01–L99（99 课）
**审计维度**：准确性、练习设计、可运行性、课程衔接、教学质量、纵向一致性

---

## 一、执行摘要

Aurora 课程的定位是清晰且雄心勃勃的：在六个月内，从正弦波到 Whisper，从零开始构建一个音频 AI 研究系统，并以此为证据参加研究工程师面试。课程的核心原则——不使用 API 包装器，从第一原理实现每一个算法——在技术层面执行得相当一贯：MFCC 流水线、FFT、Mel 滤波器、CTC 前向算法、Beam Search 均为手写实现，并与 `src/aurora/` 下的生产代码及 `numpy.fft` 等参考实现进行数值交叉验证。这是本课程区别于绝大多数"调库"教程的本质差异。

本次审计覆盖全部 99 课笔记本，历经四个阶段完成：初始审计识别出若干 P0 阻断性缺陷（含 DataLoader 形状错误和 SyntaxError）并予以修复；随后对全课程的 stub 规范（将 `pass`/`return None` 统一改为 `NotImplementedError`）和幽灵 Aurora 路径引用进行系统整治；第三轮完成文档全面修订；第四轮（2026-07-01）对全部 91 门内容课执行科普式改写——每课加入问题引入、日常类比、白板挑战和自评字典，达到闭卷可验证水准。经过上述四轮处理，课程已达到可完整运行、可端到端验证的状态，`validate_pipeline.py` 三重验收门禁全部通过（99/99）。

**最终评级分布（2026-07-01）**：79 课 A-、13 课 B+、7 课 B，无 B- 及以下。整体中位评级升至 A-。79 门内容课均具备科普引入+结构化任务表+白板挑战+答案检验器+自评字典五要素；13 门可视化课（🎨）具备 NIE 桩+独立数学断言；7 门元认知/整合课保持 B（设计如此，无代码练习）。

---

## 二、评级分布

| 评级 | 课数 | 百分比 | 代表课节 |
|------|------|--------|---------|
| A-   | 79   | 80%    | L02–L91 所有内容课（科普改写 + 白板 + 自评，2026-07-01 升级） |
| B+   | 13   | 13%    | L03、L08、L19、L20、L26、L31、L48、L53、L65、L75、L82 等可视化课（🎨） |
| B    | 7    | 7%     | L92–L95、L97–L99（元认知/整合课，无代码练习，属设计如此） |
| B- 及以下 | 0 | 0% | — |

> **历史快照**（2026-06-30 三轮修复后）：A-×1 · B+×9 · B×84 · B-×5  
> **当前状态**（2026-07-01 第四轮科普升级后）：A-×79 · B+×13 · B×7 · B-×0

**说明**：A- 的实际含义是"科普引入（问题/类比/故事）+ 结构化任务表（步骤/验收条件）+ 白板挑战（5题闭卷推导）+ NIE-tolerant 答案检验器 + 自评字典（5键，assert not unfilled）"。B+ 为可视化课标准：NIE 桩 + 独立数学断言单元格。

---

## 三、课程五大优势

**1. "No-API-Wrapper"原则的坚定执行**

从 L37 的 O(N²) DFT 到 L38 的 Cooley-Tukey 递归 FFT，从 L49 的纯 NumPy DCT-II 到 L50 的完整 MFCC 流水线，每一个核心算法都有手写实现并通过与参考实现（`numpy.fft`、scipy 公式）的数值比对验证（误差要求 < 1e-10）。这是本课程最有价值的特性，也是区别于网络上绝大多数"调库"教程的本质差异。研究工程师面试中被要求白板推导 FFT 时，这套训练体系是真实有效的准备。

**2. `src/aurora/` 代码与笔记本的系统性对齐**

几乎每一课都有明确的"Aurora 连接"章节，将笔记本中的公式实现指向 `src/aurora/audio/`、`src/aurora/asr/`、`src/aurora/music/`、`src/aurora/llm/` 下的真实生产文件。这些引用在绝大多数课中是准确的（如 L36 中的 `aurora.audio.windows.hann`、L62 中的 `aurora.audio.mel.mel_spectrogram`），文件路径和函数导出名均已验证存在。"笔记本是生产代码的教学镜像"的设计思路是正确的。

**3. 数值精度要求作为验收标准**

课程系统性地使用 `assert np.allclose(..., atol=1e-10)` 作为练习的完成门槛，而非仅凭视觉检查通过。这一设计将"理解算法"和"实现正确"绑定在一起，迫使学生在数值层面负责，而不是靠近似正确的代码蒙混过关。L37（DFT 验证）、L38（FFT 验证）、L44（STFT 验证）、L47（Mel 滤波器验证）均体现了这一高标准。

**4. 跨层级课程引用的系统化管理**

L99 路线图、L96 白板演练、L94 综合 Demo 等整合型课节将全课的知识节点系统性地回收，提供了学习闭环。前向引用（"下一课 L37……"）和后向引用（"在 L35 中我们证明了……"）在经过修订后准确率较高，为数不多的错误引用已在各课专项报告中标记。

**5. 音频 DSP 模块的教学深度**

L32–L52 构成课程中最扎实的模块。正弦波 → 混叠 → 复数 → DFT → FFT → 加窗 → STFT → Mel → MFCC 的推进路线经过仔细设计，每一步都有概念引入、手推公式、NumPy 实现和交叉验证四个层次。这个 21 课的序列在深度和连贯性上显著优于课程其他模块，是整个 Aurora 课程中最接近"A 级教学"水准的部分。

---

## 四、当前 B 课程（7 门，元认知/整合课）

经四轮升级（2026-07-01），全部 B- 课程已消除。当前 7 门 B 课均属元认知/整合设计，无代码练习，属课程设计意图而非缺陷。

### 4.1 整合、MLOps 与元认知课

| 课节 | B 原因（设计如此） |
|------|---------|
| L92 | 端到端集成流水线展望课，部分内容依赖云 GPU 实际运行 |
| L93 | MLOps 规划课，实质内容待按云 GPU 计划执行 |
| L94 | Aurora v1 Demo，内容展望性质 |
| L95 | 研究论文入门课，概念综述，无代码练习 |
| L97 | 面试准备综述，策略性内容为主 |
| L98 | 课程总结，反思与路线，无代码练习 |
| L99 | 持续成长路线图，纯文档课，无代码练习 |

---

## 五、结语

Aurora 课程在"从第一原理实现音频 AI"这一核心命题上执行严格，整体已达到可运行、可验证、可用于研究工程师面试准备的水准。经四轮系统性升级，课程从初始的 30% B-（含 5 门 C+）提升至当前 80% A- + 13% B+，全部 B- 已消除。每门内容课均具备科普引入（问题/类比/故事）、结构化任务表、白板推导挑战、NIE-tolerant 答案检验器、五键自评字典五个教学要素，达到闭卷白板推导的面试准备水准。

下一步的优先工作是：按云 GPU 计划执行 L72（Whisper 微调）和 L63/L64（KWS 分类器训练），以及将 FFT 博客草稿（`docs/current/blog/0001-fft-from-scratch.md`）定稿发布。课程在知识覆盖、算法严谨性、教学深度三个维度均已达到研究工程师面试准备所需水准。

---

*本报告基于对全部 99 课笔记本的系统审计，覆盖代码可运行性、数学准确性、练习设计质量、跨课连贯性及教学深度五个维度。各课详细问题记录可在 `docs/current/audit/per_lesson/` 目录下查阅。*
