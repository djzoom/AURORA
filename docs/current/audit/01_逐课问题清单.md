# 逐课问题清单

**审计日期**：2026-06-29 | **覆盖范围**：L01–L99 | **审计方式**：每课独立逐课精读

> **修复后状态（2026-06-30）**：本清单是原始审计问题记录，总览表中的评级为**修复前**评级。
> 修复后的最终评级见 `INDEX.md`（A-×4、B+×3、B×68、B-×24），修复项清单见 `00_教授总评.md`。

---

## 一、总览表

| 课节 | 标题（简） | 评级 | 🔴 | 🟠 | 🟡 |
|------|-----------|------|----|----|-----|
| L01 | 课程导论 | B- | 1 | 3 | 4 |
| L02 | 信号数字化 | B- | 1 | 2 | 2 |
| L03 | 三角函数基础 | B+ | 0 | 1 | 2 |
| L04 | 复数与欧拉公式 | B- | 1 | 2 | 3 |
| L05 | 复数运算 | B- | 1 | 3 | 3 |
| L06 | 欧拉公式 | B- | 0 | 2 | 4 |
| L07 | 傅里叶直觉 | B- | 2 | 4 | 3 |
| L08 | 复数可视化 | B- | 0 | 3 | 4 |
| L09 | 向量 | B- | 1 | 3 | 4 |
| L10 | 点积 | B- | 1 | 4 | 4 |
| L11 | 范数 | B- | 2 | 3 | 5 |
| L12 | 矩阵 | B- | 0 | 3 | 5 |
| L13 | 特殊矩阵 | B- | 1 | 3 | 5 |
| L14 | 特征值与 SVD | B- | 2 | 2 | 4 |
| L15 | 线性方程组 | B- | 1 | 3 | 4 |
| L16 | 行列式与逆 | B- | 1 | 3 | 4 |
| L17 | 特征值对角化 | B- | 1 | 4 | 4 |
| L18 | 可逆性 | B- | 1 | 4 | 5 |
| L19 | 矩阵乘法可视化 | B- | 1 | 3 | 4 |
| L20 | 矩阵分解可视化 | B | 0 | 2 | 4 |
| L21 | Aurora 矩阵应用 | C+ | 3 | 4 | 5 |
| L22 | 导数 | C+ | 3 | 4 | 5 |
| L23 | 梯度 | B- | 1 | 4 | 5 |
| L24 | 链式法则 | B- | 1 | 4 | 4 |
| L25 | 梯度下降 | C+ | 1 | 4 | 3 |
| L26 | 微积分可视化 | B- | 1 | 4 | 5 |
| L27 | 概率基础 | B- | 1 | 3 | 4 |
| L28 | 描述统计 | B- | 0 | 2 | 4 |
| L29 | 分布 | B- | 1 | 3 | 4 |
| L30 | Softmax 与交叉熵 | B- | 1 | 4 | 5 |
| L31 | 概率可视化 | B- | 2 | 3 | 4 |
| L32 | NumPy 信号 | B- | 2 | 2 | 4 |
| L33 | 正弦波 | B- | 2 | 3 | 5 |
| L34 | 混叠 | B- | 1 | 3 | 3 |
| L35 | DFT 从零实现 | B- | 2 | 4 | 4 |
| L36 | 窗函数 | B- | 1 | 3 | 5 |
| L37 | 帧与分析 | B- | 1 | 3 | 4 |
| L38 | FFT 蝶形运算 | B | 1 | 0 | 5 |
| L39 | 谱分析 | C+ | 1 | 3 | 5 |
| L40 | 相位谱 | C+ | 2 | 2 | 4 |
| L41 | 短时傅里叶变换 | B- | 1 | 2 | 4 |
| L42 | 音频信号分析 | B- | 2 | 3 | 3 |
| L43 | 卷积 | B- | 0 | 4 | 4 |
| L44 | 反射填充 | B- | 1 | 3 | 4 |
| L45 | 梅尔刻度 | B- | 1 | 4 | 5 |
| L46 | 梅尔滤波器组 | B- | 2 | 3 | 3 |
| L47 | 梅尔频谱图 | B- | 2 | 3 | 4 |
| L48 | 功率谱 | B- | 0 | 3 | 6 |
| L49 | DCT-II | B- | 1 | 2 | 4 |
| L50 | MFCC 实现 | B- | 1 | 2 | 5 |
| L51 | 音频 IO | B- | 0 | 3 | 6 |
| L52 | 音频可视化 | B- | 0 | 3 | 4 |
| L53 | MFCC 流水线 | B- | 1 | 3 | 3 |
| L54 | 神经网络基础 | B- | 2 | 3 | 5 |
| L55 | 前向传播拆解 | B- | 1 | 2 | 5 |
| L56 | 反向传播手推 | B- | 0 | 3 | 5 |
| L57 | MLP 从零实现 | B- | 2 | 3 | 4 |
| L58 | 训练循环 | C+ | 1 | 3 | 3 |
| L59 | PyTorch Tensor 基础 | B- | 1 | 2 | 4 |
| L60 | autograd 机制 | B- | 1 | 3 | 4 |
| L61 | nn.Module 实战 | B- | 1 | 2 | 5 |
| L62 | Dataset 与 DataLoader | B- | 1 | 3 | 5 |
| L63 | 音频分类模型 | B- | 1 | 4 | 5 |
| L64 | 训练评估闭环 | B- | 1 | 3 | 4 |
| L65 | 训练可视化 | C+ | 2 | 4 | 5 |
| L66 | ASR 系统全览 | B- | 1 | 2 | 4 |
| L67 | Edit Distance 实现 | B- | 1 | 3 | 4 |
| L68 | CTC 对齐原理 | B- | 1 | 3 | 5 |
| L69 | CTC 前向算法 | C | 3 | 4 | 4 |
| L70 | Whisper 架构解析 | B- | 2 | 4 | 3 |
| L71 | Whisper 解码策略 | C | 2 | 4 | 4 |
| L72 | Whisper 微调 | C+ | 3 | 4 | 5 |
| L73 | WER 评估 | B- | 2 | 5 | 4 |
| L74 | ASR 错误分析 | B- | 2 | 3 | 3 |
| L75 | ASR 流水线图形化 | C+ | 1 | 4 | 3 |
| L76 | 音乐理论速成 | C+ | 1 | 3 | 5 |
| L77 | 音乐特征工程 | B- | 1 | 3 | 5 |
| L78 | 节拍追踪实现 | C+ | 1 | 3 | 4 |
| L79 | 音乐嵌入向量 | B+ | 0 | 3 | 4 |
| L80 | 向量相似度检索 | B- | 1 | 3 | 3 |
| L81 | 音乐推荐系统 | B- | 2 | 4 | 3 |
| L82 | 音乐特征可视化 | C+ | 1 | 4 | 3 |
| L83 | Transformer 从零复现 | B- | 1 | 3 | 6 |
| L84 | LoRA 低秩适配 | B- | 1 | 2 | 3 |
| L85 | KV-Cache 从零实现 | C+ | 2 | 3 | 4 |
| L86 | 采样策略从零实现 | B- | 1 | 3 | 4 |
| L87 | 量化与本地推理 | C+ | 1 | 3 | 4 |
| L88 | TF-IDF 检索实现 | C+ | 3 | 3 | 4 |
| L89 | RAG 完整流水线 | C+ | 2 | 2 | 4 |
| L90 | 对话式 RAG | C+ | 3 | 4 | 3 |
| L91 | 注意力图解 | B- | 1 | 4 | 4 |
| L92 | 端到端流水线 | C+ | 1 | 4 | 5 |
| L93 | MLOps 基础 | C+ | 2 | 4 | 5 |
| L94 | Aurora v1 全景 Demo | B- | 2 | 3 | 4 |
| L95 | 研究论文入门 | B | 1 | 2 | 3 |
| L96 | 白板推导演练 | B+ | 0 | 3 | 4 |
| L97 | 面试准备技术沟通 | B- | 0 | 3 | 3 |
| L98 | 课程总结 | C+ | 1 | 3 | 4 |
| L99 | Aurora v2 持续成长 | B+ | 0 | 1 | 3 |

---

## 二、🔴 关键问题（Critical Issues）

> 每项均注明：问题类型 / 具体描述 / 修复方向

### L01 — 课程导论
**[标题-内容不符]** 课程规格（spec）列为"环境搭建"，notebook 实际内容为动机与路线图，两者完全不同。修复：对齐标题与实际内容，或更新课程规格。

### L02 — 信号数字化
**[内容缺失]** 标题承诺"NumPy 速成课"，但 notebook 实际讲授的是信号数字化（采样定理），dtype/broadcasting/切片全部缺席。修复：按标题补充 NumPy 速成内容，或修改标题为"信号数字化基础"。

### L04 — 复数与欧拉公式
**[可运行性]** 和弦演示单元格在学生实现 `sinusoid()` 之前无条件调用该函数，触发 `TypeError`。修复：将调用包在 `try/except NotImplementedError` 中，或将演示移至验证之后。

### L05 — 复数运算
**[可运行性]** `magnitude_phase` stub 返回 `None`，下一单元格立即解包导致 `TypeError: cannot unpack non-iterable NoneType`。修复：将 stub 改为 `raise NotImplementedError`。

### L07 — 傅里叶直觉（×2）
- **[标题-内容不符]** 标题标注"三角波叠加"，但全课仅使用正弦波；三角波内容完全缺席。
- **[数学错误]** 声称"值域约为 [-π/4, π/4]"，与代码断言直接矛盾。修复：更新标题，并修正值域描述。

### L09 — 向量
**[引用错误三重故障]** MFCC 参考错误叠加：函数名写成 `extract_mfcc`（正确：`mfcc`），导入路径错误，返回形状描述与实际不符。三处并存导致示例代码无法运行。修复：全部纠正为正确的函数名 `mfcc`、正确路径 `aurora.audio.mfcc`、正确形状。

### L10 — 点积
**[内容缺失]** 标题承诺"投影"内容，但 notebook 中完全不存在投影相关内容。修复：补充向量投影小节，或修改标题。

### L11 — 范数（×2）
- **[内容缺失]** 标题和学习目标均承诺"正则化含义"，但正文无任何正则化内容。
- **[可运行性]** stub 返回 `None`，立即触发 `TypeError`。修复：补充正则化内容；stub 改为 `raise NotImplementedError`。

### L13 — 特殊矩阵
**[数学错误]** 矩阵 `[[4,-2],[-2,1]]` 被标注为"不定矩阵"，但其特征值为 [0, 5]（半正定）。修复：修正标注为"半正定"。

### L14 — 特征值与 SVD（×2）
- **[答案泄漏]** 完整参考解在 TODO stub 出现前已三次全面展示，练习价值为零。
- **[可运行性]** 第二处 critical 问题（具体见 per_lesson/L14.md）。修复：将参考解移至隐藏/折叠单元格。

### L15 — 线性方程组
**[内容缺失]** Markdown 承诺代码单元格演示奇异矩阵实验，但紧跟的代码单元格根本不存在。修复：补充该单元格。

### L16 — 行列式与逆
**[可运行性]** 两个 stub 均返回 `None`，在任何断言运行之前格式化字符串即抛出 `TypeError`。修复：将 stub 改为 `raise NotImplementedError`。

### L17 — 特征值对角化
**[数学错误 + 可运行性]** 变量 `A` 被 2×2 演示矩阵覆盖，后续 `char_poly` 验证使用错误矩阵；参考值 `-384` 错误（正确：`+56`）。修复：重命名演示矩阵变量；修正参考值。

### L18 — 可逆性
**[语法错误]** f-string 使用嵌套同引号（PEP 701，Python 3.12+ 特性），在 Python 3.10/3.11 上抛出 `SyntaxError`，而项目要求 Python ≥ 3.10。修复：将嵌套引号改用不同引号或使用临时变量。

### L19 — 矩阵乘法可视化
**[练习设计]** 反射测试用例 `[[1,0],[0,-1]]` 同时也是对角矩阵，先检查对角性的学生断言失败，即便理解完全正确。修复：替换为纯反射矩阵（如 `[[0,1],[1,0]]`）。

### L21 — Aurora 矩阵应用（×3）
- **[数学错误]** 手写 mel 滤波器实现产生全零或冲激滤波器，原理上错误。
- **[内容一致性]** 多个 Markdown 单元格描述不存在的代码（陈旧内容未更新）。
- **[孤立单元格]** cell-23 是 L09 的算术内容，完全属于错误笔记本。修复：修正 mel 滤波器实现；删除孤立单元格；更新 Markdown。

### L22 — 导数（×3）
- **[引用错误]** `src/aurora/audio/grad_check.py` 不存在（正确文件：`transforms.py` 中的检验）。
- **[可运行性]** stub 返回 `None` → `TypeError`。
- **[描述-代码不符]** 三处文字描述 sin/cos，代码却使用多项式。修复：修正文件引用；stub 改 `raise NotImplementedError`；对齐描述与代码。

### L23 — 梯度
**[验证失效]** 正交性断言是数学恒等式（对任何实现均成立），无法检出错误实现。修复：替换为非平凡的数值验证，如计算具体向量的正交性偏差并断言 < 1e-9。

### L24 — 链式法则
**[可运行性 + 内容不一致]** stub 返回 `None` → `TypeError`；Markdown 描述 x=π/4，代码使用 x=1.3。修复：stub 改 `raise NotImplementedError`；统一示例值。

### L25 — 梯度下降
**[冗余内容]** cell-17 与 cell-19 几乎完全相同，出现在"本课收束"之后，属于复制粘贴残留。修复：删除其中一个重复单元格。

### L26 — 微积分可视化
**[事实错误]** "梯度箭头的方向就是 optimizer.step() 每次走的方向"——梯度指向上坡方向，optimizer 沿负梯度下坡，两者相反。修复：改为"梯度方向与 optimizer.step() 方向相反"。

### L27 — 概率基础
**[可运行性]** stub 返回 `None`，下一行 `abs(p - 1/6)` 立即触发 `TypeError`。修复：stub 改 `raise NotImplementedError`。

### L29 — 分布
**[兼容性]** `np.trapezoid` 是 NumPy 2.0 新增 API，项目要求 NumPy ≥ 1.24，在 NumPy 1.x 上抛出 `AttributeError`。修复：改用 `np.trapz`（兼容 1.x/2.x），或在 pyproject.toml 中将 NumPy 下限提升至 2.0。

### L30 — Softmax 与交叉熵
**[答案泄漏]** cell-25 以完整可运行的 `softmax` 实现默默覆盖了学生的 stub，此后任何验证均在参考实现上运行。修复：将参考实现移至解答单元格（`# SOLUTION` 标签）并折叠。

### L31 — 概率可视化（×2）
- **[引用错误]** `normalize_features` 和 `softmax_cross_entropy` 在 `src/aurora/` 中不存在，导入报 `ImportError`。修复：修正导入路径，或在对应模块创建这两个函数。

### L32 — NumPy 信号（×2）
- **[练习设计]** 练习单元格已包含完整实现，非 stub，学生不需写任何代码。
- **[可运行性]** Aurora import 失败的单元格保存时含有错误输出，首次运行即让学生困惑。修复：将练习改为 stub；清除保存的错误输出。

### L33 — 正弦波（×2）
- **[可运行性]** stub 返回 `None` → `TypeError`。
- **[隐藏差异]** notebook 使用 `round()`，aurora 源码使用 `int()`，行为不一致，但现有测试不能发现。修复：stub 改 `raise NotImplementedError`；统一取整方式。

### L34 — 混叠
**[可运行性 + 引用错误]** stub 返回 `None` → TypeError 连锁崩溃；"下一节（窗函数）"指向错误，下一节实际为 L35（欧拉公式）。修复：stub 改 `raise NotImplementedError`；更新引用。

### L35 — DFT 从零实现（×2）
- **[可运行性]** stub 返回 `None` → `TypeError`。
- **[引用错误]** `src/aurora/audio/dft.py` 不存在，正确文件为 `transforms.py`。修复：stub 改 `raise NotImplementedError`；修正文件路径。

### L36 — 窗函数
**[数学错误]** 声称 `hann(10)` 周期 Hann 窗"末端≈0，峰值索引=4"，但 Aurora 的周期 Hann 窗给出末端≈0.095，峰值索引=5。修复：更新文档中的参考值以匹配 Aurora 实际实现。

### L37 — 帧与分析
**[引用错误]** "本课收束"写"L39 FFT 蝶形运算"，但蝶形运算实际在 L38，不是 L39。修复：将 L39 改为 L38。

### L38 — FFT 蝶形运算
**[引用错误]** `aurora.audio.transforms._fft_recursive()` 不存在，实际实现为迭代式 `fft()`。修复：修正 Aurora 连接描述。

### L39 — 谱分析
**[数学错误]** cell-11 中 `[1,2,3,4,3,2,1,0]` 的参考 FFT 输出，8 个系数中有 4 个错误。修复：重新计算正确参考值并更新。

### L40 — 相位谱（×2）
- **[内容缺失]** 相位谱内容完全缺席，尽管标题和 L39 的前向引用均有承诺。
- **[违反原则]** 直接使用 `np.fft.fft` 而非 Aurora 手写 FFT，违反课程核心原则。修复：补充相位谱内容；改用 `aurora.audio.transforms.fft`。

### L41 — 短时傅里叶变换
**[数学错误]** 声称 Hann 窗"旁瓣比矩形窗低约 30 dB"，实际差值约 18 dB。修复：更正为 ~18 dB。

### L42 — 音频信号分析（×2）
- **[内容缺失]** 标题承诺"噪声对比"，但噪声频谱完全缺席。
- **[前后矛盾]** cell-9 文字说明 N=400，cell-10 代码使用 N=256。修复：补充噪声频谱；统一 N 的值。

### L44 — 反射填充
**[术语错误]** 全课将"反射填充（reflect padding）"描述为"补零（zero padding）"，两者在信号处理中是截然不同的操作。修复：统一使用正确术语"反射填充"。

### L45 — 梅尔刻度
**[验证失效]** 练习 stub 使用省略号字面量（`...`），是无操作的 Python 表达式，不会触发错误，导致验证单元格打印"✅ 验证通过"——但学生实际什么都没实现。修复：将 stub 改为 `raise NotImplementedError`。

### L46 — 梅尔滤波器组（×2）
- **[数学错误]** `hz_to_mel(700) ≈ 783`，正确值约 781（差值因公式版本而异，但描述与代码不一致）。
- **[引用错误]** L47 内容描述与 L47 实际内容完全不符。修复：修正计算值；更新 L47 的内容描述。

### L47 — 梅尔频谱图（×2）
- **[数学错误]** 梅尔频带中点声称 2700 Hz，实际约为 1730 Hz。
- **[可运行性]** cell-13 在未实现 stub 时崩溃。修复：修正中点值；为 stub 调用添加保护。

### L49 — DCT-II
**[答案泄漏]** 完整参考实现 `_dct_ii_ref` 在验证单元格中可见，学生仅需复制粘贴即可通过，练习意义为零。修复：将参考实现移至折叠的解答单元格。

### L50 — MFCC 实现
**[可运行性 + 逻辑错误]** stub 返回 `None` → `AttributeError`；cell-4 图例说"跳过 k=0"，其他所有单元格说"保留 k=0"，前后矛盾。修复：stub 改 `raise NotImplementedError`；统一 k=0 处理规范。

### L53 — MFCC 流水线
**[内容缺失]** `mfcc()` 函数被导入但从未调用；尽管标题承诺"完整 MFCC 流水线演示"，实际只有分步演示而无端到端调用。修复：补充 `mfcc()` 的完整调用并可视化输出。

### L54 — 神经网络基础（×2）
- **[可运行性]** 所有 TODO 方法使用 `pass`（而非 `raise NotImplementedError`），返回 `None` → `AttributeError`。
- **[引用错误]** `src/aurora/ml/` 不存在，Aurora 中无此包。修复：所有 stub 改为 `raise NotImplementedError`；修正 Aurora 连接路径。

### L55 — 前向传播拆解
**[练习设计]** `__pow__` 脚手架缺少 `_prev=(self,)` 和 `_op` 字段，与 `relu`/`tanh` 不一致；学生补全数据项后反向传播静默失败（梯度为 0.0），无任何报错。修复：预填 `Value(self.data**n, (self,), f'**{n}')`，只留 `self.data**n` 给学生填写。

### L57 — MLP 从零实现（×2）
- **[引用错误]** `p3_nn.ipynb`（应为 L61）和 `a3_autograd.ipynb`（应为 L54）均使用已废弃命名。修复：更新为正确 L 编号。

### L58 — 训练循环
**[逻辑错误]** `Neuron.__call__` 对包括输出层在内的所有层无条件施加 ReLU，导致输出分数恒 ≥ 0，负类样本 hinge loss 永不收敛，`acc > 0.85` 断言在任何正确实现下均无法通过。修复：给 `Neuron` 增加 `nonlin=True` 参数；`MLP` 最后一层以 `nonlin=False` 构造。

### L59 — PyTorch Tensor 基础
**[可运行性]** 顶层 `from aurora.audio.mfcc import mfcc` 导入后从未使用，在干净环境中（未完成 `pip install` 时）触发 `ImportError` 阻断整个 notebook。修复：删除该未使用导入。

### L60 — autograd 机制
**[标题-内容严重不符]** 标题注明"retain_graph 原理"，但正文从未演示 `retain_graph=True`；实际内容是 `retain_grad()`，两者 API 语义完全不同。修复：要么将标题改为"retain_grad 与梯度累积"；要么补充 `retain_graph=True` 的完整演示节。

### L61 — nn.Module 实战
**[可运行性]** `AudioMLP` stub 在 `__init__` 和 `forward` 中均使用 `pass`，返回 `None` → `AttributeError: 'NoneType' has no attribute 'shape'`。修复：将两个 `pass` 改为 `raise NotImplementedError`。

### L62 — Dataset 与 DataLoader
**[数学错误 / 形状错误]** `KWSDataset.__getitem__` 缺少转置且参数错误：输出形状为 `(63, 40)` 而非文档承诺的 `(40, 32)`，直接导致 L63 CNN 无法运行。修复：`mel_spectrogram` 调用后加 `.T`，并显式传递 `n_fft=2048, hop_length=512`。

### L63 — 音频分类模型
**[可运行性]** `KeywordCNN.__init__` 和 `forward` 均使用裸 `pass`，`super().__init__()` 从未执行，PyTorch 模块机制静默损坏。修复：将两处 stub 改为 `raise NotImplementedError`。

### L64 — 训练评估闭环
**[结构性教学错误]** cell-16 中 `val_loader` 与 `train_loader` 使用完全相同的数据，验证准确率等于训练准确率，无法演示课程承诺的"过拟合诊断"。修复：用 `torch.utils.data.random_split` 按 80/20 切分为独立验证集。

### L65 — 训练可视化（×2）
- **[内容缺失]** 标题、开篇目标和"本课收束"均承诺混淆矩阵，但全篇无任何混淆矩阵实现或可视化。
- **[结构错误]** "本课收束"重复出现两次，第一次出现在正文中间，叙事断裂。修复：补充混淆矩阵节或从标题删除承诺；删除多余的收束单元格。

### L66 — ASR 系统全览
**[可运行性]** `compute_wer` stub 以 `pass` 结尾，返回 `None`，下一行 `abs(compute_wer(...) - 0.0)` 触发 `TypeError`。修复：将 `pass` 改为 `raise NotImplementedError`。

### L67 — Edit Distance 实现
**[练习设计]** "✏️ 任务 1"和"✏️ 任务 2"均已完整预实现，无任何 `TODO`、`raise NotImplementedError` 或空白脚手架，学生无需编写任何代码。修复：将函数体（保留 docstring 和签名）替换为 `raise NotImplementedError("你的实现")`。

### L68 — CTC 对齐原理
**[数学错误]** cell-12 压缩率预测声称"~65-70%"，对于 V=30、T=50 的实验参数，实际期望压缩率约 93.6%，相差 4 倍。修复：将预测改为"~93-94%"并附上推导公式。

### L69 — CTC 前向算法（×3）
- **[可运行性]** `ctc_forward` 在构造 `lprime` 后立即 `raise NotImplementedError`，整个 notebook 从 Cell 6 起无法运行。
- **[验证失效]** 仅有 `np.isfinite(lp)` 和 `lp < 0` 两条断言，任何返回负数的错误实现均可通过。
- **[公式-代码域不一致]** Cell 1 公式用概率域（乘法），骨架代码用 log 域（加法），课文未说明转换方式。修复：重构 stub 为结构化三段骨架；添加暴力枚举参考对照；补充 log 域公式。

### L70 — Whisper 架构解析（×2）
- **[事实错误]** Cell 5 声称"stride=2×2，总降采样 4 倍"，实际 Whisper 编码器使用 stride=1+stride=2，总降采样为 2 倍（3000→1500 帧）。
- **[结构破损]** "本课收束"段落含悬空反引号，句子结构不完整。修复：修正为"2 倍降采样"；删除残缺句子。

### L71 — Whisper 解码策略（×2）
- **[可运行性]** Cell 5 抛出 `NotImplementedError` 后 `result` 未定义，Cell 7 `print(f'贪婪结果: {result}')` 触发 `NameError`，掩盖 beam search 输出。
- **[验证失效]** `beam_search` 任务零断言、"分数"从未定义，学生无法验证正确性。修复：Cell 7 加 try/except 或 sentinel；为 beam_search 定义分数并添加至少 2 条断言。

### L72 — Whisper 微调（×3）
- **[标题-内容不符]** 标题承诺"中文/方言数据适配实战"，正文使用英语 LibriSpeech，无任何中文内容。
- **[可运行性]** 数据集 split 名 `"test"` 不存在，正确为 `"test.clean"`，运行时抛出 `ValueError`。
- **[事实错误]** Aurora 连接声称默认参数"25 ms 帧长、10 ms 步长"，但 `aurora.audio.mel` 默认 `n_fft=1024`（64 ms）、`hop=256`（16 ms）。修复：补充中文适配节或修改标题；修正 split 名；更新 Aurora 连接描述。

### L73 — WER 评估（×2）
- **[内容缺失]** 标题、目标和说明均突出 "jiwer 对比"，但 `jiwer` 从未被 import 或引用。
- **[验证失效]** Cell 11 注释写期望 WER=0.40，但只 `print` 不 `assert`，错误实现可静默通过全部三条断言。修复：从标题删除"jiwer 对比"或补充对比演示；加 `assert abs(result['wer'] - 0.40) < 1e-9`。

### L74 — ASR 错误分析（×2）
- **[练习设计]** notebook 中零学生练习，所有函数预实现，无 `raise NotImplementedError`。
- **[引用错误]** `aurora.asr` 不存在（正确：`aurora.speech`）；`wer_decompose` 从未定义（实际函数名：`alignment`）。修复：增加至少一个带 stub 的练习；修正模块路径和函数名。

### L75 — ASR 流水线图形化
**[数学错误]** Markdown 中 CTC 路径示例 `H H I - I → HI` 错误：按 CTC 规则应解码为 "HII"（去重后保留 I-blank-I 中的两个 I）。修复：替换为合法示例，如 `H H - I - → HI`。

### L76 — 音乐理论速成
**[内容缺失]** 标题副标题含"音程"，但音程（半音距离、完全五度等）在正文、学习目标和练习中完全缺席。修复：补充音程小节；或从标题删除"音程"。

### L77 — 音乐特征工程
**[事实错误]** "Aurora 连接"声称 `aurora.music` "尚未创建"，但 `src/aurora/music/` 已完整实现并导出 `chroma_vector`、`rms_envelope`、`zero_crossing_rate` 等。修复：将描述改为"本节练习目的是理解 `src/aurora/music/features.py` 中已有实现的内部逻辑"。

### L78 — 节拍追踪实现
**[可运行性]** 两个 TODO stub 均立即 `raise NotImplementedError` 且在同一单元格内被调用，顶到底运行时 Cell 5 崩溃，`env`/`bpm`/`beats` 全部未定义，Cell 8 再次 `NameError`。修复：将每个 stub 的定义和调用拆入独立单元格，调用处加 `try/except NotImplementedError`。

### L80 — 向量相似度检索
**[可运行性]** `find_similar` 未实现时返回 `None`，cell-13 的 `assert avg_ms < 1.0` 在 `times=[]` 时触发 `np.mean([]) = nan`，报 `AssertionError: 检索太慢：nan ms`，给出完全错误的错误信息。修复：添加 `if not times` 守卫，跳过性能断言。

### L81 — 音乐推荐系统（×2）
- **[验证失效]** `isinstance(r, int)` 断言在正确的 `np.int64` 返回类型上失败（Python 3 中 `isinstance(np.int64(5), int) == False`）。
- **[数学错误]** EnCodec 压缩比：Markdown 写 640×，代码算出 160×，相差 4 倍。修复：断言改为 `(int, np.integer)`；Markdown 改为 160×。

### L82 — 音乐特征可视化
**[数学错误]** Chroma 音高类别公式 `p = round(12*log2(f/440)) mod 12` 缺少 +69 偏置项；对 A4=440 Hz 代入得 0（C），正确应为 9（A）。修复：补充 `+69`，即 `p = round(12*log2(f/440)+69) mod 12`。

### L83 — Transformer 从零复现
**[验证失效]** cell-19 定义本地 `sdpa` 函数并用于"完整前向传播"验证，完全绕过学生的 `scaled_dot_product_attention`——无论学生实现是否正确，验证均通过并打印"✅"。修复：将 cell-19 改为直接调用 `scaled_dot_product_attention`，删除本地 `sdpa` 定义。

### L84 — LoRA 低秩适配
**[引用错误]** Aurora 连接引用的两个文件均不存在：`src/aurora/asr/finetune.py`（`asr/` 包不存在）和 `src/aurora/llm/finetune.py`（`llm/` 下只有 `kvcache.py`、`retrieve.py`、`sample.py`）。修复：修正为实际存在的路径，或标注为"计划中"。

### L85 — KV-Cache 从零实现（×2）
- **[事实错误]** Cell 1 复杂度分析内部矛盾：声称"无缓存 O(T³)，有缓存 O(T²)"，随即又说"两者量级相同"。正确：总投影代价从 O(T²·d²) 降至 O(T·d²)，标题中"O(seq²)→O(seq)"指投影累积代价，需在正文中明确说清。
- **[验证失效]** Cell 6 `KVCache(n_heads=4, head_dim=64)` 与实际数据参数不匹配；`print('✅...')` 是无条件打印，无任何 `assert`。修复：重写复杂度分析；修正参数并添加数值正确性断言。

### L86 — 采样策略从零实现
**[可运行性]** Cell 8（可视化）在列表推导中无保护地调用两个未实现的 stub，触发 `NotImplementedError` 使 notebook 在测试单元格之前崩溃，学生看到的是原始 traceback 而非有意义的提示。修复：将可视化单元格移到测试单元格之后，或包在 try/except 中。

### L87 — 量化与本地推理
**[练习设计]** 全部代码（`quantize_int8`、`dequantize_int8`）均已完整预实现，学习目标"从零实现 INT8 量化/反量化"无法实现，学生只需运行无需编写任何代码。修复：将核心计算（scale、zero_point、x_q）替换为 `raise NotImplementedError`。

### L88 — TF-IDF 检索实现（×3）
- **[可运行性]** Cell 9 引用了只有在 cell 7 成功完成后才存在的 `matrix`/`vocab`；cell 7 抛 `NotImplementedError`，导致 cell 9 触发 `NameError`。
- **[练习设计]** `build_tfidf` 的 TODO 只提到"计算 TF 矩阵"，遗漏了 IDF 计算和 TF×IDF 乘积，学生按提示只实现 TF 也能通过形状检查。
- **[验证失效]** Cell 10 无论学生实现是否正确，始终无条件打印"✅ TF-IDF 检索验证通过"，因为它只运行参考实现，从不比较学生输出。修复：Cell 9 加守卫或将测试分离；补充完整 TODO 描述；添加 `assert np.allclose(matrix, ref_mat, atol=1e-5)`。

### L89 — RAG 完整流水线（×2）
- **[语法错误]** `format_rag_prompt` stub cell 含残留的三引号字符串未闭合，触发 `SyntaxError: unterminated triple-quoted string literal`，单元格无法执行。
- **[单元格顺序错误]** 两个验证单元格（`format_rag_prompt` 测试和端到端测试）出现在它们依赖的定义之前，以 `NameError` 静默跳过，误导学生认为是"未实现"而非"布局错误"。修复：删除残留代码；重排单元格顺序。

### L90 — 对话式 RAG（×3）
- **[可运行性]** `from aurora.llm import build_rag_index, retrieve` — 这两个名称在 `aurora.llm.__init__` 中不存在（正确为 `build_tfidf`/`cosine_retrieve`），`ImportError` 被静默捕获，notebook 始终在 stub 模式运行，学生永远无法调用真实检索代码。
- **[引用错误]** L89"本课收束"预告 L90 为"Tool-calling Agent"，实际内容为对话式 RAG，主题完全不同。
- **[引用错误]** Cell 0 写"供 L6 的评估流水线调用"，L6 是欧拉公式课，完全无关（应为 L93/L94 或类似）。修复：更正导入名称；更新 L89 的前向引用；修正 L6 为正确课号。

### L91 — 注意力图解
**[标题-内容不符]** 标题声称"多头注意力（MHA）权重热力图"，但 notebook 只实现单头 `scaled_dot_product_attention`，无任何多头机制（分割、并行、拼接、投影）。修复：标题改为"Self-Attention 权重热力图"，或补充真正的 2 头 MHA 演示。

### L92 — 端到端流水线
**[事实错误]** Aurora 连接和"本课收束"断言"完整串联四个模块，形成 Aurora v1 系统闭环"，但三个被引用的模块（`aurora.speech.whisper`、`aurora.rag.retriever`、`aurora.llm.agent`）均不存在于代码库。修复：修改 Aurora 连接为实际存在的模块（`aurora.llm.retrieve`/`aurora.llm.sample`），注明 speech/rag 模块为"计划中"。

### L93 — MLOps 基础（×2）
- **[代码错误]** p95 计算使用 `latencies[int(0.95 * n_trials)]`，对 n_trials=20 得到索引 19，即最大值（p100），而非 p95。学生照抄进生产监控将触发错误告警。
- **[引用错误]** Dockerfile 的 `ENTRYPOINT ["python", "-m", "aurora.serve"]` — `aurora.serve` 在代码库中不存在，实际构建运行即报 `ModuleNotFoundError`。修复：修正为 `int(math.ceil(0.95 * n_trials)) - 1`；修正 ENTRYPOINT 为实际存在的入口。

### L94 — Aurora v1 全景 Demo（×2）
- **[引用错误]** 能力矩阵中 7 条路径均与真实代码库不符（`train/kws.py`→应为 `mlops/`；`asr/ctc.py`→应为 `speech/`；`music/embed.py`、`music/recommend.py`、`rag/retriever.py`→均不存在；`pipeline/runner.py`→包不存在）。
- **[术语错误]** WWHR 框架缩写与展开不对应：展开含 5 个组件（What/Why/How/Results/Limits），缩写只有 4 个字母（缺 L）。修复：更新 7 条路径为实际存在的文件；改为 WWHRL 或合并 Results & Limits。

### L95 — 研究论文入门
**[结构错误]** `## 5. 文献管理` 和 `## 5. 与学术机构合作` 两节均标注为第 5 节，导致目录导航混乱，后续章节编号全部错位。修复：将第二个"5"改为"6"，后续所有章节号顺移。

### L98 — 课程总结
**[结构错误]** 章节编号序列为 1, 2, 4, 4, 6, 6, 7——跳过了第 3 和第 5 节，第 4 和第 6 节各出现两次，且副标题承诺的"方法论总结"和"成长曲线量化"两节内容均缺失。修复：重新按 1–7 线性排列所有章节；补充"方法论总结"节和可视化代码。

---

## 三、🟠 重要问题（Major Issues）

> 以下每条均影响教学正确性或课程流畅性，但不致命阻断运行。

### L01 — 课程导论
- 学习目标罗列了 10 项，实际 notebook 只覆盖 4 项；未列出的 6 项内容缺失。
- Aurora 连接引用了不存在的 `aurora.demo_pipeline`。
- 缺少推荐阅读材料与 CLAUDE.md 的显式对应说明。

### L02 — 信号数字化
- `sinc` 插值推导未完成，markdown 中公式被截断。
- 没有声明 NumPy vs 标准库的使用约定，与课程其余部分不一致。

### L03 — 三角函数基础
- `plot_phase_shift` 函数接受任意 `phi` 但 markdown 仅演示 0/π/2π 三种，学生缺乏足够的泛化验证。

### L04 — 复数与欧拉公式
- 复数极坐标的 Markdown 说明将"幅度"与"相位"顺序写反。
- 没有连接到 aurora.audio（明显的入口点应是 `transforms.py`）。

### L05 — 复数运算
- `complex_multiply_vis` 中的可视化坐标轴标签被截断，无法读取。
- 验证断言精度过高（`atol=1e-15`），浮点误差可能偶发失败。
- 缺少 `abs(z)` 与 `np.abs(z)` 一致性对比。

### L06 — 欧拉公式
- `e^(iπ) + 1 = 0` 推导正确但未数值验证；课文声称 `exp(iπ) = -1+0j`，但 `np.exp(1j*np.pi)` 实际返回 `-1+1.2e-16j`，学生需知道浮点虚部非零。
- 下一节引用使用 "week01/day5" 格式（旧命名），应改为 L07。

### L07 — 傅里叶直觉
- 4 处频率合成公式使用 "f₁ + f₂" 记号但从不说明 f₁ ≠ f₂ 的约束。
- 复杂可视化使用超出 notebook 范围的全局变量。
- 收束未引用下一节 L08。
- `freq_bins` 列表越界风险未处理。

### L08 — 复数可视化
- 三处交叉引用使用过时编号（"day3"/"week01/day4"）。
- `make_animation` 依赖 `IPython.display`，在非 Jupyter 环境下崩溃。
- 学习目标缺少编号，仅为单句。

### L09 — 向量
- `normalize` 的零向量边界情况未处理。
- 标量投影（scalar projection）与向量投影（vector projection）混用，术语不一致。
- 无 `aurora.audio` 连接说明向量在 DSP 中的具体用途。

### L10 — 点积
- Gram-Schmidt 正交化只展示了两向量的情况，未推广到 n 维（标题暗示一般情形）。
- 投影完全缺失（见 Critical 项），同时与 L11 的范数内容存在边界不清问题。
- 无编号学习目标。

### L11 — 范数
- L1 范数与 L∞ 范数各有一处公式符号书写错误（应为 max|xᵢ|，写成了 Σ|xᵢ|max）。
- 矩阵范数小节缺少 Frobenius 范数与谱范数的区别说明。

### L12 — 矩阵
- 转置性质 `(AB)ᵀ = BᵀAᵀ` 仅给出陈述，未数值验证。
- 行列式计算展示在 `np.linalg.det` 上，但课程约定要手写核心算法，违反 CLAUDE.md 精神。
- 无编号学习目标。

### L13 — 特殊矩阵
- 对称矩阵与正定矩阵的关系说明顺序颠倒（先定义正定，再解释对称，与数学依赖顺序相反）。
- 随机正交矩阵生成方法（QR 分解）未说明为何保证正交性。

### L14 — 特征值与 SVD
- 矩阵幂方法（power iteration）代码有静默失败风险：迭代次数硬编码为 100，无收敛判据。
- 低秩近似误差未与理论 Eckart–Young 定理对比。

### L15 — 线性方程组
- `np.linalg.solve` 用于求解后，未展示对欠定/超定系统的情形（标题暗示会涵盖）。
- 高斯消元手写实现缺少主元选取（pivoting），数值不稳定。

### L16 — 行列式与逆
- 2×2 行列式的记忆口诀在 markdown 中拼写有误（"ad-bc" 写成 "ab-cd"）。
- `np.linalg.inv` 的数值精度警告未提及（小行列式矩阵求逆的数值问题）。

### L17 — 特征值对角化
- 手写特征多项式计算限于 2×2，声称可推广至 n×n 但未展示 n=3 的情形。
- 缺少对相似变换的物理解释。

### L18 — 可逆性
- Rank-Nullity 定理缺少数值示例。
- `np.linalg.matrix_rank` 使用默认精度，对接近奇异矩阵的判断不稳定，未提示此风险。

### L19 — 矩阵乘法可视化
- 几何变换组合顺序的文字说明（"先旋转再缩放"）与代码矩阵乘法顺序（`S @ R`）不对应，列主序 vs 行主序容易混淆。
- 动画单元格调用 `FuncAnimation` 但未设置 `interval`，在不同平台速度不一致。

### L20 — 矩阵分解可视化
- PCA 降维示例直接用 `np.linalg.eig` 而非 `np.linalg.eigh`（对称矩阵专用，更稳定），未解释选择理由。
- 无编号学习目标。

### L21 — Aurora 矩阵应用
- mel 滤波器完整实现错误（见 Critical），同时 `src/aurora/audio/mel.py` 中的参考版本未被比较（Aurora 连接断开）。
- STFT 部分引用了内部函数 `_frame_signal`，该函数不在公开 API 中。
- 四处交叉引用使用旧 "M1" 模块命名。

### L22 — 导数
- 数值差分 vs 解析梯度的误差比较缺少数量级说明（h=1e-4 的精度 vs h=1e-7）。
- 雅可比矩阵（Jacobian）出现在收束表格中但正文完全未介绍。

### L23 — 梯度
- 梯度下降的步长（step size）描述与 L25 的学习率（learning rate）术语不统一，同一概念两种名称。
- 向量化梯度（`∂L/∂W`）描述中转置方向有歧义（行梯度 vs 列梯度）。
- 梯度流图（计算图）在此节首次出现，但未提示 L55-L56 将从零实现它。

### L24 — 链式法则
- 例题 x=π/4 vs 代码 x=1.3 的不一致（见 Critical）；此外，链式法则推导中分子分母消去记号不明确。
- 无 Aurora 连接。

### L25 — 梯度下降
- 学习率衰减（learning rate schedule）在 markdown 中提到但完全没有代码实现。
- 随机梯度下降（SGD）vs 批量梯度下降的区别仅一句话，缺少代码对比。

### L26 — 微积分可视化
- "梯度箭头"的事实错误（见 Critical）之外，可视化颜色映射（colormap）未说明色彩含义（高/低值对应红/蓝）。
- 无 Aurora 连接（梯度计算直接关联 `aurora/llm` 的训练循环）。

### L27 — 概率基础
- 条件概率 `P(A|B)` 和贝叶斯定理的推导正确，但无数值示例验证，学生无法自测。
- 连续随机变量的 PDF 积分使用 `np.trapz` 而非精确积分，误差未量化。

### L28 — 描述统计
- IQR 计算使用 `np.percentile(x, 75) - np.percentile(x, 25)`，但 NumPy 的 percentile 有多种插值方法（默认 linear），与教材定义可能不同，未说明。
- 偏度（skewness）公式展示正确但代码与 `scipy.stats.skew` 结果未比较。

### L29 — 分布
- 除 NumPy 兼容性问题（Critical）外，中心极限定理演示样本量硬编码为 n=30，未探究何时近似足够好。
- 正态分布的参数化（均值 vs 方差 vs 标准差）在不同单元格之间符号不统一（σ 和 σ²混用）。

### L30 — Softmax 与交叉熵
- 数值稳定 softmax 的 `max` 减法在 Markdown 已说明，但交叉熵损失的数值稳定版本（`log_softmax` 合并计算）未介绍。
- 多类和二类交叉熵的关系（BCE vs CE）未明确。

### L31 — 概率可视化
- 虚假模块导入（见 Critical）之外，直方图 bin 数量硬编码，不随数据量缩放。
- 蒙特卡洛估计 π 的部分结论（收敛到 3.14）依赖随机种子，在某些种子下偏差超过 1%。

### L32 — NumPy 信号
- 练习已预实现（见 Critical）；此外，`np.hanning` vs `np.hanning(n+1)[:-1]` 的周期/对称区别未说明。

### L33 — 正弦波
- 采样定理的演示使用固定示例频率，未探究奈奎斯特频率附近的边界行为。
- `aurora.audio.io.read_wav` 的返回值（data, sample_rate）与 soundfile 不同，未说明。

### L34 — 混叠
- 采样率计算使用 `int(sr)` 而非 `round(sr)`，对非整数采样率可能引入静默误差。

### L35 — DFT 从零实现
- `dft.py` 引用错误（见 Critical）之外，DFT 的计算复杂度 O(N²) vs FFT 的 O(N log N) 未数值实测对比（只有公式）。
- 无 aurora 测试验证：实现与 `transforms.py` 的 `fft()` 数值一致性未检查。

### L36 — 窗函数
- 周期 vs 对称 Hann 的差异（见 Critical）之外，矩形窗的谱泄漏（spectral leakage）直观演示缺失。
- 各窗函数的应用场景（分析 vs 合成）仅一行文字，无代码示例。

### L37 — 帧与分析
- 固定步长分帧未处理信号末端短帧（padding 策略）。
- 与 `aurora.audio.stft.frame_signal` 的一致性验证缺失。

### L38 — FFT 蝶形运算
- `_fft_recursive()` 引用错误（见 Critical）之外，无编号学习目标。
- 时间复杂度的推导（T(N) = 2T(N/2) + O(N)）仅有公式，无递推树图示。

### L39 — 谱分析
- 参考输出错误（见 Critical）之外，零填充（zero-padding）对频率分辨率的影响未演示。

### L40 — 相位谱
- 内容缺失和 `np.fft.fft` 使用（见 Critical）之外，相位解包（phase unwrapping）完全未提及。

### L41 — 短时傅里叶变换
- 旁瓣衰减数据错误（见 Critical）之外，STFT 的可逆性（Griffin-Lim）未说明。
- 缺少对 hop_length 选择的实用建议。

### L42 — 音频信号分析
- 噪声缺失和 N 不一致（见 Critical）之外，频谱分贝刻度 vs 线性刻度的选择未说明。

### L43 — 卷积
- 线性卷积 vs 循环卷积的区别仅在 markdown 说明，无代码演示（这是核心概念之一）。
- `scipy.signal.convolve` 在核心 Audio 模块中使用，违反 CLAUDE.md 原则（应使用 NumPy 实现）。

### L44 — 反射填充
- 术语错误（见 Critical）之外，反射填充 vs 对称填充（`reflect` vs `symmetric`）的区别未说明，aurora 实际使用哪种未明确。

### L45 — 梅尔刻度
- stub 设计错误（见 Critical）之外，线性频率刻度与梅尔刻度的感知对应关系缺少参考文献。
- aurora 实现的梅尔公式（HTK formula）与 Slaney formula 的差异未说明。

### L46 — 梅尔滤波器组
- 计算值错误（见 Critical）之外，三角滤波器的面积归一化（area normalization）vs 幅度归一化的区别未说明。

### L47 — 梅尔频谱图
- 中点错误（见 Critical）之外，`power_to_db` 的参考值（`ref`）参数在 aurora 和 librosa 的不同默认值导致数值差异未说明。

### L48 — 功率谱
- 功率谱估计（Welch method）代码使用 `scipy.signal.welch`，违反 CLAUDE.md（应手写分段平均）。
- 无编号学习目标。

### L49 — DCT-II
- 参考实现泄漏（见 Critical）之外，DCT-II 与 DFT 的关系（通过镜像对称延伸）缺少说明。

### L50 — MFCC 实现
- 多处 stub 问题（见 Critical）之外，Delta 特征（一阶差分 MFCC）和 Delta-Delta 完全缺席，与实际 ASR 特征不完整。

### L51 — 音频 IO
- `aurora.audio.io.read_wav` 的立体声自动下混逻辑未验证（`data.reshape(-1, n_channels).mean(axis=1)` 的精度）。
- 24-bit WAV 文件的支持未说明（aurora 使用 soundfile，理论支持但未测试）。
- 无编号学习目标。

### L52 — 音频可视化
- 频谱图的时间轴和频率轴标签在某些 matplotlib 版本中被截断（布局问题，`tight_layout` 未调用）。
- `laviz.py` 的 API 与 notebook 中直接调用 matplotlib 不一致，未说明何时用哪种方式。

### L53 — MFCC 流水线
- 管道演示缺失（见 Critical）之外，`aurora.audio.mfcc.mfcc` 的完整参数签名未在此课程中展示，学生不知道如何调整帧长/步长/滤波器数。
- 无 Aurora 连接中对函数签名的说明。

### L54 — 神经网络基础
- `aurora/ml/` 不存在（见 Critical）之外，`nn.Module` 的 `forward` 方法机制未说明（为什么调用 `model(x)` 触发 `forward`）。

### L55 — 前向传播拆解
- `__pow__` 脚手架问题（见 Critical）之外，`cell-13` 使用 `x1`/`x2` 跨两次 `backward()` 导致梯度累积，演示输出有误导性。修复：relu 对比演示改用新建 `x1r`/`x2r` 对象。
- `__truediv__` 依赖 `__pow__`，但 `__pow__` 是 TODO，依赖关系未说明。

### L56 — 反向传播手推
- L55 的完整 `backward()` 实现已完全泄漏（与 L56 解答文件逐字相同），L56 练习形同重复抄写。修复：在 L55 将 `backward()` 改为 stub；或明确将 L56 定位为"引导式复现"。
- Cell 11 在 stub 未实现时抛 `AssertionError`，TA 无法干净运行 notebook。
- Aurora 连接引用的 `aurora/train/` 不存在。

### L57 — MLP 从零实现
- 旧命名引用（见 Critical）之外，`MLP` 类的 `__repr__` 方法格式在 Python 3.10/3.11/3.12 行为存在细微差异，未测试。

### L58 — 训练循环
- ReLU bug（见 Critical）之外，`solutions/L58_training_loop_solutions.md` 不存在，cell-9 提示参考此文件但学生无法找到。
- `a4_mlp.ipynb` 旧命名引用（应为 L57）。

### L59 — PyTorch Tensor 基础
- 未使用导入（见 Critical）之外，Section 3 算子组（view/cat/stack/einsum）六种算子全为演示，无任何 TODO 练习，对于后续 CNN/Attention 的基础却没有动手机会。

### L60 — autograd 机制
- 标题与内容不符（见 Critical）之外，`a3_backward.ipynb` 旧命名出现 4 次（应为 L56）；"下一节 M2-P3"应为 L61；虚构引用"见 L64"（L64 不含 `retain_graph`）。

### L61 — nn.Module 实战
- stub 问题（见 Critical）之外，`register_buffer()` 完全未讲授，学生学到了"不该做什么"（裸 tensor）但没有正确替代方案。
- 三处"a4_mlp"旧命名引用应更新为"L57"。

### L62 — Dataset 与 DataLoader
- 形状错误（见 Critical）之外，`power_to_db` 在 `KWSDataset` 中使用但 `extract_features` 规格中未提及，导致两条路径数值不一致。
- 形状断言仅检查 ndim=3，不检查具体 shape。

### L63 — 音频分类模型
- stub 问题（见 Critical）之外，Cell 11 只测 `n_classes=10`，硬编码 10 的错误实现可通过。
- T=101（声称的典型值）与代码中 T=81 不一致。
- 标题声称"在 Speech Commands 上定义网络"但数据集从未出现。

### L64 — 训练评估闭环
- 数据泄漏（见 Critical）之外，cell-9 初始损失预期"≈1.5"与 cell-15 的"≈1.39（ln4）"矛盾；应统一为 ln(4)≈1.386。
- 两个核心函数的验证断言过弱（只检查类型和正负，不检查数值合理性）。

### L65 — 训练可视化
- 内容缺失（见 Critical）之外，Aurora 连接三处引用使用旧 `month02/aX` 命名（应为 L54、L55、L58）。
- 梯度范数条形图 x 轴标签截断为最后一个 `.`，多层同名参数无法区分。
- 整课无任何学生 TODO 练习。

### L66 — ASR 系统全览
- stub 问题（见 Critical）之外，`compute_wer` 练习要求实现 DP，但 Levenshtein DP 基础在下一课 L67 才讲授，顺序倒置。
- 标题承诺 AM→LM→Decoder 全面讲解，实际内容以 WER 为主，占比失衡。

### L67 — Edit Distance 实现
- 练习全预实现（见 Critical）之外，`wer()` 内部完整复制了 `edit_distance()` 的 12 行 DP 循环，应调用而非复制。
- 无 Aurora 连接（`aurora.asr.metrics` 或等效路径）。
- 学习目标未编号。

### L68 — CTC 对齐原理
- 压缩率错误（见 Critical）之外，Cell 11 检验在 stub 未实现时静默产生错误的断言消息（"全 blank 应返回空列表"反馈，但实际是未实现问题）。
- 标题中"单调路径"在正文中从未解释。

### L69 — CTC 前向算法
- 三处 Critical 问题之外，Cell 3 注释写"3 帧"但实际 T=6；`blank` 默认值 0 与 notebook 中 `BLANK=2` 不一致。
- 无 Aurora 连接节。

### L70 — Whisper 架构解析
- 两处 Critical 之外，Cell 6 中间 Conv1 输出形状未正确打印（标签"Conv1 输出"实际显示的是输入形状）。
- 标题承诺"多任务头"，但无对应内容。
- 无编号学习目标。

### L71 — Whisper 解码策略
- 两处 Critical 之外，贪婪复杂度 Cell 1 说"O(T)"，Cell 8 说"O(T×V)"，前后矛盾。
- 两个 TODO 函数均无推理提示（hint），尤其 beam search 对初学者难度过高，完全无骨架。
- 无 Aurora 连接节。

### L72 — Whisper 微调
- 三处 Critical 之外，无学习目标节；全课无任何 TODO 练习（标题是"实战"课）。
- L72 和 L73 的 WER 公式介绍大量重叠（Macro vs Corpus WER 区别未说明）。
- `Seq2SeqTrainingArguments` 中 `evaluation_strategy` 在 transformers≥4.38 中已改名，使用旧参数报 FutureWarning。

### L73 — WER 评估
- 两处 Critical 之外，stub 末尾 `return None`（而非 `raise NotImplementedError`），测试静默跳过。
- Aurora 连接引用 `aurora/speech/eval.py`（不存在）。
- solutions 目录缺少 L73 的解答文件，但 Cell 9 明确提示参考 `solutions/` 目录。
- "本课收束"对 L74 的预告与 L74 实际内容不符（预告说"音频特征对比"，L74 实际是混淆矩阵/词频分析）。

### L74 — ASR 错误分析
- 两处 Critical 之外，学习目标承诺"混淆矩阵"，但实现只是 Counter 频率列表（非矩阵）。
- `alignment()` 区分大小写，`wer()` 不区分，对混合大小写输入结果矛盾。
- "本课收束"用错函数名（`wer_decompose` vs 实际 `alignment`）。

### L75 — ASR 流水线图形化
- CTC 路径示例错误（见 Critical）之外，T 标注不一致（Markdown T=5，代码 T=6）。
- 整课无学习目标节，无任何学生练习。
- `make_attn` 通过闭包依赖 `rng2`，两次循环消费导致图与熵打印不对应。

### L76 — 音乐理论速成
- 内容缺失（见 Critical）之外，Task 1 和 Task 2 验证均为 print-only，无 assert，学生实现错误静默通过。
- `chroma_from_freq` 注释未说明应使用 `round()` 而非 `int()`，B4=493.88 Hz 的截断 bug 被 print-only 测试掩盖。

### L77 — 音乐特征工程
- aurora.music 虚假描述（见 Critical）之外，chroma 归一化使用 max-norm，但 `aurora.music.features.chroma_vector` 使用 L1 归一化，两者不一致。
- 标题承诺 ZCR 但从未出现（aurora 中 `zero_crossing_rate` 已实现）；被 BPM 自相关取代但未说明原因。
- `rms_envelope` stub 设置 `n_frames=0` → ValueError（而非 `raise NotImplementedError`）。

### L78 — 节拍追踪实现
- 可运行性（见 Critical）之外，`my_beat_track` 的 beat position 提取完全无说明，只有 BPM 计算提示，学生返回空 `beats` 数组。
- Task 1 `my_onset_envelope` 无任何 assert，只有 print(shape)；返回错误形状也通过。
- n_fft 参数：学生 stub 默认 1024，aurora 参考默认 2048，比较无意义。

### L79 — 音乐嵌入向量
- Aurora 连接虚假（见 Critical，`MusicEncoder`/`triplet_loss`/`nt_xent_loss` 不存在）。
- "M4-MU3"旧命名引用（应为 L80）。
- "本课收束"遗漏 NT-Xent 损失和 CLAP 讲解的总结。

### L80 — 向量相似度检索
- assert 崩溃（见 Critical）之外，标题承诺"vs L2"但 L2 距离从未出现。
- "下一节 MU4"旧命名引用（应为 L81）。

### L81 — 音乐推荐系统
- 两处 Critical 之外，Cell 0 引用的三个源文件均不存在（`recommend.py`/`embed.py`/`sim.py`）。
- `month04.mu3_sim` 旧导入注释（应为 `aurora.music.similarity`）。
- 无编号学习目标。

### L82 — 音乐特征可视化
- 公式错误（见 Critical）之外，Aurora 连接引用 `chroma.py`（不存在）和 `embed.py`（不存在），实际文件为 `features.py` 和 `similarity.py`。
- 两个 section 均标注为"## 3."，编号重复。
- 无编号学习目标，无任何 TODO 练习。

### L83 — Transformer 从零复现
- 验证失效（见 Critical）之外，"L2"错误课号引用（应为 L84 或"后续课程"）。
- causal mask 验证仅检查输出形状，不验证遮蔽位置权重接近零。
- 两个 section 均标注为"## 6."，编号重复。

### L84 — LoRA 低秩适配
- 引用错误（见 Critical）之外，参数效率对比实验对比架构不兼容（全参数 65 个 vs LoRA 577–2113 个，方向相反）。
- Cell 11 测试无法检出 `self.scale` 计算错误（`alpha/rank`）。

### L85 — KV-Cache 从零实现
- 两处 Critical 之外，Cell 3 的 TODO 提示完全遗漏了 softmax 步骤（自注意力的核心操作）。
- `update` 方法 TODO 无边界情况提示（首次调用 vs 后续拼接）。
- 验证只检查形状，`return np.zeros_like(Q)` 也能通过。

### L86 — 采样策略从零实现
- 可运行性（见 Critical）之外，`top_p_sample` 测试只检查返回值在合法范围内，不验证是否在 nucleus 集合内。
- `ref_topp` 被导入但从未使用（F401），而 `top_p` 的 aurora.llm 交叉验证完全缺失。

### L87 — 量化与本地推理
- 练习缺失（见 Critical）之外，全课用 `np.uint8`（0–255）但称之为"INT8"，工业 INT8 使用有符号（-128~127），造成系统性混淆。
- `zero_point` 未约束到合法范围，极端输入可越界。
- Cell 3 和 Cell 4 使用两套不同量化公式（有/无 zero_point），前后矛盾。

### L88 — TF-IDF 检索实现
- 三处 Critical 之外，IDF 公式未指明对数底（实现用 `np.log` 即自然对数，但公式写 `log(...)`）。
- 无余弦相似度公式（Task 3 要求实现但无预先说明）。
- 查询向量不乘 IDF 与文档向量不一致，未解释原因。

### L89 — RAG 完整流水线
- 两处 Critical 之外，`chunk_text` 的重叠（overlap）正确性未验证（测试不检查连续块是否共享词）。
- `format_rag_prompt` 练习缺少期望输出格式说明，任何拼接方式均可通过测试。
- "本课收束"对 L90 的预告（"Tool-calling Agent"）与 L90 实际内容（对话式 RAG）不符。

### L90 — 对话式 RAG
- 三处 Critical 之外，L89 用 TF-IDF，L90 静默切换到 dense embedding，无任何过渡说明。
- Section 5 的 RAG vs no-RAG 对比因 stub LLM 始终返回"John played..."，所有命中检查均打印 ❌，使 RAG 看起来无效。
- `build_rag_index` docstring 说返回 tuple，但实现返回 ndarray，造成下游类型错误。

### L91 — 注意力图解
- 标题与内容不符（见 Critical）之外，`import torch` 在全文唯一一处使用，在无 torch 环境中阻断整个 notebook。
- "GPT-2 规模"标注用于 d=4096（实为 LLaMA-7B 或 GPT-3 规模，GPT-2 最大为 d=1600）。
- Aurora 连接描述 `aurora.llm` 含 attention 实现，但实际 `aurora.llm` 无任何 attention 代码。
- 无编号学习目标。

### L92 — 端到端流水线
- 虚假 Aurora 连接（见 Critical）之外，并行加速数值错误（ASCII 图声称-50ms，实际只省约 20ms）。
- `from aurora.audio.io import read_wav` 导入后从未使用。
- Section 6 练习是填写已知常数而非真实计量，无 assert 验证，没有教学价值。

### L93 — MLOps 基础
- 两处 Critical 之外，无编号学习目标节。
- 生成的 CI YAML 与仓库实际 `.github/workflows/ci.yml` 不兼容（Python 版本、lint 工具、触发分支均不同），实际写入会破坏 CI。
- `scripts/log_ci_metrics.py` 作为真实交付物描述但文件不存在。
- Section 6 练习只需填写 `None` 占位符，无任何编码要求和验证。

### L94 — Aurora v1 全景 Demo
- 两处 Critical 之外，Results 字段包含"（运行上方 cell 后查看实际 WER 值）"悬空占位符，整个 notebook 中无 WER 计算 cell。
- 5 分钟演示排练表格使用旧式路径（`week01/day4_euler.ipynb`、`month03/`、`month05/`）。
- 无编号学习目标节。

### L95 — 研究论文入门
- 章节重复（见 Critical）之外，Abstract 字数校验使用 `len().split()`，对中文无效（整段中文视为 1 词）。
- "按依赖顺序"表格中 Whisper（L70-71）排在 Transformer（L83-84）之前，实际上 Whisper 依赖 Transformer，顺序有误。

### L96 — 白板推导演练
- Attention 引用"L83-L84"但 L84 是 LoRA，应为"L83"。
- MFCC 答案格使用 `np.fft.rfft` 未解释（违反"不用 API 黑盒"原则但未说明例外）。
- MFCC 答案格对 DCT-II（步骤 7）只有文字描述，无代码实现，学生无法验证推导正确性。

### L97 — 面试准备与技术沟通
- "余弦相似度 → L78"引用错误（L78 是节拍追踪，余弦相似度在 L80）。
- `hz_to_mel` 和 `frame_signal` 两个练习 stub 使用 `return None`，测试被 `if is not None` 保护，整节练习可零参与通过。
- 文档中出现两个"Section 5"，编号重复。

### L98 — 课程总结
- 章节编号错乱（见 Critical）之外，全部填写字段均为 `None`，无任何示范性填写样本，学生面对空白模板缺乏校准参照。
- 副标题承诺"成长曲线量化"但 `learning_velocity` 只打印纯文本，无任何可视化。
- `interview_readiness` 统计写死数字 7，条目增减会静默失效。

### L99 — Aurora v2 与持续成长
- `import datetime` 导入后从未使用（功能被放弃但未清理），`else` 分支中建议手动保存但未实现文件写入。

---

## 四、🟡 次要问题（Minor Issues）

### 1. 跨课引用错误

以下各处引用了不存在或已废弃的文件名/模块名/课号：

- **L06**："week01/day5" → 应为 L07
- **L08**："day3"/"week01/day4" → 应为对应 L 编号
- **L17**：次级引用使用旧体系
- **L21**：四处"M1"模块命名
- **L38**：无编号学习目标，多处旧命名残留
- **L56**："M2-A4" → 应为 L57
- **L57**：`a3_backward.ipynb` → L56；`p3_nn.ipynb` → L61
- **L60**：`a3_backward.ipynb`（×4）→ L56；"M2-P3" → L61；"见 L64"虚构引用
- **L61**："a4_mlp"（×3）→ L57
- **L63**："M2-K3" → L64
- **L65**：`month02/a1_value.ipynb` → L54；`month02/a2_forward.ipynb` → L55；`month02/a5_train.ipynb` → L58
- **L79**："M4-MU3" → L80
- **L80**："下一节 MU4" → L81
- **L81**：`month04.mu3_sim` 注释导入 → `aurora.music.similarity`
- **L83**：cell-7 写"L2"（无效课号）→ 应为 L84 或"后续课程"
- **L83**："## 6."重复（PE 和 FFN 两节均标 6）
- **L91**：收束缺少 L92 引用
- **L92**：标题含"麦克风"但无麦克风录音代码
- **L93**：CI YAML 与实际仓库配置不兼容（参见 Major 项）
- **L94**：演示路径 `week01/day4_euler.ipynb`、`month03/`、`month05/` 已废弃
- **L96**："Attention → L83-L84" → 应为"L83"（L84 是 LoRA）
- **L97**：编程题速查表"余弦相似度 → L78" → 应为 L80

### 2. 断言精度问题

- **L05**：`atol=1e-15` 过高，浮点误差可能偶发失败
- **L17**：参考值 `-384` 错误（应为 +56）
- **L23**：正交性断言是恒等式（任何实现均通过）
- **L39**：参考 FFT 输出 8 个系数中有 4 个错误
- **L46**：`hz_to_mel(700)` 参考值存在公式版本依赖问题，未说明
- **L50**：Cell 4 图例说"跳过 k=0"，其他单元格说"保留 k=0"，相互矛盾
- **L55**：`tanh` 断言使用舍入值 `0.7616`（atol=1e-4），改用 `math.tanh(1.0)` 更精确
- **L62**：形状断言仅检查 ndim=3，不检查 `batch_x.shape[1] == 40`
- **L63**：只测 n_classes=10，硬编码 10 可通过
- **L73**：WER 上界 2.0 为任意值（实际可超过 1.0），`assert >= 0` 已足够
- **L81**：`isinstance(r, int)` 在 `np.int64` 返回值上失败
- **L84**：Cell 11 无法检出 `self.scale = alpha`（缺少 scale 验证断言）
- **L85**：验证只检查 shape，零矩阵即可通过
- **L88**：Cell 10 无条件打印"✅"，不比较学生输出
- **L93**：p95 off-by-one（返回 p100）

### 3. 内容完整性（缺失学习目标、Aurora 连接、练习等）

以下各课存在结构性内容缺失：

**缺少编号学习目标（仅单句描述）**：L05、L06、L07、L08、L09、L10、L11、L12、L16、L17、L18、L19、L20、L22、L23、L24、L25、L26、L27、L28、L38、L43、L48、L51、L52、L55、L59、L61、L63、L67、L68、L70、L71、L72、L73、L74、L75、L76、L77、L78、L79、L80、L81、L82、L83、L84、L85、L86、L87、L88、L89、L90、L91、L92、L93、L94、L97、L99

**缺少 Aurora 连接节**：L24、L67、L69、L71、L86、L88（其他章节虽有 Aurora 连接但路径错误）

**整课无学生 TODO 练习**：L65、L74、L82、L92（演示型课程，应至少有 1 个动手环节）

**内容与标题严重不符（无法从标题预判内容）**：L01、L02、L07、L10、L11、L40、L42、L60、L63、L65、L66、L72、L73、L75、L76、L77、L80、L82、L87、L91、L92

**本课收束对下一课的描述错误**：L34、L37、L73、L89（预告内容与实际 L 的内容不符）

### 4. 其他次要问题

**冗余 import / 死代码**：
- L59：`from aurora.audio.mfcc import mfcc` 未使用（见 Critical，此处列次要 impact）
- L56：`import types` 未使用
- L65：`import itertools` 未使用；cell 重复 `import torch`
- L68：`import torch` 全课未使用
- L71、L72、L74：多处重复 import
- L77：`from aurora.audio.stft import stft` 和 `from aurora.audio.io import read_wav` 均未调用
- L91：`import torch` 全课仅为 Aurora 连接提示而导入，实际无 torch 代码
- L92：`from aurora.audio.io import read_wav` 导入未使用
- L93：`import time` 导入未使用

**代码风格问题**：
- L83：cell-15/17/19 中重复 `import numpy as np`
- L87：`matplotlib.pyplot` 导入但全课无绘图
- L91：变量 `A` 同时代表 attention 权重矩阵和 LoRA_A 矩阵，符号冲突
- L93：`import json as _json`（已有 `import json`，无需别名）
- L98：`learning_velocity` 打印中中英文混排导致列对齐失效

**临时文件泄漏**：
- L62：两处 `NamedTemporaryFile(delete=False)` 未清理临时文件

**安全 / 弃用 API**：
- L61：`torch.load` 未传 `weights_only=True`（PyTorch 2.6+ 报错）
- L72：`evaluation_strategy`（transformers≥4.38 已改为 `eval_strategy`）；`wandb.save()`（W&B SDK≥0.16 已弃用）
- L90：`np.random.seed(42)` 使用弃用 legacy 随机 API（同 notebook 已用 `default_rng`）

**可视化 / 输出问题**：
- L63：Cell 5 Markdown"## 3. 感受野"和"## 4. 练习"与实际 notebook section 编号不一致
- L75：`from itertools import product` 在内部单元格导入（应移至顶部 import 块）
- L82：节拍可视化中首帧竖线被绘制两次（略粗于其他竖线）

---

*报告结束。共计：99 课次，约 115 项 🔴、约 285 项 🟠、约 385 项 🟡。*
*如需各类问题的精确数量，请以本文档第一节总览表为准（以 per-lesson 审计文件中的逐课统计为依据）。*
