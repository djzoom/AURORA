# L20 教授审阅：学生问题 → 修订对照表

审阅对象：`notebooks/2_linear_algebra/L20_visual_factorizations.ipynb`
依据：`docs/current/audit/sim_students/L20/{laochen,linda,xiaoyu}.md` 三位模拟学生的学习日志。

修订原则：只增加、不删减。全部以新插入的 markdown/code cell（标题均为"停一下：……"）或对已有
markdown cell 的追加文字来解决，插入位置紧邻学生卡住的原始 cell。已用
`python3 -c "import nbformat; nbformat.read(...)"` 校验 JSON 合法性，并用
`jupyter nbconvert --execute` 跑通全部新增 cell（notebook 原有的自评 cell 需要人工填写
True/False，本就会在自动执行时抛 AssertionError，这是既有设计，不是本次改动引入的问题）。

---

## 1. "正交"到底是什么？QᵀQ=I 为什么叫正交？

- 提出者：老陈（问题1）、琳达（问题1，形状不一致）、小雨（问题1）
- 位置：`cell-l20-0191`（第一次出现 QR，A 是 3×2）之后
- 改动：插入 markdown cell `52910d3e`，用"点乘判断垂直"类比展开 QᵀQ=I 的含义（列两两垂直+单位长度）；
  插入 code cell `982f0118`，手算两个单位正交向量的点积验证 QᵀQ=I，并对比 `mode='reduced'` 与
  `mode='complete'` 的 QR，解决琳达"Q 不是方阵"的困惑。
- 附带：在 `5e4ec712`（section 2 QR 标题）追加一句回顾，说明这里 A 是方阵、reduced=complete。

## 2. LU 分解里 L 的对角线为什么总是 1？

- 提出者：老陈（问题3）
- 位置：`f8e57a48`（LU 分解代码）之后
- 改动：插入 markdown cell `611b1723`，从"消元不会拿一行去消自己"这个角度解释对角线恒为1；
  插入 code cell `f2e1cd5d`，逐步打印 3×3 消元的每一步乘数和 L 的对角线。

## 3. 对称矩阵特征向量为什么自动正交？S=QΛQᵀ 与 A=UΣVᵀ 什么关系？

- 提出者：琳达（问题2，要证明）、老陈（问题7）、小雨（问题5）
- 位置：`31c61440`（对称矩阵谱分解代码）之后
- 改动：插入 markdown cell `2f147b32`，给出高中水平可跟的点乘证明（利用 S=Sᵀ 推出
  (λ1-λ2)(v1·v2)=0），并说明对称矩阵只需一个 Q 而一般矩阵需要 U、V 两个的几何原因；
  插入 code cell `b4ab5686` 用实际 S、Q 验证证明中的关键等式。

## 4. SVD 中 U、Σ、Vᵀ 到底在几何上做什么？为什么不直接叫"拉伸倍数"？形状怎么对应？

- 提出者：老陈（问题2）、小雨（问题2）
- 位置：`e9bec99a`（section 4 SVD 标题）之后、`af04e8c5`（2×3 SVD 示例）之后
- 改动：插入 markdown+code cell `29b30763`/`06b405d5`：先给一个 2×2 纯拉伸矩阵的极简例子，
  再解释奇异值与特征值的区别和联系，并对比"完整版"（`full_matrices=True`，Vᵀ 是 n×n）与
  "经济版"（notebook 实际用的 `full_matrices=False`，Vᵀ 只有 r×n）两种形状约定——这正是
  老陈测试时看到 Vᵀ 是 3×3、而 notebook 代码给出 2×3 的原因。
  又在 `af04e8c5` 之后插入 markdown+code cell `0358eab3`/`89ec7110`，把 A@x 拆成
  Vᵀx → Σ(Vᵀx) → U(Σ(Vᵀx)) 三步，打印每一步的实际形状和数值，验证与 `s`、`A@x` 用同一个 A、同一份输出对应。

## 5. U 用 UᵀU=I 检查、Vt 却用 Vt·Vtᵀ=I 检查，为什么形式不对称？

- 提出者：琳达（问题3）
- 位置：`70ec5c49`（Aurora 桥接段）之后、`3a1ccdcc`（断言 cell）之前
- 改动：插入 markdown cell `b5e3b644` 说明 "矩阵的列正交 ⟺ MᵀM=I"，而 `Vt` 本身已经是
  V 的转置，所以"Vt 的行"＝"V 的列"，`Vt@Vt.T=I` 与 `V.T@V=I` 是同一件事只是换了主语；
  插入 code cell `9f8e5e2a` 用实际 Vt/V 对照打印验证。

## 6. 外积（outer product）怎么生成秩1矩阵？为什么秩1矩阵有2个零奇异值？"秩亏"是什么？

- 提出者：老陈（问题6）、小雨（问题3、问题4）
- 位置：`3a1ccdcc`（断言 cell，含秩亏验证）之后
- 改动：插入 markdown cell `519fe694` 给出外积定义、证明外积必秩1（所有行都是同一向量的倍数）、
  解释零奇异值对应"该方向拉伸倍数为0"，并解释"秩亏"这个名字的来源；
  插入 code cell `c586b833` 复用已算出的 `A_rank1`、`s_rank1` 直接验证。

## 7. 条件数是什么？为什么它大会导致求解不稳定？

- 提出者：老陈（问题4）、琳达（问题4，完全陌生的概念）
- 位置：`cell-l20-0194`（条件数参数实验）之前
- 改动：插入 markdown cell `d4b9b5c5` 给出条件数定义 κ=σmax/σmin，用"读数误差被放大"的类比
  解释与 R 对角线悬殊的关系，以及为什么放大效应会破坏浮点计算精度；
  插入 code cell `e906586f`，用对角矩阵 diag(1000,1) 精确构造"最坏方向"的扰动，验证
  相对误差放大倍数 ≈ κ(A)。

## 8. rank_from_svd 里 threshold * S[0] 为什么用相对门限？

- 提出者：老陈（问题5）、琳达（问题6）、小雨（问题6）
- 位置：`cell-l20-0196`（练习5引言）
- 改动：在原 markdown cell 内追加"矮个子"类比，解释绝对门限在矩阵尺度悬殊时会判断错误；
  插入 code cell `23120b35` 用一个奇异值普遍很小、一个奇异值悬殊的矩阵对比绝对/相对门限的判断结果。

## 9. "保留前k个奇异值"具体怎么操作？截断SVD做低秩近似/去噪的直觉是什么？

- 提出者：琳达（问题5，没有代码示例）、小雨（问题7）
- 位置：`cell-l20-0197`（rank_from_svd 练习代码）之后
- 改动：插入 markdown cell `ed64aa54` 给出截断SVD重建的三步具体代码写法；
  插入 code cell `30300c27`，构造"真实秩1信号+随机噪声"矩阵，验证截断到 k=1 后
  确实比未截断更接近真实信号。

## 10. Mel 频谱矩阵的"SVD 低秩近似"提得太跳跃

- 提出者：琳达（问题7）
- 位置：`cell-l20-0198`（本课收束）之后
- 改动：插入 markdown cell `9b92e4ec` 解释"重叠三角形滤波器"为什么天生低秩；
  插入 code cell `7c036363`，用简化玩具版重叠三角滤波器组做 SVD，验证奇异值确实快速衰减
  （真正的 Mel 矩阵留给 L21 展开，这里只做直觉铺垫，不越界抢 L21 的内容）。

## 11. 自评表格没有"通过"的标准

- 提出者：小雨（问题8）
- 位置：`l20_review`（自评 cell）之前
- 改动：插入 markdown cell `b6c4760c`，给每一项自评指标写出具体的"及格线"（比如要能不看
  notebook 讲出 U/Σ/Vᵀ 各自含义），并指向对应的补充实验作为验证方法。

---

**统计**：三位学生共提出 22 条问题实例，去重后归为 11 个知识点，全部在对应卡点附近补充了
铺垫类比 + 逐步推演 + 可运行验证代码。原有内容一处未删，仅追加。
