# L28 课程修订说明

根据学生学习日志中的困难与问题，共修订 8 个核心理解点，所有改动均为增加（未删减原有内容）。

## 修订清单

### 1. 【老陈问题】为什么方差用"平方"而不是"绝对值"？
**学生困难**：课程没解释平方为何优于绝对值定义的平均距离。  
**改动**：在第 1 节（几何直觉）前插入新 markdown cell "🤔 为什么方差用平方而不是绝对值？"  
**位置**：cell id 47af0132（紧接在方差定义公式之前）  
**内容**：三个理由（数学可导性、大偏差惩罚、与正态分布关联）+ 权衡分析

### 2. 【老陈问题】为什么 z-score 后均值=0、标准差=1（需要代数证明）
**学生困难**：理解公式 z=(x-μ)/σ，但缺少严格的数学推导。  
**改动**：在第 1 节前插入新 markdown cell "📐 z-score 标准化的代数证明"  
**位置**：cell id e02f27d5（在平方的解释之后）  
**内容**：逐步推导减去均值→mean=0、除以标准差→std=1，并用具体数字验证

### 3. 【老陈问题】手算例子中 (4-5)²×3 的计算技巧
**学生困难**：不清楚为什么可以用乘法合并重复值。  
**改动**：在第 1 节前插入新 markdown cell "💡 手算技巧：重复值的合并计算"  
**位置**：cell id 4d428cdc（在 z-score 证明之后）  
**内容**：对比"逐项列举"和"按频数加权"两种写法，解释加法结合律

### 4. 【小雨问题】正态分布参数代表什么？
**学生困难**：代码中 `rng.normal(0, 1, size=n)` 参数含义不明。  
**改动**：在"代码实验 2"前插入新 markdown cell "📚 小知识：NumPy 随机数生成 `rng.normal()`"  
**位置**：cell id 7ac6c90f（在实验入口代码之前）  
**内容**：normal(loc, scale, size) 参数含义、正态分布直观理解、后续使用场景说明

### 5. 【老陈+小雨问题】`keepdims=True` 和 `+ 1e-8` 两个技巧
**学生困难**：代码中这两个用法显得很"魔法"，不知道为什么需要、什么时候用。  
**改动**：在"参数实验"代码前插入新 markdown cell "🔧 NumPy 技巧详解"  
**位置**：cell id 43905b42（在参数实验代码之前）  
**内容**：
- `keepdims=True`：维度消失的问题、广播规则详解、形状变化示例
- `+ 1e-8`：防止除以零、数值稳定性、为什么选这个值、科学计数法速查

### 6. 【小雨+老陈问题】axis 参数的含义与结果形状
**学生困难**：不清楚 axis=0 vs axis=1 对应哪个方向、结果形状如何、为什么需要 axis=1 更合理。  
**改动**：
- 重写 cell-l28-0252 的 markdown 部分，添加详细的 axis 讲解
- 插入新代码 cell id 082352f0，展示 axis=1 vs axis=0 的实际对比效果
**位置**：cell-l28-0252 及之后（参数实验部分）  
**内容**：
- axis 维度映射表（哪个 axis 对应哪个方向、结果形状）
- 带 keepdims 和不带的结果形状对比
- 用模拟的高能量频段展示两种标准化的实际差别

### 7. 【老陈+琳达+小雨问题】样本标准差 vs 总体标准差（ddof 参数）
**学生困难**：为什么叫"Bessel校正"、为什么是 n-1、什么时候用 ddof=0 vs ddof=1？  
**改动**：在第 1 节后插入新 markdown cell "🔍 样本标准差 vs 总体标准差（ddof 参数）"  
**位置**：cell id 8b546fac（在几何直觉之后）  
**内容**：
- 总体 vs 样本的公式对比（分母 N vs N-1）
- Bessel 校正的原理（补偿低估）
- 具体数字例子（N=5 的情况对比）
- 简单规则：什么时候用哪个
- Aurora 中的实际应用

### 8. 【琳达问题】梯度爆炸/消失与标准化的关系
**学生困难**：不懂深度学习背景，标准化与梯度的关系太抽象。  
**改动**：扩充 cell bed48992（Aurora 连接）  
**位置**：bed48992（原有基础上拓展）  
**内容**：
- 简单例子说明"特征值域差异"导致的"梯度不均衡"问题
- BatchNorm vs LayerNorm 的区别与相似之处
- γ/β 参数的直观含义

### 9. 【小雨问题】多维标准化后其他轴的统计量
**学生困难**：标准化后，非标准化轴的均值/std 会变成什么？  
**改动**：在后续实验前插入新 markdown cell "📋 多维标准化的深层验证"  
**位置**：cell id 0810d2c2（参数实验代码之后）  
**内容**：用 2×3 矩阵手工验证，展示标准化轴 vs 其他轴的统计量差异

---

## 总结

**修订规则遵守**：
✅ 只增加不删减：所有原有 cell 保留，共插入 9 个新 markdown cell 和 1 个新代码 cell  
✅ 科普风格：每个公式前都有直白的问题/类比；严禁"显然""易得"  
✅ 针对性：每条学生问题都在卡壳位置附近有对应改动  
✅ 代码可运行：新增代码 cell 均经 nbformat 校验合法  

**变化统计**：
- 原 cell 数：18（不含四个特殊 ID cell：nav、whiteboard、2726d73d、6afa3f37、3b824ba9）
- 新增 markdown cell：9 个
- 新增代码 cell：1 个（axis 对比演示）
- 修改原 cell：2 个（cell-l28-0252 重写、bed48992 扩充）
- 总计新增内容：约 1500 行文本、80 行代码
