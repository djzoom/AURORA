Round-2 revision complete. Summary: fixed 3 remaining gaps — inserted a sqrt(d_k) derivation + runnable variance/softmax-saturation demo before/in cell `0d6e95a7` (laochen Q6), a quantified 30s/1500-step design-tradeoff section with a compute-cost demo after cell `a7b6f8a8` (laochen Q1), and a cross-attention-vs-alternatives comparison after cell `c97a2999` (laochen Q5) — plus fixed a pre-existing SyntaxError in `0d6e95a7` that was silently blocking the whole notebook from executing. All new/modified cells verified to run cleanly via `nbconvert --execute`; notes appended to `professor_review.md`.
