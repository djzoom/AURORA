# 琳达 — L70 学习日志

## 我理解了什么

Whisper 把音频处理分成三段：先把原始音频变成 80 维的 log-Mel 特征（一种数学变换），然后编码器看这个特征并提取重要信息，最后解码器"一个字一个字"地生成文字。整个流程中参数很固定（30秒音频 → 3000 帧），所以模型输入输出都是固定形状，不用动态处理。Python 代码逻辑我基本能读，特别是 PyTorch 的 tensor 操作。

## 遇到的困难和问题

### 问题 1：log-Mel 归一化的三行公式到底在干啥【推演】

Cell "abe2ffbd" 中这三行：
```
log_mel = log10(max(mel_power, 1e-10))
log_mel = max(log_mel, log_mel.max() - 8.0)
log_mel = (log_mel + 4.0) / 4.0
```

我看得出每一行的 Python 操作，但卡在：
- 第一行是取 log，我懂
- 第二行的 `max(log_mel, log_mel.max() - 8.0)` 是什么意思？`log_mel.max()` 是数组中最大值，为什么要减 8.0？
- 第三行加 4.0 再除 4.0 是想把范围压缩到 [-1, 1]，但这些具体数字是怎么设计出来的？

**希望补充**：能否解释这个三步的"为什么"？比如 -8.0 和 4.0 是根据什么算出来的数字？

---

### 问题 2：stride=1 和 stride=2 怎么影响输出长度【理解】

Cell "d242fe64" 说：
> Conv1d(80 → d, kernel=3, padding=1, stride=1) → 3000 → 3000  
> Conv1d(d → d, kernel=3, padding=1, stride=2) → 3000 → 1500

虽然代码演示中确实看到了 (1, 512, 3000) 变成 (1, 512, 1500)，但我不理解"stride=2 为什么就能让序列长度变成一半"。stride 参数什么意思？为什么改这个参数就能改长度？

**希望补充**：能不能用一个简单例子（比如 3×3 的数组）演示 stride=1 vs stride=2 的卷积操作会产生什么结果？或者给个公式解释卷积输出长度的计算。

---

### 问题 3：center=True 为什么会导致多出 1 帧【理解】

Cell "abe2ffbd" 说：
> 注意：Aurora 的 `mel_spectrogram` 默认 `center=True`（反射填充），实际返回 **3001** 帧（`480000 // 160 + 1`）

计算上 480000 ÷ 160 = 3000 帧，但加了 center=True 就变 3001？什么是"反射填充"？为什么这样会多 1 帧？

这个问题卡在：我没学过 center padding 的概念，课程也没解释。感觉是一个 DSP 的细节，但不知道原因。

**希望补充**：解释一下 center=True 的 padding 机制——它在音频的前后加什么内容，导致为什么会多出 1 帧？

---

### 问题 4：交叉注意力中，Q @ K 怎么就产生了对的形状【推演】

Cell "0d6e95a7" 的代码：
```python
attn_weight = (Q @ K.transpose(-1,-2)) / scale  # 形状应该是 (B, T_dec, T_enc)
```

- Q 是 (1, 20, 512) — batch, 解码步, 维度
- K 转置后是 (1, 512, 1500) — batch, 维度, 编码步
- 两个矩阵相乘产生 (1, 20, 1500)?

我知道矩阵乘法规则 (m, n) × (n, p) = (m, p)，所以应该是 20 × 1500 = 20。但怎么确保中间维度对齐？为什么 transpose 选择 `(-1, -2)` 就够了？

**希望补充**：能否用具体数字演示 Q 和 K 的矩阵乘法每一步是怎么进行的，特别是为什么 transpose 要选最后两个维度？

---

### 问题 5：前缀 token 是怎么影响解码结果的【理解】

Cell "6645d121" 和代码演示说多任务靠前缀 token 切换：
```python
"识别（中文）": [SOT, LANG_ZH, TRANSCRIBE, NO_TIMESTAMPS],
"翻译→英文":   [SOT, LANG_ZH, TRANSLATE, NO_TIMESTAMPS],
```

这些 token 被送进解码器后，模型怎么"知道"后续要做翻译而不是识别呢？这些 token 进入自注意力和交叉注意力后，是怎么影响后续 token 的生成的？看起来就是把几个特殊 ID 放在前面，模型怎么就能理解我要翻译而不是转写？

**希望补充**：能否简单解释 Transformer 解码器的自注意力和交叉注意力是怎么利用前缀 token 信息的？

---

### 问题 6：causal mask 是什么，为什么需要【理解】

Cell "d242fe64" 和 "c97a2999" 提到：
> 编码器不用 causal mask（每帧可以看全局声学上下文）  
> 解码器是 causal Transformer

什么是 causal mask？课程没有解释。我猜测"causal"可能是"因果"的意思（之前的能看之后的，之后的看不到之前的？），但为什么编码器和解码器不同？

**希望补充**：能否用图示或简单例子说明 causal mask 是什么，为什么解码器需要它但编码器不需要？
