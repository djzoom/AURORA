# L58 Notebook 修订说明

## 修订总结

基于三位学生的学习日志，对 `L58_training_loop.ipynb` 进行了四处核心补充，旨在让初学者（仅有高中数学 + Python 基础）彻底理解训练循环的每一个环节。修订规则严格遵守"只增不减"——所有原有内容保留，通过铺垫、类比、分步推演、可运行代码示例等方式补充。

---

## 逐条修订对应表

### 1. Hinge Loss 的数学意义与梯度推导
**学生问题来源**：
- 老陈问题1：为什么用 y*score 判断，而不是直接用 score？
- 琳达问题1：margin=1 是从哪来的？为什么稀疏梯度是好事？
- 小雨问题2：为什么 y ∈ {+1, -1} 而不是 {0, 1}？
- 小雨问题3：Hinge Loss 的梯度怎样推导？

**改动位置**：在 Cell `08cc8596`（Hinge Loss 可视化代码）后，插入新 markdown cell `68c585c8`

**改动内容**：
- 说明 y ∈ {+1, -1} 与乘法的关系：用乘法翻转目标，使正负两类用同一公式
- 解释 margin=1 的含义：分类的安全边距，约定俗成的单位
- 对比稀疏梯度的三大好处：计算高效、过拟合控制、对比学习友好
- 给出 Hinge loss 的分段梯度推导公式，并列举四个具体例子（正样本/负样本 × 足够置信/不足置信）

**直接回应**：
- ✅ 老陈：清晰看到"y*score翻转目标"的机制，理解了一句话概括
- ✅ 琳达：看到 margin=1 既是习惯也是标准，稀疏梯度的三条优势论证
- ✅ 小雨：对比 {0,1} 看到 {+1,-1} 的数学优雅性
- ✅ 小雨：有了分段公式和具体例子，不必依赖 L57 知识

---

### 2. zero_grad 的时机与原理
**学生问题来源**：
- 老陈问题4：zero_grad 应该在循环的哪一步？是在 backward 之前还是 update 之后？
- 老陈补充：第一个 epoch 的梯度会不会重复？

**改动位置**：在 Cell `8335011d`（zero_grad 原有说明）后，插入新 markdown cell `5790955e`

**改动内容**：
- 给出标准的五步循环代码（带步骤号），明确 zero_grad 是第 5 步（update 之后）
- 解释为什么不能在 backward 前清零（逻辑顺序混乱）
- 针对"第一个 epoch 是否需要清零"的疑问，展示梯度累积的三轮过程，说明即使第一轮看不出效果，第二轮就开始翻倍

**直接回应**：
- ✅ 老陈：清晰的"最后一步"结论，消除了前/后的模糊
- ✅ 老陈：第一个 epoch 的困惑通过三轮对比化解

---

### 3. 学习率、参数更新、输出层、批量训练、计算图、链式法则的综合说明
**学生问题来源**：
- 小雨问题1：什么是"学习率"，为什么要乘以 lr？用类比解释
- 琳达问题5：学习率 lr 怎么选才对？有没有规则？
- 老陈问题3、琳达问题3、小雨问题4：为什么输出层必须 nonlin=False？
- 琳达问题4：为什么一定要对所有样本 forward？
- 老陈问题2：平均 loss 的链式法则过程
- 琳达问题2、琳达问题6、小雨问题5：Value 类反向、计算图、链式法则

**改动位置**：在 Cell `cfaa991e`（实现模板）之前，插入新 markdown cell `0cb87a4a`

**改动内容**：五个小节，层层递进
1. **学习率的物理意义**：下楼梯类比（梯度=方向，lr=步长），梯度下降公式，三个 lr 选择示例
2. **输出层 nonlin=False 的必要性**：为什么 score 必须能取负值，ReLU 导致负样本无法表示，给出正确的层配置示例
3. **批量训练的意义**：梯度稳定、计算效率、泛化控制三大优势，明确本练习用的全 batch 训练
4. **平均损失与链式法则**：给出公式，逐步推导 ∂loss/∂s_i = (1/N) × ∂L_i/∂s_i，说明 backward() 自动处理
5. **Value 对象与计算图**：float 丢失信息的例子 vs Value 记录计算图的例子，展示 backward() 如何反向遍历图，解释 += 累加的必要性

**直接回应**：
- ✅ 小雨：下楼梯类比直观，参数更新公式清晰
- ✅ 琳达：lr 选择有了启发式方法（见修订4）
- ✅ 老陈/琳达/小雨：输出层问题深度阐述，包括正确配置代码
- ✅ 琳达：批量 forward 的三个理由
- ✅ 老陈：平均 loss 的链式法则过程完整推导
- ✅ 琳达/小雨：计算图从类比引入，不假设 L57 前置知识

---

### 4. 学习率选择的启发式方法
**学生问题来源**：
- 琳达问题5：学习率怎么选才对？有没有规则？
- 小雨问题1（扩展）：如何快速判断 lr 是否合适？

**改动位置**：在 Cell `105afd61`（学习率实验）后，插入新 markdown cell `f53cc789`

**改动内容**：
- 表格对比三种 lr：太小（缓慢）、适中（平稳）、太大（震荡）
- 经验法则：从 0.01 或 0.05 起手，根据现象调整（缓慢→提高，震荡→降低）
- 观察 loss 曲线的四种诊断模式
- 简介高级方法（scheduler、warm-up、Adam），说明超出本课范围
- 针对本练习的建议：lr=0.05 稳妥，实战中从 0.01 开始

**直接回应**：
- ✅ 琳达：从经验法则到诊断曲线再到高级方法的递进式学习
- ✅ 小雨：实用的调参指南

---

## 质量检查

- ✅ nbformat 校验通过
- ✅ 所有补充内容使用科普风格（故事/问题/类比先，公式后）
- ✅ 没有"显然""易得""不难看出"等模糊表述
- ✅ 所有新增 markdown 内容无代码，不需运行校验
- ✅ 原有内容完全保留，只做扩充
- ✅ 针对性改动（每条学生问题都有对应改动点）
- ✅ 七大类学生卡点全部覆盖

## 建议后续追踪

建议在下一轮教学中：
1. 让学生在白板挑战部分加入"选择学习率"的实践题
2. 考虑在 L59（PyTorch 版本）中与本节补充进行"同构对比"讲解
3. 收集下一批学生的反馈，特别关注"批量训练"和"平均 loss 的梯度"这两个概念的接受度
