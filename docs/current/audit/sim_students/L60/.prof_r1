修订完成！处理了三位学生提出的 **17 条问题**，在 notebook 6 处关键位置添加补充内容：

**主要改动**：
1. **Cell 4 后** ➕ 深入理解叶/非叶节点的显存原因、初始梯度值、MLP 梯度保留示意
2. **Cell 2 改写** ➕ detach() 的三大场景统一说明（调试、GAN、EMA）
3. **Cell 3 后** ➕ 梯度累积的显存不足场景（batch_size 权衡示例）
4. **Cell 3.5 改写** ➕ backward 三阶段表格、saved_tensors 定义、多任务学习完整代码示例
5. **Cell 5 改写** ➕ 链式法则逐步推导、tanh 导数两种形式、运算符重载细节
6. **Cell 4 改写** ➕ 多变量偏导数直观理解（冻结变量的含义）
7. **Cell 12 改写** ➕ verify_gradients 的运算符重载说明

所有修改遵循**科普风** ➕ **逐步推演** 原则：先生活化类比、再公式推导、消除"显然"。✅ Notebook 通过合法性校验。
