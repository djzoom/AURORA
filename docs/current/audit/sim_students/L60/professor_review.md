# L60 Notebook 修订记录

本次修订针对三位学生（小雨、老陈、琳达）的学习日志，在 notebook 中添加了详细补充，确保每条学生问题都得到解答。

---

## 修订清单

### 1. **小雨 Q1：非叶节点的梯度为什么默认不保留？**
   
**问题**：理解"显存爆炸"的理由，以及非叶节点梯度的含义和具体保留/丢弃场景。

**改动**：
- **位置**：Cell 4 后插入新 markdown cell "深入理解：为什么要分叶/非叶？"
- **内容**：
  - 用数字化的示例说明显存消耗（12 层×100 万激活值 = 1200 万梯度）
  - 初始梯度值是 `None` 的原因和梯度累积的联系
  - 边界情况说明：`torch.tensor(...) * 2` 的叶/非叶判断
  - 3 层 MLP 的具体哪些梯度保留、哪些丢弃的示意图

---

### 2. **小雨 Q2：retain_grad() 和 retain_graph=True 为什么是两个 API？**

**问题**："正交"这个比喻太抽象，为什么要分开设计？只用其中一个会怎样？

**改动**：
- **位置**：Cell 3.5（原有 retain_graph 说明）完全改写
- **内容**：
  - 新增 backward 的三个阶段表格（前向→反向→清理），说明 `saved_tensors` 的概念
  - 详细对比两个 API 的作用时机、保留对象、代价
  - `saved_tensors` 的定义和为什么 `grad_fn` 持有它们
  - 多任务学习的完整代码示例（共享 encoder + 两个 head）
  - 同时使用两个 API 的代码示例，展示两者"正交"的具体含义

---

### 3. **小雨 Q3：L = tanh(w*x + b) 对 w 求导的链式法则**

**问题**：tanh 导数公式 `1 - tanh²(z)` 是否正确？有没有其他形式？

**改动**：
- **位置**：Cell 5（参数实验）前改写
- **内容**：
  - 完整的链式法则推导：`∂L/∂w = (∂L/∂z) · (∂z/∂w)`
  - 标注每一步的来源（∂z/∂w = x，∂L/∂z = 1 - tanh²(z)）
  - tanh 导数的两种等价形式（`1 - tanh²(z)` vs `sech²(z)`）
  - 从定义推导 tanh 导数的代码示例

---

### 4. **小雨 Q4：verify_gradients() 函数的理解**

**问题**：确认实现逻辑，需不需要 `detach()` 或清零？

**改动**：
- **位置**：Cell 4（verify_gradients 说明）改进
- **内容**：
  - 新增运算符重载的细节说明（`2 * a` → `__rmul__` 机制）
  - 说明 `2*a*b + 3*b` 全程在计算图上
  - 标量 Tensor 的 `backward()` 使用方法

---

### 5. **小雨 Q5：梯度累积（gradient accumulation）的实际用途**

**问题**：什么是"梯度累积训练"？什么场景下想要累积而不清零？

**改动**：
- **位置**：Cell 3（梯度累积）后插入新 markdown cell "梯度累积的实际场景：显存不足的分批训练"
- **内容**：
  - 显存限制场景：实际需要 batch_size=128，但只能跑 batch_size=32
  - 错误做法 vs 正确做法的代码对比
  - 梯度累积的数学原理：4 个 batch 累积后 = 128 样本的平均梯度
  - 显存 ↔ 精度的权衡说明

---

### 6. **老陈 Q1：叶节点的判断规则边界情况**

**问题**：`c = torch.tensor(3.0, requires_grad=True) * 2` 算叶子还是非叶子？

**改动**：
- **位置**：Cell 4 的新增部分（深入理解）中已包含
- **内容**：边界情况说明确认了"**only 直接创建的 + requires_grad=True 才是叶子，运算结果都是非叶子**"

---

### 7. **老陈 Q2：retain_grad() 和 retain_graph=True 的实际区别**

**问题**：两个 API 保留的对象不同吗？是同一个"计算图"的不同侧面吗？

**改动**：
- **位置**：Cell 3.5 完整改写（见小雨 Q2 的改动）
- **内容**：通过 backward 三阶段表格和对比表格，清晰区分两个 API 的作用范围和代价

---

### 8. **老陈 Q3：saved_tensors 的含义**

**问题**：`saved_tensors` 具体指什么？为什么是 `grad_fn` 持有而不是 Tensor？

**改动**：
- **位置**：Cell 3.5 新增小节"什么是 `saved_tensors`"
- **内容**：
  - 定义：反向求导需要的前向中间值（如乘法的两个操作数）
  - 具体例子：`c = a * b` 求导需要 `b` 的值
  - 为什么 `grad_fn` 持有：因为每个运算都需要不同的中间值

---

### 9. **老陈 Q4：运算符重载 `2 * a * b` 是否能正确运行**

**问题**：int 2 调用的是 `__mul__` 还是 Tensor 的 `__rmul__`？能确保正确吗？

**改动**：
- **位置**：Cell 4（verify_gradients 说明）新增"运算符重载的细节"
- **内容**：详细说明 Python 的运算符重载机制在这个表达式中的工作流程

---

### 10. **老陈 Q5：梯度累积的使用场景**

**问题**：什么时候我会想要两次 backward 的梯度加起来？

**改动**：
- **位置**：Cell 3 后新增 markdown cell（见小雨 Q5 的改动）
- **内容**：显存不足的分批训练是最常见的梯度累积场景

---

### 11. **老陈 Q6：叶节点的初始梯度值**

**问题**：`x.grad` 在 backward 前是 None 还是 0.0？PyTorch 怎么"给 None 加数字"？

**改动**：
- **位置**：Cell 4 的新增部分（深入理解）中已说明
- **内容**：backward 前 `.grad` 是 `None`；第一次 backward 时创建 Tensor；后续加法是标准 Tensor 运算

---

### 12. **琳达 Q1：叶节点和非叶节点设计的必然性**

**问题**：为什么必须这样分？是为了显存的人为划分还是有更深的原因？

**改动**：
- **位置**：Cell 4 的新增部分（深入理解）
- **内容**：说明了数学上的必然性——参数才需要梯度更新，激活值梯度是中间过程

---

### 13. **琳达 Q2：链式法则在 dL/dw = 2z·x 中的应用**

**问题**：是链式法则吗？希望能明确标注。

**改动**：
- **位置**：Cell 5（参数实验）
- **内容**：在链式法则推导中**明确标注**"← 链式法则"，并逐步展开计算

---

### 14. **琳达 Q3：非叶节点梯度默认为 None 的实际含义，什么时候必须看中间梯度**

**问题**：什么时候必须看中间梯度，什么时候可以忽略？

**改动**：
- **位置**：Cell 4 的新增部分（深入理解）
- **内容**：
  - 标准训练：只看参数梯度，中间梯度可忽略
  - 调试梯度消失/爆炸：需要看中间梯度 → 用 `retain_grad()`

---

### 15. **琳达 Q4：backward(retain_graph=True) 和 retain_grad() 的区别，不能合并吗？**

**问题**：为什么这两个概念容易混淆？为什么不能用一个 API 搞定？

**改动**：
- **位置**：Cell 3.5 完整改写
- **内容**：
  - 通过 backward 三阶段表格展示两个 API 作用在不同阶段
  - 对比表格清晰列出时机、对象、代价的差异
  - 多任务学习代码示例说明为什么需要分开

---

### 16. **琳达 Q5：detach() 的典型应用场景**

**问题**："转 numpy 打印" 和 "GAN 固定路径" 两个用途风格不同，缺乏统一说明。

**改动**：
- **位置**：Cell 2（detach 与 .grad 的存储规则）完全改写
- **内容**：
  - 三个典型场景统一框架说明（调试/打印、GAN 分离、EMA 冻结）
  - 每个场景都有对应的代码示例
  - 最后对比 `detach()` vs `retain_grad()` 的问题域差异

---

### 17. **琳达 Q6：偏导数的深层理解**

**问题**：机械应用偏导数规则，没有真正理解为什么这样推是对的。

**改动**：
- **位置**：Cell 4（verify_gradients）前插入新 markdown cell "背景：多变量函数的偏导数"
- **内容**：
  - 偏导数的直观定义："只改变某个变量，其他冻结"
  - `L = 2ab + 3b` 的具体例子，分别求 ∂L/∂a 和 ∂L/∂b
  - 计算方法：对每个变量分别求导，把其他看常数
  - 与神经网络的联系：参数梯度就是这样的偏导数

---

## 修订统计

- **总问题数**：17 条（跨越三位学生）
- **新增 markdown cell**：4 个
- **改写 markdown cell**：3 个（Cell 2, 3.5, 5）
- **改进 markdown cell**：2 个（Cell 4, 12）
- **涉及的核心概念**：
  - 叶/非叶节点的设计原因和显存权衡
  - `retain_grad()` vs `retain_graph=True` 的区别与 `saved_tensors`
  - 梯度累积的实际场景
  - 链式法则和 tanh 导数
  - detach() 的三大应用场景
  - 偏导数的直观理解

## 核心补充方向

所有修改遵循 **"科普风 + 逐步推演"** 原则：
1. **先问题/类比/故事**：为什么显存会爆？梯度累积解决什么实际问题？
2. **再公式/代码**：链式法则的完整推导，多任务学习的代码示例
3. **严禁"显然"**：每个关键步骤都有解释或示例

---

## 验证状态

✅ 修改后 notebook 通过 nbformat 合法性校验
