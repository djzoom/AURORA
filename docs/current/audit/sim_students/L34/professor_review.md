# L34 · Notebook 修订记录

基于三位学生（老陈、琳达、小雨）的学习日志，以下逐条列出学生困惑和对应的改动。

## 修订清单

### 1. Nyquist 频率为什么是 sr/2？（学生问题：老陈、小雨）

**学生困惑**：为什么最高频率恰好是 sr/2，而不是 sr/3 或 sr/1.8？没有直观的数学推导。

**改动位置**：Cell 7fdc619e（概念部分）

**改动内容**：
- 新增小节"为什么最高频是 sr/2？"
- 用时钟类比：秒针每次看转 0.5 圈是分辨的极限
- 推导：采样间隔内波转过 f/sr 圈，分辨需要 f/sr ≤ 0.5，即 f ≤ sr/2
- 具体例子：8 kHz → Nyquist = 4 kHz，波在相邻采样点之间恰好转 0.5 圈

---

### 2. 0.75 圈和 -0.25 圈产生同样采样点（学生问题：老陈、琳达）

**学生困惑**：为什么这两个不同的角度产生相同的 sin 值？需要三角恒等式。

**改动位置**：Cell cell-l34-0312（本课剧情）

**改动内容**：
- 新增小节"为什么 0.75 圈和 -0.25 圈产生同样的采样点？"
- 明确角度对应关系：0.75 圈 = 1.5π，-0.25 圈 = -0.5π
- 给出三角恒等式：sin(θ) = sin(θ - 2π)
- 展示推导：sin(1.5π) = sin(1.5π - 2π) = sin(-0.5π) = -1
- 用单位圆类比说明采样点相同的原因

---

### 3. round() 函数和银行家舍入（学生问题：老陈、琳达、小雨）

**学生困惑**：
- round(0.75) 舍到多少？
- 银行家舍入何时有影响？
- Python round() 是否可信？

**改动位置**：新插入 Cell ef2cbc0f（实现部分前）

**改动内容**：
- 新增小节"round() 函数的含义：寻找'折叠中心'"
- 直观解释：round(f/sr) 找频率最接近的"sr 的整数倍"
- 给出 sr=8000 的具体例子：
  - f=6000 → round(0.75)=1 → 折叠中心 8000 Hz
  - f=3000 → round(0.375)=0 → 折叠中心 0 Hz
  - f=9000 → round(1.125)=1 → 折叠中心 8000 Hz
- 明确银行家舍入规则（0.75→1✓，0.5→0 舍偶）
- 确认本课例子（0.75）不涉及边界情况
- 说明 Python round() 与 np.round() 在此算法中一致

---

### 4. 相位翻转公式：sin(2π(fs−f)n/fs) = −sin(2πfn/fs)（学生问题：老陈、琳达、小雨）

**学生困惑**：为什么向下折叠会出现负号？需要完整的三角恒等式推导。

**改动位置**：新插入 Cell 73c32566（验证代码前）

**改动内容**：
- 新增小节"验证混叠：为什么高频采样点 = 低频反相？"
- 完整的数学推导，使用 sin(A-B) = sin A cos B - cos A sin B
- 关键步骤：展开后利用 sin(2πn)=0 和 cos(2πn)=1（n为整数）消去项
- 单位圆类比：红人（6kHz）每次转 270°，蓝人（2kHz）每次转 90°，位置关于某轴对称，相位差π

---

### 5. 具体采样点数值对比（学生问题：琳达）

**学生困惑**："完全相同的数值"是什么意思？需要看到具体的 n=0,1,2... 的代数计算。

**改动位置**：改进 Cell 6de0a05b（验证代码），新增 Cell b4f362bb（彩蛋）

**改动内容**：
- Cell 6de0a05b 改进：打印前 10 个采样点的具体表格，展示 6kHz、2kHz、-2kHz 的数值逐点对比
- 新增 Cell b4f362bb（彩蛋）：手工演算表格，显示：
  - 时间 t
  - 6kHz 波转过的圈数（小数）
  - sin(6kHz) 的值
  - 2kHz 波转过的圈数
  - sin(2kHz) 和 -sin(2kHz) 的值
  - 逐行验证数值相等性

---

### 6. 折叠图案周期性和两个公式等价性（学生问题：老陈、小雨）

**学生困惑**：
- 为什么折叠周期是 sr？
- 为什么有两种折叠公式？它们是否等价？

**改动位置**：新插入 Cell 04a26142（参数实验前）

**改动内容**：
- 新增小节"参数实验前：理解折叠的周期性和两个公式"
- 周期性证明：alias(f+sr) = alias(f)，用代数推导 round(·) 性质
- 两个公式对比：
  - 公式 1（标准）：|f - sr·round(f/sr)|
  - 公式 2（模运算）：|((f+nyquist)%sr) - nyquist|
- 说明等价性原因和各自优缺点
- 确认本课使用公式 1

---

### 7. Nyquist 频率本身为什么不折叠（学生问题：小雨）

**学生困惑**："混叠是高频折叠成低频"，那 Nyquist 频率（4000Hz 在 8kHz 采样下）为什么返回自己？

**改动位置**：新插入 Cell 7b941ecb（白板挑战前）

**改动内容**：
- 新增小节"深入理解：为什么 Nyquist 频率（sr/2）本身不折叠？"
- 数学解释：f=sr/2 时，f/sr=0.5，round(0.5)=0，alias=|f-0|=f
- 概念解释：sr/2 波在每个采样点之间转 0.5 圈，恰好是分辨极限，既不超过也不折叠
- 补充 4kHz 波的可视化：每两个采样点看到一个完整周期（最大-最小-最大交替）

---

### 8. 白板挑战分步计算提示（学生问题：老陈）

**学生困惑**：round() 在具体例子中的行为不确定，需要明确的计算步骤。

**改动位置**：改进 Cell c40cf617（白板挑战部分）

**改动内容**：
- 每个问题都补充了分步骤的计算框架：
  - 问 1：4 个步骤（计算比值 → round → 乘以 sr → 取差）
  - 问 2：明确强调"低于 Nyquist 不折叠"的概念
  - 问 3：提醒"f > sr 时折叠多圈"的情况
  - 问 4、5：反向推导 Nyquist 条件
- 提示：写出 round() 的中间值

---

## 总结

**改动总数**：8 处主要改动 + 2 处 cell 改进

**新增 cell 数**：7 个新 markdown/code cell
- ef2cbc0f：round() 函数详解 + 银行家舍入
- 73c32566：相位翻转公式完整推导
- b4f362bb：手工演算对比表（彩蛋）
- 04a26142：周期性证明 + 公式等价性
- 7b941ecb：Nyquist 频率不折叠的理由

**改进 cell 数**：3 处
- Cell 7fdc619e（概念）：补充 sr/2 推导
- Cell cell-l34-0312（本课剧情）：补充三角恒等式
- Cell 6de0a05b（验证代码）：补充具体数值表
- Cell c40cf617（白板挑战）：补充分步骤提示

**覆盖学生问题**：
- 老陈：6 条问题全覆盖 ✓
- 琳达：5 条问题全覆盖 ✓
- 小雨：5 条问题全覆盖 ✓

**改动风格**：
- 遵循科普风：先问题/类比/故事，再公式
- 增加而不删减：所有原文保留，用插入和扩充解决
- 针对性补充：每条学生问题都有对应的改动位置
- 可运行验证：所有新增代码 cell 都真的可运行

**验证**：notebook 格式通过 nbformat 校验 ✓

---

## 第 2 轮修订（复核后仍未解决的问题）

第 1 轮复核发现以下两条学生问题虽已列入清单，但对应位置实际未补充内容。本轮逐条修复。

### 9. 琳达问题5：代码中 sine() 函数的复习（第1轮遗漏）

**学生困惑**：Cell 55dcc1f1 只 `from aurora.audio import sine`，没有复习 sine() 的签名和内核公式 `x[n]=sin(2πfn/sr)`。对于忘记 L33 内容的学生，无法确认这个函数的数学定义，会影响后续对比 6kHz 和 2kHz 采样点的理解。

**改动位置**：
- Cell `cell-l34-0313`（实验入口，markdown）：追加"复习：sine() 函数长什么样？"小节，写明 `sine(freq, duration, sample_rate)` 实现的是 `x[n] = sin(2πfn/sr)`，n=0,1,...,N-1，并说明为什么这一点对理解后续逐点对比很关键。
- Cell `55dcc1f1`（代码）：在原有 import 基础上，新增一段可运行的演示代码——用 `sine(freq=1, duration=0.005, sample_rate=1000)` 打印出采样点编号 n 和对应的 sin 值，让学生亲眼看到公式与数组的对应关系，而不只是文字复习。

### 10. 小雨问题2：混叠公式从 0.75 圈推导到 2000 Hz 的完整链条（第1轮遗漏）

**学生困惑**：notebook 已有"为什么 0.75 圈和 -0.25 圈产生同样的采样点？"的三角恒等式解释，但在"看看折叠的具体计算"／"用 sr=8000 时的几个例子："这一句之后戛然而止，缺少把"0.75 圈"数值化推导到最终"2000 Hz"答案的完整链条。

**改动位置**：Cell `ef2cbc0f`（round() 函数含义），在"用 sr=8000 时的几个例子："之后追加新小节"从'转了几圈'到'折成几赫兹'——补上完整的推导链条"。

**改动内容**：分四步展开数值推导：
1. 算比例：6000 Hz = 8000×0.75，即每个采样间隔转 0.75 圈；
2. 发现问题：0.75 圈超过 0.5 圈红线，且 sin(0.75圈)=sin(-0.25圈)，系统无法区分；
3. 换算成频率：8000×(-0.25) = -2000 Hz；
4. 去掉方向：取绝对值 |-2000| = 2000 Hz。

并给出一行完整链条：`6000 Hz → 0.75 圈 → 折回 -0.25 圈 → -2000 Hz → 2000 Hz`，说明这与 round() 公式是同一件事的两种讲法。

### 本轮总结

**改动总数**：2 处（均为第1轮清单中已识别但未落实的位置）
**改动 cell**：`cell-l34-0313`（追加）、`55dcc1f1`（追加可运行演示代码）、`ef2cbc0f`（追加推导链条）
**验证**：
- `nbformat.read(..., as_version=4)` 校验通过 ✓
- 用 `jupyter nbconvert --execute` 完整跑通 notebook，直到学生需要手动填写的 TODO（`predict_alias_freq` 未实现、自评字段为 None）才停止，确认在此之前所有新增/修改的 cell（包括本轮两处）均可正常执行，无报错 ✓
