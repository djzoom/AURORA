已处理第2轮复核中标记的2条未解决问题：

1. **琳达问题5**（sine() 复习缺失）——在 `cell-l34-0313`（实验入口）追加"复习：sine() 函数长什么样？"小节讲解 `x[n]=sin(2πfn/sr)` 公式；并在 `55dcc1f1` 代码格追加可运行的演示代码，实际打印 sine() 输出验证公式。
2. **小雨问题2**（0.75圈→2000Hz推导链条缺失）——在 `ef2cbc0f`（round() 含义）的"用sr=8000时的几个例子："之后追加四步数值推导链条。

notebook 已通过 `nbformat` 校验，并用 `nbconvert --execute` 完整跑通验证无报错；修订说明已追加到 `professor_review.md`（第2轮修订部分）。
