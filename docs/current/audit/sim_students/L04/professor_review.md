# L04 教授修订记录（2026-07-07）

依据老陈、琳达、小雨三份学习日志，对 `notebooks/1_complex_trig/L04_trig.ipynb` 做了只增不删的修订。
共处理 15 条学生反馈（合并为 8 个主题），全部代码 cell 在 TODO/已实现两种状态下端到端跑通，
`nbformat` 校验与 `scripts/validate_pipeline.py` 全部通过。

## 逐条对照：学生问题 → 改动内容与位置

### 1. 复数 `np.exp(1j·angles)` 空降、零铺垫（老陈 P1 / 琳达 P1 / 小雨 P1）
- 在「动手观察：四个特殊角度」的说明与代码之间**新增 markdown cell**（黑盒声明）：
  讲清 `1j` 是 Python 的虚数单位写法（含"为什么是 j、为什么必须带 1"）；把 `np.exp(1j*θ)`
  定位为"喂角度、同时吐出 cos 和 sin 的黑盒机器"（`z.real`=cos、`z.imag`=sin）；明确
  "原理 L05/L06 揭晓，本课不要求理解、你没有漏学"；并解释现在出场的理由（它是"单位圆
  转动点"的代码化身）。
- 尾部「小检查：同一个角度，同时看 cos、sin、复数」代码 cell **顶部追加注释**，重申同一
  黑盒约定，避免二次撞见时再困惑。

### 2. "1.85 倍、1.41 倍"从天而降、代码没算过（老陈 P2 / 琳达 P4 / 小雨 P2）
- 在「代码实验：遍历频率、振幅和相位」的打印 cell 之后**新增 markdown + code 两个 cell**：
  - markdown：先承认"不该让读者自己发明步骤"，介绍 `np.diff`（"相邻两数相减"，配
    4 个数的小例子）；再给"为什么不是精确翻倍"的直白解释——正弦被 ±1 封顶，相邻差
    最大是 2，增长必然饱和（1.85 → 1.41 → 趋近 1）；并点破 1.41 = 1.0/0.707 = √2，
    来自 f=4 时样本恰好落在峰/谷/零点上，不是巧合。
  - code：用 `np.diff` + `np.max(np.abs(...))` 把每档频率的"最大一步"和相邻档倍数
    （1.85、1.41）直接打印出来，说明里的数字在输出里可以直接看到。

### 3. 过零次数 2f−1 只给结论；"4 倍正比"表述绕（老陈 P3 / 琳达 P3 / 小雨 P3 前半）
- 在「参数实验：只改一个旋钮」markdown cell 内**追加三个小节**（原文一字未删）：
  - 「为什么是 2f−1」：先肯定学生的 2f 直觉，再以 f=1 逐点对账——t=0 的零是"出发"
    不是"穿过"（第一个样本无前项可比），t=1.0 的穿越落在采样窗口 [0,1) 之外，所以
    "掐头去尾"恒少 1；−1 来自数数方式与窗口边界，不是波本身少振。
  - 「"并不是简单的 4 倍正比"翻译成人话」：写明原句想否定的误读（1×4=4），理论值
    2f 才是正比，实测 2f−1 只是恒差 1。
  - 「动手改 phi」：承认原示例代码没有 phi 变量、"想改也没处下手"，引出下方新 cell；
    附小彩蛋——phi=π/2 时零点挪离窗口边界，会数到完整的 2f 次，反向验证 −1 的来历。

### 4. `signbit/diff/布尔求和` 三个没教过的函数是天书；解释文字在代码后面（小雨 P3）
- 在过零计数代码 cell **之前新增 markdown cell**「先认识三位新朋友」：用
  `x = [0.5, -0.2, -0.7, 0.3]` 的 4 数小例子逐步拆解 signbit（是不是负数）→ diff
  （相邻相减找变号）→ sum（True 当 1 数个数），并预告"理论 2f、实测差一点"悬念，
  让说明先于代码出现。

### 5. 示例没有 phi 可改、想跟指令做实验做不了（小雨 P3 后半 / 老陈 P3 关联）
- 在「参数实验：只改一个旋钮」之后**新增 code cell**：带真实 `f`、`phi` 两个可改变量的
  过零计数器，逐行中文注释复述三个函数的作用，并作图用红叉标出每个被数到的过零点，
  标题同时给出实测值与理论 2f，"数出来的次数"可见可验。

### 6. 和弦 demo sr=200 与"每周期采样点 = sr/f"公式自相矛盾（老陈 P4 / 琳达 P2 / 小雨 P5）
- 在和弦代码 cell **之前新增 markdown cell**（预防针）：先表扬学生用问 3 公式自查
  （200/261.6 ≈ 0.76 点/周期，检验完全正确）；引入混叠（aliasing）概念，用"电影里
  马车轮倒转"类比；给出三个音各自的伪装频率（261.6→61.6、329.6→70.4、392→8 Hz，
  与琳达实测"392 Hz 像 8 Hz 慢波"吻合）；预告奈奎斯特条件（sr > 2×最高频率，L07 讲）；
  说明 sr=200 下 DFT 也无法分解回 C4/E4/G4；解释保留原 cell 的理由（叠加思想没错，
  且是绝佳反面教材）。
- 在和弦代码 cell **之后新增 markdown + code 两个 cell**（修好它）：sr 提到 8000、
  只画前 20 ms（并解释为什么截短），每张子图标注"20 ms 内应有几个周期——数数看"，
  打印"每周期 30.6 点、公式自查通过"，并补上"这个采样率下 DFT 才能真正分解"的呼应。

### 7. 白板问 2 需要 sin(π+x) = −sin(x)，诱导公式忘光（老陈 P5 / 小雨备注）
- 在白板挑战 markdown cell 问 2 提示后**追加"救生圈"引文块**：给出诱导公式本身，并用
  第 1 节单位圆重造它——"角度加 π = 转过半圈 = 影子跑到圆心对面 = 变号"，算到
  −√2/2 ≈ −0.707；强调画面比背公式可靠。
- 「对答案格」code cell 中 `x2_theory` 处**追加分步注释**：① 诱导公式变号 ② 特殊角
  sin(π/4)=√2/2 ③ 合成 −√2，补上老陈指出被跳过的中间一步。

### 8. 单位圆"影子"只有文字没有图；"超前"是左移还是加 π/2 打架（小雨 P4）
- 第 1 节 markdown cell 末尾**追加路标**：预告两个常见疑问（圆在哪、超前往哪边）将在
  下面第二格代码解决，防止读者卡在原地。
- 在 sin/cos 曲线图 cell 之后**新增 markdown + code 两个 cell**：
  - code：左图画单位圆 + 转动点（θ=π/3）+ 两条投影虚线并标注"x 轴影子 = cos θ、
    y 轴影子 = sin θ"；右图在 sin/cos 曲线上用红箭头标出"同一个峰，cos 早了 π/2 出现"。
  - markdown：把"超前"的三种说法对齐——事件更早发生 = 曲线整体左移 = 自变量加 π/2，
    并用 θ=0 代入 `sin(0+π/2)=1=cos(0)` 自检，点破"加号加在括号里、曲线反而往左移"
    这个最易记反的点。

## 验证
- `python3 -c "import nbformat; nbformat.read(...)"` 通过；`nbformat.validate` 通过（37 cells）。
- 全部 code cell 按顺序在同一命名空间执行两遍（sinusoid 未实现 / 已实现），无报错，
  新增 cell 输出与正文数字（1.85、1.41、2f−1、30.6 点/周期等）逐一吻合。
- `scripts/validate_pipeline.py` 全部检查 PASSED。
