完成！修订总结如下：

**处理了 17 条学生问题，做了 8 处改动**：

1. **新增"闭包详解"**（在"概念1"后）— 直观对比"直接存数值"vs"闭包捕获对象"，解释为什么需要延迟执行
2. **新增"幂函数导数推导"** — 从导数定义推导 `d(x^n)/dx = n·x^(n-1)`，验证负幂例子
3. **新增"tanh导数推导详解"** — 用商法则从 `(e^{2x}-1)/(e^{2x}+1)` 推导到 `1-tanh²(x)`
4. **增强"概念2"** — 加入"为什么只用前向值不用 x"的数学特性说明及与其他激活函数的对比
5. **增强"概念3"** — 补充"非光滑"概念、次梯度定义、"概率极小"的实践解释、Dead ReLU问题
6. **增强"参数实验"** — 逐步展开链式法则 `∂output/∂w1 = ∂output/∂z · ∂z/∂w1`，对比relu激活下梯度为0的现象
7. **增强"实现"说明** — 补充exp用途（softmax、cross-entropy）及完整导数表格
8. **新增"拓扑排序深挖"**（在Value类后）— 用"建房子"类比、递归执行流程追踪、具体数值例子说明为何必须逆序遍历

→ 全部改动都在学生卡住的位置附近，遵循"只增加不删减"原则，notebook从20cells→25cells，已通过nbformat校验 ✅
