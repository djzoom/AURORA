# L01 教授复审 · 修订说明（2026-07-07）

依据 5 位学生（阿凯、老陈、琳达、小美、小雨）的学习日志，对
`notebooks/0_foundation/L01_motivation.ipynb` 做了只增不减的修订。
原有 cell 内容全部保留（唯一的位置调整：FFT 冒烟测试格原样移到热身预测格之后，
内容一字未改）。修订后全部代码格按顺序实测跑通，nbformat 校验通过，
`scripts/validate_pipeline.py` 全绿。

## 学生问题 → 改动内容与位置

### 一、第 1 节正弦波代码（5 人中 4 人卡住）

1. **阿凯 P1 / 老陈 P5 / 小美 P1：`2π` 哪来的、sin 吃的是什么单位、弧度全课未提**
   → 在正弦波代码格（c2）**之前**新增 markdown「读第一段代码前：三把钥匙」（7e54a3ed）：
   钥匙 3 专讲弧度——转一整圈 = 360° = 2π 弧度；`freq * t` = "到时刻 t 转了几圈"，
   乘 2π 把圈数换算成弧度；并给出小美要的"点绕圆转圈、sin 是它的高度"类比。

2. **阿凯 P2 / 老陈 P5 / 小雨 P1：`t = np.arange(round(duration*sr))/sr` 与 sr=16000 无解释、"A4 音"不明**
   → 同一新增 cell 的钥匙 1（采样率 = 每秒 16000 张快照，L02 详讲）和钥匙 2
   （两步拆解：先编号 0..319，再除以 sr 换算成秒；点数 = 时长×采样率）；
   末尾补"A4 = 钢琴标准音 A，440 Hz"。

3. **小雨 P1 / 小美 P1：缺一句"现在看不懂没关系"的安抚**
   → 该 cell 开头即写明"你现在的任务只是跑通它、看图感受——采样 L02 拆解、sin 和 2π 是 L04 主角"。

4. **（配套加固）** 在 c2 之后新增可运行验证格（db18d51b）：打印 `t[:5]`、相邻间隔
   =1/16000 秒、点数=320、理论波数 8.8 个 vs 实数波峰 9 个——把"先信了"换成"打印出来看"。

### 二、路线图与练习字典的编号错位（5 人全部提及）

5. **老陈 P4 / 小美 P5 / 小雨 P2 / 阿凯 P6 / 琳达（间接）：路线图 ①–⑪（L01–L05 开场…）与目录
   （0_foundation 只有 L01–L03、1_complex_trig 是 L04–L08）对不上；字典 10 键 vs 路线图 11 模块**
   → 两处改动：
   (a) 路线图 cell（c5）末尾追加「两张地图怎么对照」：逻辑模块 vs 硬盘文件夹的 11 行对照表，
   明确 ① 前三课独立成 `0_foundation`、① 后半 + ② 合并进 `1_complex_trig`；
   (b) 练习格（c9）前新增 markdown（db78db74）：字典键 ↔ 路线图圈号 ↔ L 编号三列对照表
   （即阿凯点名要的那张表），并解释为何字典没有 `0_foundation` 这一键。

### 三、course_purpose 练习写不动（琳达 P5、小美 P4）

6. **琳达 P5a / 小美 P4a：`8_music`、`9_llm` 等后段模块只有一行路线图文字，写不出具体的 Y**
   → 上述新增对照表（db78db74）为 10 个键各配一行"白话版：它大概做什么"
   （如 8_music = "把一首歌变成一串数字，让机器能比较和生成音乐"），作为动笔抓手。

7. **琳达 P5a / 小雨（自述抄路线图）：写得笼统怕不合格**
   → 同 cell 免责声明 1："后段模块写笼统没关系，甚至写'学到 L76 回来重写'也算合格答案；
   目的清单是活文档，每进新模块回来修订"。

8. **琳达 P5b：第 8 节承诺的 `<details>` 参考答案全课没有，solutions 文件无人提及**
   → 同 cell 免责声明 2：给出 `solutions/L01_motivation_solutions.md` 的显式链接；
   另外新增的 FFT 对答案格（af90a81a）内真正兑现了一个 `<details>` 折叠答案块。

### 四、术语密度（琳达 P6）

9. **琳达 P6：第 5 节通关标志表里 W&B / WER / CTC 等缩写无标注，读者会慌**
   → 通关标志 cell（c6）末尾追加安抚块："上表和第 7 节表里的名词现在一个都不需要懂，
   它们恰恰是课程要教的——读法是电影预告片"；并给 W&B、WER、CTC、KWS 各一行白话注解 + 课号。

### 五、FFT 冒烟测试与热身推理（5 人全部提及，问题最集中处）

10. **阿凯 P3 / 老陈 P1 / 琳达 P2 / 小美 P2 / 小雨 P3：热身说"运行下面的 FFT 格之前先预测"，
    但 FFT 格实际排在热身题上面，顺序执行必被剧透**
    → 将 FFT 验证格原样（内容零改动）移到"填答案格"（51014a91）之后。
    新顺序：概念铺垫 → 画图 → 热身题 → 写预测 → 运行 FFT → 对答案 → 反例实验。
    原文"在运行下面的 FFT 验证格之前"和"答案写完后运行下一格对答案"两句指示现在完全成立。
    （阿凯附带提出的"assert 消息剧透 DC 答案"：消息按只增不减原则原样保留，
    但由于概念包已提前讲明 out[0]=总和，该消息此时已是教学内容而非剧透。）

11. **老陈 P1 / 琳达 P1 / 小雨 P3：FFT 格"从天而降"，无一字说明它是干什么的**
    → FFT 段落之前新增小节「9.5 冒烟测试」（6d09408c）：解释 smoke test 的含义、
    这格只为确认 aurora 包能干活、FFT L37–L41 才手写、"看不全懂是设计如此，不是你的问题"。

12. **阿凯 P4 / 老陈 P3 / 琳达 P3 / 小雨 P4：热身 Q1 的"归一化 sr=1""基频"未定义**
    → 同一新增 cell 内给出"最小概念包"：采样点、归一化 sr=1（换成最方便的单位）、
    基频（N 个点里恰好装下 1 个完整来回的波，= sr/N，是"频率尺子"）。

13. **琳达 P3 / 小美 P3a：四个孤立的数字看不出"振荡"，需要一张图**
    → 新增可运行画图格（5ae5196f）：stem 画出 4 个采样点，叠加一条穿过它们的余弦曲线，
    标题标明"1→0→−1→0→回到 1：正好一整个来回"，打印读图结论"频率 = 1 倍基频"。

14. **老陈 P3 / 琳达 P4 / 阿凯 P5b / 小美 P3b：Q2 误导性暗示 out[0]=均值（实为总和 = N×均值），
    本例均值恰为 0 导致错误规律也能对上答案**
    → 三处修正：概念包中直接写明"out[0] = 所有采样点之和 = N×均值，不是均值本身"；
    对答案格 `<details>` 中点破"本例是巧合"；并新增可运行反例实验格（562f649e）——
    用老陈自己造的 `[2,1,0,1]`（均值 1 → out[0]=4）和 `[1,1,1,1]` 当场揭穿，
    还顺带演示"整体抬高只改 out[0]"的线性拆账直觉。

15. **阿凯 P5a / 老陈 P2 / 琳达 P1 / 小雨 P4：输出里的 `j` 和 `.real` 零铺垫**
    → 概念包最后一条："FFT 输出是复数 a+bj，j 暂时理解为'第二个坐标的记号'，
    `.real` 取前一半 a，L05 讲透，今天只看 .real"。

16. **阿凯 P4 / 小雨 P4：Q1 无处对答案——不知道输出哪个位置印证"1 倍基频"**
    → 新增对答案格（af90a81a）：给出"out 第 k 个位置 = k 倍基频成分强度"的读法，
    逐个解读 [0, 2, 0, 2] 四个数字，并附 `<details>` 折叠的 Q1/Q2 参考答案
    （明确 out[1]=2 就是 Q1 的对答案位置）。

17. **小美 P3c：out[3] 为什么和 out[1] 一样，怕是自己算错**
    → 同 cell 解读 out[3]："实数信号的对称性，out[N-k] 是 out[k] 的镜像，L38 推导——
    不是你算错了"。

18. **琳达 P4 / 小雨 P4："直流分量"为什么叫"直流"、声音里哪来的电流**
    → 概念包中解释：借自电学，直流电不振荡是一条水平线，故"频率 0 的成分"= 信号整体水平高度。

### 六、Python 语法前提（小雨 P5，老陈亦提及星号）

19. **小雨 P5a：练习默认会字典（字面量、逐条登记、items() 双变量遍历），但只学过列表**
    → `check_imports` 练习格（c14）之前新增「字典 30 秒速成」markdown（15abe503，
    通讯录类比 + 三招）和可运行热手演示格（83e0d888），末尾点明
    "check_imports 的骨架 = 空字典 → 逐个登记 → 返回"。

20. **小雨 P5b / 老陈（小结提及）：`*Path.cwd().parents` 的星号是黑盒，只能照抄**
    → `environment_report` 骨架格（c18）之前新增 markdown（29831e3b）：星号 = 解包，
    摊平 vs 塞入的对比示例，给出小雨要的无星号等价写法
    `[Path.cwd()] + list(Path.cwd().parents)`；再配可运行验证格（fb9c597c）
    现场证明两种写法结果相同，并打印前 3 个候选目录。

## 验证

- `python3 -c "import nbformat; nbformat.read(..., as_version=4)"` 通过（41 cells，schema 合法）。
- 全部 code cell 按 notebook 顺序模拟执行（TODO 按学生标准答案填入）全部通过，
  包括 6 个新增代码格。
- `python scripts/validate_pipeline.py` 5 项检查全绿（99 notebooks 结构检查含 L01）。

---

# L01 教授复审 · 第 2 轮修订说明（2026-07-07）

第 1 轮复核确认仅剩 1 条遗留问题，本轮已处理；其余 5 位学生的全部问题
在第 1 轮均已解决（见上文），本轮未做无关改动。

## 学生问题 → 改动内容与位置

### 琳达 P5（对答案部分，第 1 轮遗留）：cell db78db74 承诺 solutions 文件
"包括本练习（course_purpose）的示范写法"，但该文件实际只有 check_imports 与
environment_report 两个函数实现——学生按链接去对 course_purpose 答案会扑空，
承诺再次落空。

→ 两处改动（采纳复核建议的第一方案：补全 solutions，兑现承诺）：

1. **`notebooks/0_foundation/solutions/L01_motivation_solutions.md`**：
   在「参考实现 1」之前新增一节「**参考实现 0 · course_purpose 示范写法（第 6 节练习）**」：
   - 开宗明义"没有唯一标准答案，考的是能不能把模块接到 Aurora 上"；
   - **样品 A（具体版）** 两条：`2_linear_algebra`（DFT = 一次矩阵乘法）、
     `5_audio_dsp`（亲手写 FFT/STFT/Mel，造出 Whisper 输入端的"声音配方表"）；
   - **样品 B（合格的笼统版）** 一条：`8_music`——诚实写"我还不知道 embedding
     是什么 + 学到 L76 回来重写"，正是琳达要的"后段陌生模块合格笼统答案"示例；
   - 附两条自查标准（X/Y 是否落实；活文档约定：进新模块后把 B 升级为 A）。

2. **notebook cell db78db74（第 6 节练习前的"抓手"cell）**：
   保留原文，仅在免责声明 2 末尾**追加**一段指路文字：明确"打开文件后第一节
   『参考实现 0』就是本练习的示范写法（两条具体版 + 一条笼统版样品），
   往下的参考实现 1/2 才是两个函数的代码"——学生点链接后不再需要自己翻找。

## 验证

- `python3 -c "import nbformat; nbformat.read(...)"` + `nbformat.validate` 通过（41 cells）。
- 将 solutions 新增的示范条目（含隐式字符串拼接的多行写法）填入 course_purpose
  实测：notebook 第 6 节验证格（c10）的两条 assert 全部通过。
- 本轮未移动、未删除任何 cell；notebook 仅 db78db74 一处追加文字。
