好的，我已经完整学习了整个 notebook。现在让我按照小雨的身份写一份真实的学习日志：

---

# 小雨 — L18 学习日志

## 我理解了什么

一个矩阵要么能"反向操作"（可逆），要么不能。判断可逆有三套方法：看行列式、看特征值，或者用"严格对角占优"快速检查。我能写出 `is_sdd()` 函数逐行验证矩阵对角元是否明显大于其他元素，而且实验证明 SDD 矩阵在 Jacobi 迭代中会收敛、非 SDD 矩阵会飙升发散。

## 遇到的困难和问题

### 【理解】秩（rank）和"线性无关"到底是什么关系？
在"秩 = 线性无关行（列）的数目"这里卡住了。课程说"秩 = n 才能可逆"，但什么是"线性无关"？为什么一行如果能由其他行组合出来，矩阵就不可逆了？需要一个具体的 2×2 例子：比如 [[1,2],[2,4]]，第二行是第一行的 2 倍，所以秩只有 1，不可逆。这个因果链条清楚了吗？还是有其他原因？

### 【理解】什么是"奇异值"和 SVD？为什么 Vt[-1] 就是零空间向量？
代码里突然出现 `U, s, Vt = np.linalg.svd(A)`，我不知道 SVD 是什么。σ（奇异值）是什么？为什么最后一个奇异值是 0 就代表存在零空间？为什么零空间向量恰好是 Vt 的最后一行？这跳过了太多推导。

### 【理解】"残差"（residual）和迭代收敛的关系不直观
在 Jacobi 迭代的例子里，看到 ‖Ax_k - b‖ 逐步缩小就说"收敛"了。但为什么要计算 Ax_k - b？这是什么意思？如果 Ax_k 越来越接近 b，说明 x_k 越来越接近真实解吗？需要从"我们在用迭代解什么方程"开始讲清楚。

### 【理解】特征值 = "缩放因子"的几何直观缺失
说"有一个方向缩放为 0，信息丢失，所以不可逆"，听起来有道理，但我想象不出来。一个 2×2 矩阵有两个特征值，一个是 0 时，它到底对平面做了什么？是把平面压成一条直线吗？需要一个图示：原向量空间 → 矩阵作用后 → 坍缩的情况。

### 【理解】det(A) 与"把空间压扁"的关系还是很虚
比喻说"行列式等于零 = 把空间压扁一个维度"，但这个比喻没有视觉化。一个 3×3 矩阵，det = 0 时，是把 3D 压成 2D？还是 1D？还是一个点？行列式的符号表示什么？需要一个具体的例子和图。

---
