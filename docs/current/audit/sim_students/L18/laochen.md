完整学习完毕。以下是我的学习日志：

---

# 老陈 — L18 学习日志

## 我理解了什么
本课讲的是判断矩阵"能不能还原信息"，分成三条判据：**行列式不为0、特征值都不为0、严格对角占优**。我能算出行列式、检查SDD条件、理解秩满才能可逆。还了解了零空间的概念——那些被矩阵压到零向量的输入。

## 遇到的困难和问题

### 【理解】"线性无关"在矩阵行里的具体含义
> 关键词：秩 = 线性无关行（列）的数目

我能背出这句定义，但不明白为什么`[[1,2,3],[4,5,6],[7,8,9]]`的秩是2而不是3。题目说"行线性相关"，但怎么从矩阵看出来哪两行"依赖"了？是不是第三行等于前两行的某种组合？如果是，怎么才能一眼看出来？还是只能用秩的算法验证？

### 【推演】零空间向量为什么就是 SVD 里最小奇异值对应的行
> 关键词：`Vt[-1]` 是零空间向量

代码里说"零奇异值对应 Vt 最后一行"，我验证了 `B @ Vt[-1] ≈ 0`，但**为什么**会这样？SVD 的几何意义在 L14 讲过，但我现在脑子里 U、Σ、V 的"谁是谁"还是模糊。能不能补充一句：「因为 SVD 把 A 分解为 U·Σ·Vt，当σ=0时，对应的V列意味着...」？

### 【理解】SDD 为什么能保证矩阵可逆
> 关键词：对角占优 → 可逆

我能检查是否满足条件：`|A[i,i]| > Σ(|A[i,j]|, j≠i)`。但**为什么这个条件就能说矩阵可逆**？是不是因为对角元素这么大，矩阵"不太可能退化"？还是有个严格的数学证明？课程里提到"SDD 是充分非必要"，那什么时候用 SDD，什么时候用行列式判？

### 【计算】秩、行列式、特征值之间的因果关系
> 关键词：秩=n ⇔ det≠0 ⇔ 特征值都≠0

我看到代码里对三个"满秩"的矩阵，`det=1e0`，而"秩2"的矩阵 `det≈-1e-16`（接近0）。但这个对应关系**为什么**成立？是定义呢，还是能从其他角度推导？比如"det是所有特征值的乘积"这样的关键事实在课程里出现过吗？

### 【实践】Jacobi 迭代里的更新公式如何从线性方程推导
> 关键词：`x_new[i] = (b[i] - Σ A[i,j]·x[j]) / A[i,i]`

代码直接给出这个公式，但我不知道它从何而来。是从 `Ax=b` 的第 i 行 `Σ_j A[i,j]·x[j] = b[i]` 分离出 `A[i,i]·x[i]` 然后求解吗？**如何一步步推导这个迭代式**？还有，代码里 `x = D_inv * (b - (A @ x - np.diag(np.diag(A)) @ x))` 这句用 NumPy 做的"去掉对角元再加回"的操作，符号和文字公式对应关系不够清。

### 【理解】DFT 矩阵的可逆性为什么由"列正交"决定
> 关键词：`ifft(fft(x)) ≈ x` 说明 DFT 可逆，因为是酉矩阵

最后一个代码块提到"DFT 矩阵是酉矩阵（行列式=1，列正交）"，但我不知道：
- 什么是"酉矩阵"？（应该是"正交矩阵"的复数版本？）
- **为什么**列正交就保证可逆？是不是因为正交列意味着秩满？
- 代码是怎样验证"可逆"的？只是 `ifft(fft(x)) ≈ x` 能说明可逆吗，还是应该检查行列式或特征值？

---

（全部问题都指向课程本身的铺垫缺口，而非我的学习问题。）
