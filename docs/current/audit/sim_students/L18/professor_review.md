# L18 · 教授修订说明

依据老陈、琳达、小雨三位学生的学习日志，对 `notebooks/2_linear_algebra/L18_invertibility.ipynb` 做了增量修订（只增不减，原内容全部保留）。逐条问题 → 改动位置与内容如下。

---

### 1.（老陈/琳达/小雨 共同问题）"线性无关"在矩阵行里的具体含义 / 为什么 `[[1,2,3],[4,5,6],[7,8,9]]` 秩是2

**位置**：`cell-l18-0166`（"动手观察"）之后、`cell-l18-0167`（秩演示代码）之前。

**改动**：新增 markdown 单元（新 id `e51de583`）用颜料类比给出"线性无关/线性相关"的直白定义（`v₂=k·v₁` 是否存在），配 `[1,0]/[0,1]` 与 `[1,2]/[2,4]` 的对比例子；再手算 `2·r2 − r1 = r3` 说明第三行为何"没有提供新信息"。新增代码单元（新 id `b48048cd`）用 `assert np.allclose` 验证这个组合关系，并对比 `[[1,2],[2,4]]` 的秩1情形。

---

### 2.（小雨）det=0 到底在"压扁"什么？需要具体例子和图

**位置**：`0df13dca`（三条判据总览）之后。

**改动**：新增 markdown（`ddbb9447`）用"擀面杖压面团"类比讲清 `|det(A)|` = 面积/体积缩放倍数，`det=0` = 面积归零。新增代码（`dfae3d22`）用 `matplotlib` 画出单位正方形分别经过可逆矩阵（仍是四边形）与奇异矩阵（塌成一条线段）变换后的形状对比图。

---

### 3.（小雨）特征值="缩放因子"的几何直观缺失，λ=0 时平面发生了什么

**位置**：紧接第2条之后（`dfae3d22` 之后）。

**改动**：新增 markdown（`a1cc0cc0`）说明 `A@v=λv` 中 λ=0 意味着该方向被压到原点，并把这与"面积变0"关联起来。新增代码（`b12443bc`）构造一个含零特征值的矩阵，用 `aurora.laviz.arrows2d` 分别画出"λ=0 方向被压到原点"和"λ≠0 方向只是被拉伸"两张图；同时打印 `det(A)` 与特征值之积相等，回应老陈"秩/det/特征值因果关系"里"det 是否等于特征值乘积"的疑问。

---

### 4.（老陈）秩、行列式、特征值之间的因果关系；det 是否等于特征值乘积

**位置**：与第3条同一代码单元（`b12443bc`）内，追加了 `det(A)` vs `np.prod(eigvals)` 的打印验证与文字说明，直接呼应"两条充要判据在数值上指向同一个事实"这句课件原文。

---

### 5.（老陈/琳达）SDD 为什么能保证矩阵可逆？是否有严格证明？什么时候用 SDD、什么时候用 det？

**位置**：紧接第3、4条之后（`b12443bc` 之后），即"三条判据"介绍段落的收尾处。

**改动**：新增 markdown（`0260ba7a`），先给"接力队"类比建立直觉，再给出完整的反证法证明（取 `Ax=0` 非零解中绝对值最大的分量所在行，用三角不等式推出与 SDD 定义矛盾），最后说明 SDD（充分非必要、O(n²)快速判）与 det/特征值（充要、O(n³)）各自的使用场景。

---

### 6.（老陈/小雨）什么是 SVD？σ（奇异值）是什么？为什么 `Vt[-1]` 就是零空间向量？

**位置**：`cell-l18-0168`（"代码实验"引言）之后、`cell-l18-0169`（SVD 零空间演示代码）之前。

**改动**：新增 markdown（`8c5dc6a0`）用"转-伸缩-再转"的类比讲 `A=UΣVᵀ` 的几何意义，解释 σ=0 为什么对应"被压成0的方向"，以及为什么最小奇异值排在 `s` 数组最后、对应 `Vt` 最后一行。

---

### 7.（琳达）满秩矩阵的 `Vt[-1]` 是不是也会被误当成"零空间"？表述容易误解

**位置**：`cell-l18-0169`（既有 SVD 演示代码）之后、`L18-null-space-exercise-md`（练习说明）之前。

**改动**：第6条的 markdown 中新增"⚠️ 重要澄清"段落，明确"零空间只在秩亏矩阵才真实存在，满秩矩阵零空间只有零向量"。并新增代码单元（`6660952a`）用满秩矩阵 `A_full`（对角矩阵，奇异值 2/3/4）与秩亏矩阵 `A_deficient` 对比最小奇异值的量级差异，直观区分"真零空间"和"只是压得最扁的方向"。

---

### 8.（老陈/小雨）残差是什么？Jacobi 迭代更新公式如何从 `Ax=b` 推导？

**位置**：`cell-l18-0172`（Jacobi 参数实验说明）之后、`cell-l18-0173`（Jacobi 函数实现）之前。

**改动**：新增 markdown（`e9087ecc`），先解释"为什么要迭代 + 残差 ‖Ax_k−b‖ 衡量什么"（回应小雨），再从 `Ax=b` 第 i 行展开、分离对角项、两边除以 `A[i,i]`，逐步推出 Jacobi 更新公式（回应老陈/琳达）。

---

### 9.（琳达）`D_inv * (b - (A@x - np.diag(np.diag(A))@x))` 这行代码看不懂，`(A-D)@x` 是否等价、有无性能差异

**位置**：同第8条的 markdown 单元（`e9087ecc`）内，逐词拆解这一行代码，说明其与 `(A-D)@x` 数学等价、指出 `D` 可以挪到循环外预计算的小优化点（不影响正确性）。并新增代码单元（`2c13c78c`）用 2×2 小例子手算一轮，与向量化写法的结果 `assert np.allclose` 对比，验证公式与代码严格一致。

---

### 10.（琳达）谱半径没有定义，为什么 SDD 能保证谱半径 < 1

**位置**：`b1654259`（Aurora 连接段落，原文首次提到"谱半径"）。

**改动**：在该 markdown 单元原文后追加一段（未删除任何原文），给出谱半径 `ρ(A)=max|λᵢ|` 的定义、它如何决定迭代误差是否收敛，并把结论与第5条的 SDD 证明关联起来。

---

### 11.（老陈）DFT 矩阵为何因"列正交"而可逆？什么是酉矩阵？

**位置**：`cell-l18-0174`（本课收束）之后、`a734406e`（DFT 可逆性验证代码）之前。

**改动**：新增 markdown（`bae0521e`），从更熟悉的"正交矩阵"引入"酉矩阵"（`Aᴴ·A=I`，正交矩阵的复数版本），解释"列两两垂直 ⇒ 线性无关 ⇒ 满秩 ⇒ 可逆"的链条，并说明 `ifft(fft(x))≈x` 只是抽查一个信号，更直接的证据是检查 DFT 矩阵本身的正交性。新增代码单元（`c6f4581a`，插入在 `a734406e` 之后）手动构造 4×4 DFT 矩阵，验证 `Wᴴ@W≈I` 与 `|det(W)|≈1`。

---

## 校验

- `nbformat.read(..., as_version=4)` 通过，JSON 结构合法。
- `jupyter nbconvert --execute` 全量执行：除原有的、按设计需要学生手填的自评单元格（`038a96ad`，未填写会 `assert` 报错，这是既有行为、非本次改动引入）外，其余全部单元格（含全部新增单元格）执行无误，包括新增的 matplotlib 可视化、SVD 对比、Jacobi 手算验证、DFT 酉矩阵验证等代码。

---

## 第二轮修订（复核后仍未解决的 3 条问题）

上一轮复核（`verify_r1.json`）指出以下三条问题仍未真正解决——第一轮的相关内容只是"贴近了话题"，没有直接给出学生要的答案。本轮逐条补上。

### 12.（小雨）行列式符号的含义

**原问题**（小雨日志）：*"行列式的符号表示什么？需要一个具体的例子和图。"* 第一轮只讲了 `|det(A)|`（面积大小），完全没碰符号本身。

**位置**：`ddbb9447`（判据1"压扁"说明，原内容保留）末尾追加新段落；`dfae3d22`（原压扁演示代码，保留不变）之后插入新代码单元（新 id `8baec138`）。

**改动**：在 `ddbb9447` 追加"行列式的符号又是什么意思？"一节，用"照镜子，右手变左手"类比引入"方向翻转/镜像"的概念，把符号和"绕单位正方形顶点一圈是顺时针还是逆时针"绑定；给出 `det([[2,0],[0,3]])=6>0`（绕行不变）与 `det([[1,0],[0,-1]])=-1<0`（绕行反转）两个手算对比例。新增代码单元 `8baec138`：实现鞋带公式 `signed_area()` 计算变换后正方形的有符号面积，标出顶点编号 0→1→2→3，用两张子图分别画出"纯拉伸（det>0，仍逆时针）"和"镜像翻转（det<0，变顺时针）"，并打印验证符号与绕行方向一致。

### 13.（老陈）秩、行列式、特征值三条判据的等价性链式推导

**原问题**（老陈日志）：*"这个对应关系为什么成立？是定义呢，还是能从其他角度推导？比如'det是所有特征值的乘积'这样的关键事实在课程里出现过吗？"* 第一轮只在 `b12443bc` 里加了一行 `det(A)` vs 特征值之积的数值验证，没有把"秩"也纳入同一条逻辑链，没有解释"为什么"三者互相推出对方。

**位置**：`cell-l18-0167`（秩演示代码，原内容保留）之后，插入新 markdown 单元（新 id `492a061d`）与新代码单元（新 id `464eec7d`）。

**改动**：新 markdown 用"体检测三项指标，其实是同一个病灶"类比，给出完整三段式推导：① 把"列线性相关"的定义方程直接写成 `A@x=0`（x 就是相关关系里的系数向量），得到"秩<n ⇔ 存在非零x使Ax=0"；② 把 `λ=0` 代入特征值定义 `Av=λv` 得到 `Av=0`，说明这和①是同一件事的两种说法；③ 用"det=特征值乘积"这一已验证事实，说明"某特征值=0"和"det=0"互为充要。三步串成一条链：`秩<n ⇔ 存在非零x使Ax=0 ⇔ 0是特征值 ⇔ det=0`。新代码单元在 `[[1,2,3],[4,5,6],[7,8,9]]`（课程中反复出现的"秩2"矩阵）上，把秩、特征值、det、以及 `Ax≈0` 的非零解 x（取 λ≈0 对应的特征向量）一次性算出来并交叉验证，让学生看到四句话在同一个矩阵上同时成立。

### 14.（琳达）SDD 为什么保证谱半径 < 1

**原问题**（琳达日志）：*"这段解释缺失了"*——第一轮虽然在 `b1654259` 里补了谱半径的定义，但结论部分明确写"证明超出本课范围，只需记住结论"，等于没有回答"为什么"。

**位置**：`b1654259`（谱半径定义段落，原内容保留）末尾追加证明；其后插入新代码单元（新 id `c95b23ad`）。

**改动**：在 `b1654259` 追加完整证明，明确指出这是"证明 SDD⇒可逆"（`0260ba7a`）同一个"抓最大分量+三角不等式"技巧的推广，不是新招数：写出 Jacobi 迭代矩阵 `T = -D⁻¹(A-D)`（`T[i,i]=0`，`T[i,j]=-A[i,j]/A[i,i]`），设 `λ` 是 `T` 的特征值、`v` 是特征向量，取 `v` 中绝对值最大的分量 `v_k`，对 `Tv=λv` 的第 k 行取绝对值、套三角不等式、除以 `|v_k|`，得到 `|λ| ≤ Σ_{j≠k}|A[k,j]|/|A[k,k]| < 1`（最后一步正是 SDD 定义）。因为这对任意特征值都成立，谱半径 `ρ(T)=max|λ|<1` 随之成立。新代码单元在 `A=[[10,1,1],[1,10,1],[1,1,10]]` 上显式构造 `T`，打印其特征值与谱半径，`assert rho < 1` 验证证明的结论。

## 第二轮校验

- `nbformat.read(..., as_version=4)` 通过，JSON 结构合法（46 个单元格，较第一轮增加 4 个）。
- `jupyter nbconvert --execute --allow-errors` 全量执行：除同一个既有的自评单元格（`038a96ad`，未填写会按设计 `assert` 报错）外，其余全部单元格（含本轮新增的 `8baec138`／`492a061d`／`464eec7d`／`c95b23ad`）执行无误，数值输出与手工推导一致（例如 `ρ(T)=0.2000 < 1`，秩2矩阵链式验证 `det≈-9.52e-16`）。matplotlib 图中中文标题出现字体缺字警告（DejaVu Sans 缺 CJK glyph），这是既有环境限制，非本次改动引入，不影响代码正确性或单元格执行。
