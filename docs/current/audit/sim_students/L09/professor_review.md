# L09 向量 — 教授审阅与修订记录

审阅对象：`notebooks/2_linear_algebra/L09_vectors.ipynb`
来源：老陈、琳达、小雨三位学生的学习日志（`laochen.md` / `linda.md` / `xiaoyu.md`）

修订原则：只增不删，原文全部保留；在学生卡住的位置附近插入新 markdown/code cell，
科普风格（类比先行、公式殿后），不使用"显然/易得/不难看出"。

下面按学生问题逐条列出改动位置与内容。

---

### 1. 【老陈 Q1 / 琳达 Q1】向量既是"箭头"又是"数组"，"点"和"箭头"两种说法怎么切换？

**位置**：`## 1. 向量就是 numpy 数组` 之后（cell id `14222414`，本次会话开始前已插入）。

**处理**：新增 markdown cell「先别急着抽象：箭头是怎么变成数组的？」，用"从家往东3格往北4格"类比说明箭头的全部信息就是一组数字；专门用一段解释"点"与"箭头"是同一对象的两种叙述视角，工程上不必纠结用哪个词；并说明维度超过 3 后向量运算仍是"逐分量操作"，不依赖能否画图。

（此项在本轮之前已完成，本次审阅确认已覆盖两位学生的问题，未重复修改。）

---

### 2. 【老陈 Q2 / 琳达 Q3 / 小雨 Q1】为什么任意向量都能由 `e1, e2` 线性组合表示？基向量的独立性是什么、能否换别的向量当基？

**位置**：`## 代码实验：任意向量都是基向量的线性组合` 的演示代码之后（新增 markdown cell `47086519` + code cell `8f23fde3`，插入在原 code cell `cell-l09-0056` 之后）。

**处理**：
- 用"指路"类比引出"线性无关""基"的定义：两个方向互相独立才能当基。
- 解释 2D 恰好需要 2 个基向量是"维度"一词的定义方式，不是巧合。
- 用可运行代码演示换一组不平行的基 `f1=[1,1], f2=[1,-1]` 依然能表示目标向量（`np.linalg.solve` 求系数），并演示两个平行向量 `g1=[1,1], g2=[2,2]`（线性相关）无法作为基，触发 `LinAlgError`。

---

### 3. 【老陈 Q3 / 小雨 Q4】`‖v‖` 符号和 "norm" 一词的由来，L1/L2 范数是什么关系

**位置**：`## 动手观察：shape 和 norm` 的演示代码之后（新增 markdown cell `e84653c0` + code cell `0bcdf5bd`，插入在原 code cell `cell-l09-0054` 之后）。

**处理**：
- 用"操场向东3米向北4米"的勾股定理类比，说明 `‖v‖=√(v₁²+v₂²)` 就是斜边公式的改写，不是凭空规定。
- 解释双竖线符号是为了和标量的绝对值 `|x|` 区分；"norm"一词源自拉丁语 *norma*（尺子/标准）。
- 引出 L1（曼哈顿距离）与 L2（欧式距离）的区别，用"出租车不能抄对角线"类比 L1。
- 新增代码用 `np.linalg.norm(v, ord=1)` 对比 L1/L2 的实际数值。

---

### 4. 【琳达 Q2】为什么长度公式是 √(v₁²+v₂²)，而不是绝对值相加？负分量为什么不影响？

**位置**：同上第 3 条的新增 markdown cell `e84653c0`（与老陈 Q3、小雨 Q4 合并处理，因为三人问的是同一个"长度公式从哪来"的问题簇）。

**处理**：解释平方运算天然抹掉负号，因此分量为负也不影响长度；同时把"为什么不用绝对值"直接对应到 L1 范数的介绍，说明两者是两种合法但用途不同的"尺子"。

---

### 5. 【老陈 Q4 / 小雨 Q6（高维方向部分）】高维向量的"方向""反转"是否还有意义？

**位置**：`## 3. ✏️ 你的任务：实现 scale` 的推理路线之后（新增 markdown cell `0b2ca55e` + code cell `131a0b1f`，插入在原 markdown cell `b1b4933e` 之后）。

**处理**：
- 承认 2D 的"180度转向"图像在 13 维消失，但把"方向反转"重新定义为两条可验证的代数性质：大小不变（‖v‖=‖-v‖）与首尾相消（v+(-v)=0）。
- 说明这两条性质与维度无关，高维只是丢了"能画图"这一层，代数结构原样保留。
- 预告下节课点积会给出高维"夹角"的可计算定义。
- 新增代码对一个模拟的 13 维 MFCC 向量验证上述两条性质。

（小雨 Q6 关于"高维向量加法/缩放非可视化直觉"的部分，已由上一轮插入的 cell `14222414` 中"维度一高，别指望画图"一段覆盖，本条新增内容补上"方向"这一子问题。）

---

### 6. 【老陈 Q5】`matrix[i]` 与 `matrix[i:i+1]`、`matrix[i,:]` 的 shape 陷阱

**位置**：`**🔗 Aurora 连接**` 段落之后（新增 markdown cell `dd2a9c18` + code cell `6583095a`，插入在原 markdown cell `aaa6a9c9` 之后）。

**处理**：
- 用"点名册"类比：整数索引拿到"学生本人"（降一维），范围切片拿到"只有一行的名册"（保留二维）。
- 给出规则：方括号写整数 → 降维；写切片 → 保留维度；`mfcc[i,:]` 等价于 `mfcc[i]`。
- 新增代码用一个简化的 3×3 矩阵对比 `mfcc_like[1]` / `mfcc_like[1:2]` / `mfcc_like[1, :]` 三种写法的 shape 差异。

---

### 7. 【琳达 Q4 / 小雨 Q2】矩阵 `A @ v` 对向量的作用具体是什么、规则是什么？

**位置**：`## 🎨 图示：向量 = 带方向的箭头，缩放 = 改变长度` 中 `arrows2d` 演示代码之后、矩阵探针代码之前（新增 markdown cell `5ca22c16` + code cell `aed7b492`，插入在原 code cell `4a525441` 之后）。

**处理**：
- 承认完整理论留到 L12，但给出一个衔接性直觉：矩阵的每一列就是"基向量被搬到哪里"。
- 推导 `A @ v = v₁·(A的第一列) + v₂·(A的第二列)`，把矩阵作用与刚学的线性组合直接挂钩，而不是让学生凭空记忆"矩阵能拉伸/旋转"这句结论。
- 新增代码验证 `A @ v` 与"用 A 的两列做线性组合"数值相等。

---

### 8. 【琳达 Q5】线性组合的几何意义/实际用途是什么？

**位置**：`## ✏️ 练习 B：实现 linear_combination` 代码之后（新增 markdown cell `c59cf12f` + code cell `a5cb185a`，插入在原 code cell `aurora-ex-lincomb-code` 之后）。

**处理**：
- 用调音台均衡器（EQ）类比：每个频段的标准波形是"基向量"，推子位置是"系数"，混合输出就是线性组合。
- 点出"化繁为简、按权重叠加"是信号处理和机器学习的共享思路（呼应后续傅里叶分解、MFCC 特征基）。
- 新增代码：用线性组合实现"男声调到 0.5 倍音量、女声调到 1.5 倍音量后混合"的具体例子（依赖学生已实现的 `linear_combination`，用 try/except 兼容未完成的情况）。

---

### 9. 【小雨 Q3】负数缩放的几何含义，需要一张对比图

**位置**：`## 4. 几何：向量是带方向的箭头` 的 `[3,4]` 箭头图之后（新增 markdown cell `3c4429a2` + code cell `01942332`，插入在原 code cell `74cfcfb8` 之后）。

**处理**：新增 `v=[3,4]`（蓝）与 `-v=[-3,-4]`（红）同图对比的 `quiver` 图，直接展示"方向相反、长度相同"的 180 度翻转效果。

---

### 10. 【小雨 Q5】为什么用 `np.allclose` 而不是 `==`？浮点误差是怎么回事？

**位置**：`scale()` 函数定义之后、第一次出现 `np.allclose` 的验证代码之前（新增 markdown cell `359614d3` + code cell `85009675`，插入在原 code cell `463a40e9` 之后）。

**处理**：
- 用"只能读到毫米的尺子"类比浮点数的有限精度。
- 举出 `0.1 + 0.2 != 0.3` 的经典例子。
- 解释 `atol`（绝对容差）的含义，说明这是数值计算的标准做法而非偷懒。
- 新增代码直接验证 `0.1+0.2 == 0.3` 为 `False`，而 `np.allclose(0.1+0.2, 0.3)` 为 `True`。

---

## 验证

- `python3 -c "import nbformat; nbformat.read(...)"` 通过，notebook JSON 合法。
- `python scripts/validate_pipeline.py --json --syntax` 通过（99 个 notebook 全部合法）。
- 手工执行全部 code cell（以参考实现填入三个练习函数）：除预期需要人工填写的"本课自评"格外，全部无报错、输出符合预期。
