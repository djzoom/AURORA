# L11 · 范数 —— 教授修订说明

根据 `laochen.md`、`linda.md`、`xiaoyu.md` 三位学生的学习日志，对
`notebooks/2_linear_algebra/L11_norms.ipynb` 做了增量修订：**只增加、不删减**，
在每位学生卡住的位置附近插入铺垫、类比、推导或可运行的验证代码。全部新增代码
已实测可运行（含 `normalize` 尚未实现时的预期报错路径）。

以下按"学生 → 问题 → 改动位置与内容"逐条列出。

---

## 老陈（laochen.md）

1. **向量距离的几何含义**（为什么 `dist(a,b)=‖a-b‖`）
   - 位置：cell `e14bd0e2`（"两个向量的距离"标题）之后、原距离代码 `94ff1469` 之后。
   - 改动：插入 markdown 用 A=(1,2)、B=(4,6) 的地图类比，分两步讲清"先求位移向量 b-a，再量它的长度"的逻辑；并在原代码后插入 `arrows2d` 图示，画出 a、b、b-a 三个箭头。

2. **L∞ 范数名字"切比雪夫"的来历**
   - 位置：section 1 表格（`eade0ab1`）之后。
   - 改动：插入 markdown 介绍数学家 Chebyshev 与"最坏情况误差界"的关联，以及"棋盘距离"别名的由来。

3. **L1/L2 正则化梯度的推导过程**
   - 位置：section 4 "正则化含义"（`a1b2c3d4`）之后、原梯度对比代码之前。
   - 改动：插入"推导（可选）"markdown，逐步展开 `d/dw(w²)=2w`（L2）和绝对值函数在 `w=0` 处的次梯度约定 `sign(0)=0`（L1）。

4. **梯度下降中 L1 为什么能精确到 0**
   - 位置：紧接上一条推导之后，原多权重对比代码之前。
   - 改动：新增代码格，用 `w=0.3, lr=0.1, λ=0.1` 精确模拟 10 步梯度下降，并解析计算出"共需 30 步精确归零、之后停在 0 不变负"。

5. **通用范数公式中的 p 值，及 `np.linalg.norm(v, p)` 的关联**
   - 位置：Lp 单位球代码 `cell-l11-0078` 之前。
   - 改动：插入 markdown 给出 `‖v‖ₚ=(Σ|vᵢ|^p)^(1/p)`，逐一代入 p=1、2、∞ 推出 L1/L2/L∞，并解释为何单位球对 normalize 有意义。

6. **零向量返回全零的处理约定**
   - 位置：`normalize` 任务说明 `c7bce6f0` 原文之后（追加，未删原文）。
   - 改动：补充 MFCC 静音帧场景、"抛异常 vs 返回全零"的工程权衡、NaN 传播风险。

7. **"MSE 损失 = 误差向量 L2 范数平方"的对应关系**
   - 位置：本课收束 `cell-l11-0080` 之后。
   - 改动：并排写出 MSE 与 `‖e‖₂²` 公式，标出 `MSE=‖e‖₂²/n`，并加代码数值验证。

8. **"归一化后余弦相似度 = dot(a,b)"**
   - 位置：🔗 Aurora 连接 `2e721b13` 之后。
   - 改动：推导 `â·b̂=cos(θ)` 的代数恒等式，并用他指定的 `a=[3,4], b=[6,8]` 加一组非平行向量做数值验证。

---

## 琳达（linda.md）

1. **L∞ 范数的几何含义 / 为什么 v=[1,100] 时 L∞ 反而"最小"**
   - 位置：section 1 表格（`eade0ab1`）之后。
   - 改动：与老陈问题 2 合并处理，同一 markdown 中补充"三把尺子各自的定义，不是在比赛谁是真实最短路径"的澄清，并给出衣服尺寸检测的日常类比。

2. **为什么 L∞ ≤ L2 ≤ L1 总是成立**
   - 位置：三角不等式代码 `cell-l11-0074` 之后。
   - 改动：插入两步严格推导（`L2²≥M²` 故 `L2≥L∞`；`L1²≥L2²` 故 `L1≥L2`），随后新增 matplotlib 代码画出 L1（菱形）/L2（圆）/L∞（正方形）三种单位球的嵌套关系图。

3. **normalize 为什么不能用 `v/sum(v)` 或 `v/max(v)`**
   - 位置：`normalize` 推理路线 `c7bce6f0` 之后。
   - 改动：新增对比代码，对 `v=[3,4]` 分别除以 sum、max、L2 范数，打印各自结果的 L2 范数，直观看出只有除以 L2 范数才等于 1。

4. **L1 vs L2 正则化梯度公式怎么来的（超出高中范围的顾虑）**
   - 位置：与老陈问题 3、4 合并处理；额外在梯度公式前插入"梯度铺垫"markdown（`cell-l11-0079` 之后），用"山坡走一步"的类比说明梯度是什么，并注明推导部分可跳过、结论可直接当实验事实使用。

5. **零向量为何在 MFCC 中出现，为什么不直接抛异常**
   - 位置：与老陈问题 6 合并，在 `c7bce6f0` 追加的说明中给出静音帧场景与 NaN 传播风险。

6. **下一课"矩阵列的 L2 范数决定拉伸倍数"是什么意思**
   - 位置：本课收束 `cell-l11-0080`（追加，未删原文）。
   - 改动：追加一句直觉预告——矩阵乘法把输入沿各列方向摊开叠加，列的 L2 范数越大、这个方向的拉伸就越大，完整展开留给 L12。

---

## 小雨（xiaoyu.md）

1. **符号 `‖v‖` 和高中学过的 `|v|` 区别未讲清**
   - 位置："符号入口" `cell-l11-0072` 之前。
   - 改动：插入 markdown 说明 `| |` 是给数字用的绝对值，`‖ ‖` 是给向量/矩阵用的范数，靠角标区分具体定义。

2. **`‖v/c‖=‖v‖/c`（齐次性）没有证明，只有例子验证**
   - 位置：`normalize` 推理路线 `c7bce6f0` 之后，紧跟 sum/max 对比代码。
   - 改动：按她本人给出的推导思路，插入完整的齐次性证明 markdown（`√(1/c²·X)=(1/c)√X`），并代入 `c=‖v‖₂` 得出结论。

3. **零向量为什么返回全零而不是报错**
   - 位置：与老陈问题 6、琳达问题 5 合并，`c7bce6f0` 追加说明中处理。

4. **第 4 节"梯度"跳跃太陡，缺少"梯度是什么"的铺垫**
   - 位置：与琳达问题 4 合并，"梯度铺垫" markdown（`cell-l11-0079` 之后）用"山坡"类比讲清梯度和梯度下降公式，再进入原有的 L1/L2 梯度对比。

5. **L∞ 的"国王走法"类比不够直观**
   - 位置：与老陈问题 2、琳达问题 1 合并，在切比雪夫说明中追加"衣服尺寸质检"日常类比（她本人建议的场景）。

---

## 校验

- `python3 -c "import nbformat; nbformat.read(..., as_version=4)"` 通过。
- 逐格 `exec` 全部代码 cell（`normalize` 未实现前的预期 `NotImplementedError` 分支、以及自评格的预期 `AssertionError` 属于原有设计，非新增代码引入的问题），其余新增代码格全部正确运行并输出预期结果。
