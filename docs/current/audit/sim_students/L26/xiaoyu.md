我现在认真学习这个 notebook。让我逐个 cell 理解...

## 我的学习过程

### 第一部分：数值化斜率（中心差分）
读到"用中心差分 `(f(x+h)−f(x−h))/(2h)` 在五个点上算 `f(x) = x²` 的近似斜率"时，我能心算验证：
- 在 `x=1` 时，理论斜率应该是 `2×1=2`，公式给出的也是约 `2.000`
- 这个数值方法很直白，就是算函数在左右两边的变化

### 第二部分：切线、一维下降、二维等高线
- **切线（tangent）**：就是在某一点贴着曲线的一条直线，斜率等于那点的导数。这个我能理解。
- **一维梯度下降**：从一个点出发，沿着负斜率方向（下坡）每次走一小步，最后走到谷底。很像在山上下山。
- **二维梯度下降**：垂直等高线走，每一步都往低处移动。图的逻辑很清楚。

### 第三部分：学习率的影响
根据注释理解：lr 太小走得慢，lr 适中就稳定收敛，lr 太大在谷底左右弹跳。

### 最后的数学验证代码
代码检验了中心差分精度、二维梯度、梯度下降收敛、以及 lr 过大导致发散的例子。我没实际跑，但按逻辑都说得通。

---

# 小雨 — L26 学习日志

## 我理解了什么
- 导数就是函数在某点的切线斜率，用中心差分可以数值化计算
- 梯度下降就是沿着负梯度方向（下坡最陡的方向）一步步走到谷底
- 学习率大小直接影响收敛速度和稳定性：太大会振荡，太小会很慢

## 遇到的困难和问题

### 问题 1【理解】中心差分公式的几何/数值原理

**卡在哪里**：课程直接给公式 `(f(x+h) - f(x-h))/(2h)` 但没有解释为什么是这个形式。

**具体困惑**：
- 为什么不用更简单的 `(f(x+h) - f(x))/h` 而要用中心差分？
- 这两个有什么区别？为什么中心差分的近似会更准确？
- "中心"是指对称地用 x 两边吗？

**希望补充**：能否说明中心差分相比单向差分的优势（精度为什么更高），以及 h 和精度的关系（为什么 h=1e-5 时误差能到 1e-8）。

---

### 问题 2【理解】梯度为什么指向最陡上升方向，反方向就能下降

**卡在哪里**：课程说"梯度把这个方向推广到多维"，然后说"沿梯度反方向一步步走向最低点"，但没有解释为什么。

**具体困惑**：
- 一维时，斜率是正数就说明向右函数值增大、是负数就增大...好，这个懂。
- 二维时，梯度是一个向量 `(2x, 4y)`，这个向量指向的"最陡上升方向"是什么意思？为什么？
- 梯度的方向是怎么定义的？

**希望补充**：用具体例子（比如 `f(x,y)=x²+2y²` 在点 `(1,2)` 处）解释梯度向量 `(2,8)` 的几何含义，为什么这个向量的反方向就是下降最快的方向。

---

### 问题 3【理解】二维梯度怎么从函数式算出来

**卡在哪里**：代码给出 `f(x,y)=x²+2y²` 的梯度是 `∇f=(2x, 4y)`，但没有说怎么推导。

**具体困惑**：
- `2x` 是对 x 求偏导？"偏导"是什么？和普通导数有什么区别？
- 为什么对 y 求偏导结果是 `4y` 而不是 `2y`？是因为 y 的系数是 2 吗？
- 梯度向量的两个分量 `(2x, 4y)` 分别代表什么？

**希望补充**：简单解释偏导数（对其中一个变量求导，把另一个当常数），然后用一两个例子演示怎么从 `f(x,y)` 推出 `∇f`。

---

### 问题 4【理解】凸函数和鞍点对梯度下降的影响

**卡在哪里**：代码注释说"`f(x,y)=x²+2y²` 是凸函数，(0,0) 是全局极小值"，又说"`f(x,y)=x²−y²` 有鞍点"。

**具体困惑**：
- 什么叫"凸函数"？为什么 `x²+2y²` 是凸的？怎么判断？
- 什么是"鞍点"？"在某方向上升、另一方向下降"具体是什么意思？
- 这对梯度下降有什么影响？如果遇到非凸函数或鞍点，梯度下降会失败吗？

**希望补充**：给凸函数和鞍点一个直观的定义或画个示意图，说明凸保证了梯度下降收敛，非凸会遇到什么麻烦。

---

### 问题 5【实践】练习题的精度要求和 h 的选择策略

**卡在哪里**：练习题要求"误差小于 `1e-6`"，代码默认 `h=1e-5`，但没说这是怎么选的。

**具体困惑**：
- `1e-6` 和 `1e-5` 是什么关系？为什么这个 h 能达到这个误差？
- 如果精度要求更高（比如 `1e-9`），h 应该怎么改？
- h 太小会有什么问题吗？（浮点数精度？）

**希望补充**：补充一小段关于数值精度的陷阱：h 并不是越小越好，因为浮点数精度有限，过小的 h 会引入舍入误差。最优的 h 往往是一个平衡点。
