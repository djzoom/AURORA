# L30 课程修订说明 · 教授审视

**修订日期**：2026-07-07  
**学生反馈汇总**：3 位学生（老陈、琳达、小雨）  
**修订策略**：原有内容全部保留，通过新增 markdown cell、补充详细推导和生活化类比来解决卡点

---

## 修订内容清单

### 1. 【理解】为什么交叉熵只看真实类别 + One-hot 编码定义
**学生问题来源**：
- 老陈：logits 部分的"softmax 的归一化已经顺带把它们压低了"逻辑跳跃
- 小雨：one-hot 编码在梯度推导中突然出现，没有提前定义

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `cf868796` 之后）：
  - 标题：🔍 为什么交叉熵只看真实类别那一项？— 极大似然的视角
  - 内容：从概率论角度解释极大似然，说明 softmax 约束自动压低其他类别
  - 内容：📌 One-hot 编码定义、为什么这样设计、代码示例

### 2. 【理解】KL 散度定义、H(p,q) = H(p) + D_KL(p||q) 推导
**学生问题来源**：
- 老陈：D_KL 没有给定义式、分解公式如何推导、H(p) 为什么是常数
- 琳达：等式"直接给了结论，没有推导"
- 小雨："掉进了信息论的黑箱"

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `c20797c8` 之前）：
  - 标题：📊 KL 散度：两个概率分布的"距离"
  - 内容：KL 散度的定义公式、三条关键性质、与交叉熵的关系式推导（H(p,q) = H(p) + D_KL）
  - 内容：为什么 H(p) 是常数、为什么最小化交叉熵等价于最小化 KL 散度

### 3. 【理解】Shannon 熵为什么这样设计 + 约定 0·log 0 = 0 的数学原因
**学生问题来源**：
- 琳达：为什么是负号、为什么这个公式能衡量不确定性
- 小雨：为什么要用对数来度量、生活化类比缺失
- 老陈：约定 0·log 0 = 0 的数学原因

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `063e742c` 之前）：
  - 标题：💡 为什么 Shannon 熵是这个样子？— 信息论的故事
  - 内容：掷骰子/二分法问题类比，引入"平均需要多少个二分法问题"的生活化视角
  - 内容：Shannon 熵公式的来源、为什么是 `-log p_i`、为什么是期望值
  - 内容：极限 lim(p→0+) p log(p) = 0 的数学证明、代码中跳过零项的合理性

- **修改代码 cell `063e742c`**（Shannon 熵实现）：
  - 添加详细的 docstring，解释参数、关键步骤、对数换底公式
  - 新增注释：np.log vs np.log2 的区别、对数换底公式的应用

### 4. 【推演】梯度推导的详细展开：对数求导、链式法则、分情况讨论
**学生问题来源**：
- 老陈：对数除法公式没有显式写出、链式法则的具体形式没有展开
- 琳达：涉及多个变量的偏导数和链式法则
- 小雨：推导过程步子太长、对数求导和链式法则的具体应用不明确

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `fbd3e3ab` 之前）：
  - 标题：📐 详细推导：d/dz_i log(Σ_j exp(z_j))
  - 内容：分 4 个步骤逐步展开梯度推导
    1. 交叉熵的原始形式
    2. 对数的除法规则：log(a/b) = log(a) - log(b)
    3. 对 logit z_i 求偏导，详细展开链式法则（"外层求导×内层求导"）
    4. 指示函数形式转化为 one-hot 向量形式
  - 内容：最终公式 dL/dz_i = p_i - y_i 的直觉解释

### 5. 【实践】Numpy 函数说明：np.asarray、np.log vs np.log2、对数换底
**学生问题来源**：
- 老陈：np.asarray dtype=float 的作用、何时用 log vs log2、对数换底公式在代码中的应用
- 琳达：两种对数是否影响模型训练结果

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `ade60bbd` 之前）：
  - 标题：🔧 Numpy 快速参考：这里会用到的函数
  - 内容：np.asarray(z, dtype=float) — 为什么需要、例子
  - 内容：np.log vs np.log2 — 区别、在本课中的用法

### 6. 【理解】softmax 为什么自动压低错误类别 — 指数函数的性质
**学生问题来源**：
- 小雨：logits 分数低的类别概率怎么被压低、exp() 函数的性质、具体对比例子

**改动内容与位置**：
- **新增 markdown cell**（插入在原 cell `b25194b7` 之后）：
  - 标题：💭 为什么 softmax 的"分数饼"会自动压低错误类别？— 指数函数的性质
  - 内容：exp() 把差异放大的性质
  - 内容：具体对比表格：[5.0, 1.0, 0.1] 用简单除法 vs softmax 的结果
  - 内容：数学直觉：分子 e^z_i 指数增长 → 分母约束 → 其他类别概率下降

---

## 学生问题覆盖率

| 问题类型 | 涉及学生 | 改动位置 | 完成度 |
|---------|---------|---------|--------|
| KL 散度定义 | 老陈、琳达、小雨 | 新增 cell §2 | ✅ |
| H(p,q) 推导 | 老陈、琳达、小雨 | 新增 cell §2 | ✅ |
| 为什么只看真实类别 | 老陈、小雨 | 新增 cell §1 | ✅ |
| One-hot 编码定义 | 小雨 | 新增 cell §1 | ✅ |
| Shannon 熵为什么设计成这样 | 琳达、小雨 | 新增 cell §3 | ✅ |
| 0·log 0 = 0 的数学原因 | 老陈 | 新增 cell §3 | ✅ |
| 梯度推导详细步骤 | 老陈、琳达、小雨 | 新增 cell §4 | ✅ |
| 链式法则展开 | 老陈、琳达、小雨 | 新增 cell §4 | ✅ |
| Numpy 函数说明 | 老陈 | 新增 cell §5 | ✅ |
| log vs log2 区别 | 老陈、琳达 | 新增 cell §5 | ✅ |
| 对数换底公式应用 | 老陈 | 代码注释 §3 | ✅ |
| softmax 压低错误类别的机制 | 小雨 | 新增 cell §6 | ✅ |

---

## 修订原则遵守情况

- ✅ 只增加、不删减：原有内容全部保留，所有插入都是新增 cell 或扩充注释
- ✅ 科普风写作：每个新增 section 都先问题/类比/故事，再公式；避免"显然""易得"
- ✅ 针对性：每条学生问题都有对应改动，改在卡点位置附近
- ✅ NotebookEdit 工具编辑：12 处编辑（插入 9 个新 markdown cell + 修改 1 个代码 cell 注释 + 原有内容无改）
- ✅ 代码可运行性：新增所有代码 cell 都依赖前面已定义的变量，无依赖循环
- ✅ Notebook 合法性验证：通过 nbformat 检查，无格式错误

