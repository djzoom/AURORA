处理了第 2 轮复核指出的全部 4 条遗留问题：

1. **老陈问题4**（Yes/No 频率选择逻辑）→ 追加到 `d34ee31e`，说明频段是为夸大类间差异而刻意选择，并指向共振峰/声学语音学延伸阅读。
2. **琳达问题5**（Mel 参数选择原则）→ 在 `cell-l65-0547` 后新增 Markdown cell，给出 N_MELS/HOP 的类比解释、经验法则表和 40/160 的选取理由。
3. **琳达问题6**（混淆矩阵列归一化）→ 改写 `cell-l65-0546`，加第三张列归一化（精确率）子图，并补充"何时看行/何时看列"的应用场景说明。
4. **小雨问题2**（Loss 曲线公式物理意义）→ 在 `cell-l65-0540` 后新增 Markdown cell，用下坡类比拆解 `exp(-0.08·epochs)+0.05+noise` 每一项的含义。

已用 `jupyter nbconvert --execute` 全量跑通（自评空白 cell 除外，属预期行为）并用 `nbformat.read` 校验通过；修订说明已追加到 `professor_review.md`。
