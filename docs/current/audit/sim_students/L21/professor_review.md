# L21 教授审阅记录 — 学生问题 → notebook 修订对照

来源：`docs/current/audit/sim_students/L21/{laochen,linda,xiaoyu}.md`（3 位模拟学生的学习日志）。
修订对象：`notebooks/2_linear_algebra/L21_aurora_matrices.ipynb`。
修订原则：只增不删——原有 cell 全部保留，新增 markdown/code cell 插在学生卡住的位置附近；新增代码均已通过 `nbconvert --execute` 验证可运行（唯一提前终止点是原有的、需要学生手填 `True/False` 的自评 cell `a2382f0c`，与本次修订无关，属于既有设计）。

以下按"问题 → 改动"逐条列出。同一类问题被多位学生提出时合并为一条，标注全部提出者。

---

### 1.【老陈 / 琳达 / 小雨】DFT 公式 `W[k,n]=exp(-2πi·kn/N)` 里，为什么是负号？为什么是 2π？`kn/N` 是什么意思？公式从哪来？

**改动位置**：`3f32a0ef`（符号入口）之后、`f1367898`（DFT 矩阵构造说明）之前，新增 markdown cell `49b292ec`。

**改动内容**：用"圆形跑道 + 不同速度的人"类比解释 `kn/N`＝圈数、`2π`＝弧度换算；负号的来源解释为"内积定义要求对基准向量取共轭，而 `e^{+iθ}` 的共轭正是 `e^{-iθ}`"，并点出这是内积操作本身的要求，不是随意约定。IDFT 用正号（对应 `Wᴴ/N`）也在此处埋下伏笔，供后面酉矩阵一节呼应。

---

### 2.【老陈 / 琳达 / 小雨】`exp(-3πi/2)` 怎么算出来等于 `j`？白板挑战跳得太快。

**改动位置**：紧跟第 1 条，新增 code cell `f3deea37`（N=4, k=1 手算验证，把故事变成具体数字）；另在 `0c3a82ff`（"1. DFT 是一个矩阵 W"）之后新增 markdown cell `d3906c9e` + code cell `96da3ce0`。

**改动内容**：`d3906c9e` 给出常用角度（0, π/6, π/4, π/3, π/2, π, 3π/2, 2π）的 cos/sin 速查表，并把白板 Q1 逐步拆成"化简角度 → 换算成等价正角 → 查表 → 代回欧拉公式"四步；`96da3ce0` 用代码把这四步跑一遍并断言结果为 `j`。同时明确点出这是 L06 已经学过的欧拉公式/单位圆知识的复用，而非新知识点。

---

### 3.【老陈 / 琳达 / 小雨】共轭转置 `Wᴴ` 是什么？为什么验证正交性要用共轭而不是普通转置？为什么要除以 N？酉矩阵和正交矩阵是什么关系？这个性质有什么实际用处？

**改动位置**：`0c3a82ff` 之后，新增 markdown cell `2cfb14c7` + code cell `325ec8c5`（顺序在第 2 条两格之后）。

**改动内容**：从"复数的模长需要乘共轭才能得到实数" (`z·z̄=|z|²`) 类比到向量的 `v^H v`，解释共轭转置的物理意义；说明除以 N 是为了把"每行长度 √N"归一化成单位矩阵；给出"正交矩阵（实数，转置）↔ 酉矩阵（复数，共轭转置）"的对应关系；列出两个实际用处（求逆几乎免费 = `Wᴴ/N`、Parseval 能量守恒）。代码格用普通转置 `W@W.T/N` 对照共轭转置版本，展示普通转置版对角线并不全为 1，证明共轭是必需的，不能省。

---

### 4.【老陈】mel 矩阵为什么要用 `N_mels+2` 个端点，而不是 `N_mels` 或 `N_mels+1` 个？

**改动位置**：`6dd94ccf`（简化 mel 滤波器说明）之后、`2dc9f17e`（mel 矩阵构造代码）之前，新增 markdown cell `914c8ea8` + code cell `32ea5d78`。

**改动内容**：用"栅栏柱子 vs 栅栏段"类比解释"共享端点"的几何关系，说明每个滤波器需要三个连续端点（左脚/峰顶/右脚），相邻滤波器错位共享，因此总端点数是 `N_mels+2`；同时简要说明这不是唯一实现方式（也可用边界扩展/镜像补点），但当前写法最直接。代码格用 `N_mels=3` 具体打印出 5 个端点及每个滤波器对应的三元组。

---

### 5.【老陈】课程说"减少滤波器数量会让相邻 bin 合并、重叠程度变化"，但没有给出"重叠程度"的量化定义或可视化方法。

**改动位置**：`111f2e19`（参数实验：改变 n_mels）之后、`5b48f58b`（本课收束）之前，新增 markdown cell `3fcf3116` + code cell `f2cd7b32`。

**改动内容**：把"重叠"定义为"相邻两个滤波器同时非零的 bin 数 / 单个滤波器覆盖的 bin 数"，并用 `overlap_ratio()` 函数对 n_mels=40/80/128 三组实际计算平均重叠比例，把抽象描述变成可复现的量化指标。

---

### 6.【老陈 / 琳达 / 小雨】`np.outer` 为什么能一次性构造出 DFT 矩阵里 `k·n` 那部分？`np.outer` 的数学含义是什么？

**改动位置**：紧跟第 1 条之后，新增 markdown cell `cba7cde1` + code cell `757d088e`（`3f32a0ef` 之后，`f1367898` 之前）。

**改动内容**：把 `np.outer(a,b)` 类比成"乘法表"（竖排 a、横排 b，格子填 `a[i]*b[j]`），用 `np.outer([1,2,3],[1,2,3])` 的具体输出验证 `[1,2]` 位置确实等于 `2*3`；再说明 DFT 矩阵第 `(k,n)` 位置需要的 `k·n` 恰好就是这张乘法表，因此 `np.outer(k_idx, n_idx)` 配合逐元素 `np.exp` 就能一行构造出整张矩阵。

---

### 7.【老陈】Cell-13（简化 DCT 演示）跳过了 Mel 滤波和 log 两步，直接对功率谱做 DCT——这样算出的结果还能和真实 MFCC 比较吗？为什么要跳过？

**改动位置**：`d2745f77`（简化 DCT 演示代码）之后、`cbc3f1ee`（参数实验说明）之前，新增 markdown cell `10a6f843` + code cell `d1204843`。

**改动内容**：写出完整 MFCC 五步管道，明确指出这格代码是"矩阵链乘结构"的教学演示，故意跳过 mel/log 两个非线性步骤，因此其输出**不能**直接与真实 MFCC 数值比较；随后用 Aurora 真正的 `mel_filterbank()` + `log` 计算"更完整版"结果，与"跳步版"并排打印，让学生亲眼看到两者数值范围完全不同。

---

### 8.【老陈 / 琳达】`build_simple_mel_matrix` 练习里，"simple"是什么意思？是线性刻度还是 mel 刻度？练习的验收标准是什么？（琳达同时问及 mel 全零行问题的标准做法）

**改动位置**：直接修改练习 cell `b8b893e0`（原内容全部保留，追加注释说明，未删减任何原有代码/注释）。

**改动内容**：在 `build_dft_matrix` 上方追加一行注释，明确"验收标准就是注释掉的 assert 格，通过即算完成，没有另外的标准答案"；在 `build_simple_mel_matrix` 上方追加大段注释，说明"simple"= 跳过 mel 刻度换算（`hz_to_mel`），只在线性 bin 编号上等距取端点，因此不应与真实 `mel_filterbank()` 做数值对比；并指向"全零行"问题的成因（参考条目 9 的解释），说明这是 n_mels 相对 n_fft 过大导致的正常退化现象，不是代码错误。

---

### 9.【琳达 / 小雨】mel 滤波器构造代码里 `+1e-9` 是干什么的？会不会污染结果？为什么 n_mels=128, n_fft=64 时会出现大量全零滤波器行，这是 bug 吗？

**改动位置**：`2dc9f17e`（mel 矩阵构造代码）之后、`0c3a82ff`（"1. DFT 是一个矩阵 W"）之前，新增 markdown cell `782dad16` + code cell `641896f0`。

**改动内容**：解释 `1e-9` 是防止除以 0 的极小值保护，正常情况下影响可忽略、不会污染结果；再解释全零行的成因——当滤波器数量远多于可用频率 bin 数时，滤波器的"左脚-峰顶-右脚"间距小于 1 个 bin 宽度，导致 `min(上升沿,下降沿)` 在每个 bin 上都为负、被裁剪成 0，整行塌缩——这是"分辨率不够"的必然结果而非 bug。代码格用 Aurora 真正的 `mel_filterbank(n_mels=128, n_fft=64, ...)` 复现该现象并统计全零行数。

---

### 10.【琳达】滤波器为什么必须是三角形？为什么不是高斯或矩形？这跟人耳听觉有什么关系？

**改动位置**：`8c9b830f`（"3. mel 滤波器组也是一个矩阵"）之后、`be196fdf`（真实 mel_filterbank 可视化）之前，新增 markdown cell `59e466fb` + code cell `bd952d63`。

**改动内容**：先解释人耳对频率的对数感知（mel 刻度的动机），再对比矩形（硬切、边界跳变）、高斯（平滑但计算贵、无清晰边界）、三角形（线性插值、计算便宜、边界清晰）三种滤波器形状的工程取舍，说明选三角形是"用最简单形状换取足够好的听觉近似"。代码格具体打印矩形 vs 三角形在同一组 bin 上的权重数值对比。

---

### 11.【琳达】FFT 只是"DFT 矩阵的快速分解"，但没说清楚为什么更快、怎么从 O(N²) 变成 O(N log N)。

**改动位置**：`l21-l37-bridge`（前向锚点：L37 循环版预告）之后、`6dd94ccf`（简化 mel 滤波器）之前，新增 markdown cell `acf2b397` + code cell `c2e8b7cb`。

**改动内容**：用"奇偶下标分治"解释 FFT 蝶形递归的核心思想——长度 N 的 DFT 可以用两个长度 N/2 的 DFT 拼出来，不需要重新做矩阵乘法；说明递归层数是 `log₂N`、每层合并代价 O(N)，因此总复杂度 O(N log N)；代码格具体打印 N=8/64/512/4096 时矩阵版和 FFT 版的运算次数及加速比，把抽象的复杂度概念变成具体数字。

---

## 覆盖小结

三份日志共提出 22 条独立问题点（含重复提问后合并 11 条主题），全部已在上述 11 个改动点中得到回应。改动方式：新增 9 组 markdown+code 说明 cell（共 18 个新 cell）+ 1 处原 cell 内追加注释（`b8b893e0`，未删减原内容）。notebook 已通过 `nbformat.read(..., as_version=4)` 校验，且所有新增代码格已通过 `jupyter nbconvert --execute` 验证可顺利运行（执行在原有的、需学生手动填写的自评 cell `a2382f0c` 处按设计预期终止，与本次修订无关）。
