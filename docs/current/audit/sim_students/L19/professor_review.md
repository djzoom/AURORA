# L19《亲眼看见矩阵搬动整个空间》— 教授修订说明

根据老陈、琳达、小雨三份学习日志，对 `notebooks/2_linear_algebra/L19_visual_multiply.ipynb` 做了一轮"只增不减"的修订：19 条学生问题全部在原位附近插入新 markdown/code cell 予以解答，原有内容全部保留。修订后已用
`python3 -c "import nbformat; nbformat.read(...)"` 校验 JSON 合法，并用
`jupyter nbconvert --execute --allow-errors` 全量跑通——除原有「本课自评」cell（按设计要求学生手动填 True/False，断言本就应该失败）外，无任何执行错误。

下面逐条列出「学生问题 → 改动内容与位置」。

---

## 1. 矩阵的"4 种看法"太抽象，要具体例子

- **提出者**：老陈
- **位置**：第 1 节「一个矩阵的 4 种看法」，`matrix_4ways` 调用之后
- **改动**：插入新 markdown cell，用课程里实际使用的 3×2 矩阵 `[[1,2],[3,4],[5,6]]` 逐条列出"整体 / 6 个数字 / 2 列 / 3 行"四种看法的具体内容，并用"切蛋糕"类比说明为什么四种看法是同一个对象，最后指出后续第 2、3、4 节分别会用到哪种看法。

## 2. "秩 1 矩阵"是什么？为什么外积是秩 1？

- **提出者**：老陈、琳达、小雨（三人都问到，且都提到希望有具体例子/对比）
- **位置**：第 2 节「向量×向量」之前
- **改动**：插入两个 cell：
  1. markdown——用"一盒彩笔（红蓝 2 色 vs 深浅不同的红）"类比讲清"秩 = 独立方向的个数"，再从外积公式 `u@v.T 的第 k 列 = v[k]*u` 推出"所有列都是同一个 u 的倍数，所以秩为 1"。
  2. code——具体算一个 3×2 外积矩阵，打印"第1列/第2列"的比值验证方向相同，并用 `np.linalg.matrix_rank` 对比一个秩 2 矩阵，让"秩1 vs 秩2"的差别可运行、可见。

## 3. mel_filterbank / DFT 矩阵与"列的线性组合"具体怎么对应？

- **提出者**：老陈
- **位置**：第 3 节 Aurora 注释（`M @ stft_output`、`W @ x`）之后
- **改动**：插入 markdown 解释 M 的列对应 FFT 频点、`stft_output` 分量是频点能量；再插入 code cell，构造 2×3 迷你 mel 矩阵和长度 3 的迷你 stft 向量，实算 `M_mini @ stft_mini` 并用手动列组合公式复算一遍验证一致，把"直接实例"落到具体数字。

## 4. laviz 函数是黑箱，想知道内部逻辑

- **提出者**：琳达
- **位置**：「写代码前，先把变量表补完整」cell 之后
- **改动**：插入"可选深挖" markdown + code：用自助餐"打饭"类比说明 `mat_times_vec` 的核心逻辑（每列乘以对应系数、逐列累加），再手写一遍等价逻辑（不调用 laviz），用第 3 节同款例子核对 `A_demo @ x_demo` 与手动累加结果一致。

## 5. 旋转矩阵公式从何而来？

- **提出者**：小雨
- **位置**：「参数实验」引入旋转矩阵之前
- **改动**：插入 markdown，从"矩阵由列决定"出发，把 `[1,0]`、`[0,1]` 当指南针分别转 θ 角推导出两列 `(cosθ,sinθ)`、`(-sinθ,cosθ)`，拼出 `R(θ)`；再插入 code cell 验证 `R(90°)` 把 `[1,0]` 转到 `[0,1]`。

## 6. 为什么代码里用 `np.radians()` 而不是直接写角度？

- **提出者**：小雨
- **位置**：紧接第 5 条之后，「参数实验」cell 之前
- **改动**：插入 markdown 说明三角函数的弧度约定，解释直接传入度数（如 30）会被当成 30 弧度导致旋转错误，`np.radians(30)` 做的是角度→弧度换算。

## 7. 为什么 R@Rᵀ=I 就能推出"保距"（长度不变）？

- **提出者**：老陈、琳达、小雨
- **位置**：「参数实验」cell 之后（正交矩阵保距断言之前）
- **改动**：插入完整推导 markdown：从 `‖v‖²=vᵀv` 出发，展开 `‖Rv‖²=(Rv)ᵀ(Rv)=vᵀRᵀRv`，代入 `RᵀR=I` 得到 `‖Rv‖²=‖v‖²`，不跳过任何一步，呼应老陈"应该在 notebook 里直接展示推导过程"的要求。

## 8. det 的正负号（+1/-1）几何上是什么意思？

- **提出者**：老陈、琳达、小雨
- **位置**：旋转/缩放/剪切可视化 cell 之后，几何变换性质断言 cell 之前
- **改动**：插入 markdown，用"基向量围成的正方形被拉成平行四边形"讲 `|det|` = 面积缩放倍数，用"翻到纸背面变镜像"讲 det 符号 = 定向是否翻转；再插入 code cell 对比旋转（det=+1）、反射（det=-1）、各向异性缩放（det=6，直接对应琳达问的"6 从哪来"）。

## 9. "保距" vs "保面积"，几何上有什么不同？

- **提出者**：老陈
- **位置**：紧接第 8 条之后
- **改动**：插入 markdown，用"推斜一摞扑克牌"类比讲清剪切"面积不变但长度、角度会变"，与旋转的"保距"对照；插入 code cell 验证剪切矩阵 `K` 满足 `det(K)=1` 但 `K@Kᵀ≠I`，且 `‖K@v‖≠‖v‖`。

## 10. classify_transform 判断顺序为什么重要？给具体反例走一遍

- **提出者**：老陈、琳达、小雨（三人都要求"具体代码对比错误顺序 vs 正确顺序"）
- **位置**：练习 `classify_transform(A)` 说明 cell 之后，学生实现 cell 之前
- **改动**：插入 code cell，对 `[[0,1],[1,0]]` 分别跑"❌ 先查对角"和"✅ 先查正交"两条路径，打印每一步的中间判断值（是否正交/是否对角/det），直接展示错误顺序会把它误判成 shear；再插入 markdown 解释原因——该矩阵对角线外的元素是 1（非 0），"对角检查"因此正确地认定它"不是对角矩阵"，若顺序错了就会在正交检查之前就掉进 shear 分支。

## 11. 内积、外积在 Aurora 中各自的角色不清楚

- **提出者**：琳达
- **位置**：「本课收束」cell 之后
- **改动**：插入总结表格 markdown，对比内积（DFT、余弦相似度，"压缩成一个数"）与外积（SVD、LoRA，"铺成一个矩阵"）的输入输出、Aurora 中的具体用途、以及各自对应矩阵×向量 / 矩阵×矩阵哪张图。

---

## 覆盖情况

- 老陈 7 条问题：全部对应第 1、2、3、7、8、9、10 条改动。
- 琳达 6 条问题：全部对应第 2、7、8、4、10、11 条改动。
- 小雨 6 条问题：全部对应第 2、7、5、10、8、6 条改动。
- 共 19 条学生问题实例，收敛为 11 处插入位置（多名学生重复提出的问题在同一位置一次性解答）。
- 所有新增内容遵循"先类比/故事，后公式"的科普写法，未使用"显然/易得/不难看出"；新增代码全部为可运行 cell，依赖前置变量的插入顺序已核对（如 `A_demo`、`R_demo` 等均在使用前定义）。
