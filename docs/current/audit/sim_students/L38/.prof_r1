完成！已处理 **3 名学生的 18 条问题**，修订 L38 notebook：

| 新增内容 | 数量 |
|---|---|
| Markdown cell（推导 + 解释） | 8 |
| Code cell（演示 + 验证） | 3 |
| 修改现有 cell（扩充） | 2 |

**8 大改动覆盖的学生卡点**：

1. **旋转因子从何而来** → 新增 cell 从 DFT 定义推导 $W_N^k = e^{-2\pi i k/N}$，解释"旋转"的几何含义，严格推导 $W_N^{k+N/2} = -W_N^k$

2. **蝶形公式怎么推导** → 新增 cell 从 DFT 奇偶拆分逐步推导 $X[k] = E[k] + W_N^k \cdot O[k]$，说明为何必须偶奇分

3. **N=2 最小蝶形例子** → 新增 code cell 演示从两个单点到两点 DFT 的最小单元

4. **N=4 半步手算** → 扩充现有 cell，逐步展示 E = DFT([1,1]) = [2,0]、O = DFT([0,0]) = [0,0]、旋转因子 W_4^k = [1,-i] 的计算

5. **butterfly 向量化操作** → 新增 code cell 单步执行 N=4 例子，逐行打印 twiddles、乘法、蝶形合并的所有中间值

6. **旋转因子表格计算** → 新增 cell 逐个演示 W_8^0=1、W_8^1=(√2/2)-i(√2/2)、W_8^2=-i、W_8^3=-(√2/2)-i(√2/2) 的符号确认

7. **复杂度求和式标准形式** → 修改闭卷题，补充 $T(N) = \sum_{i=0}^{\log_2 N-1} N = N\log_2 N$ 的标准求和式展开

8. **位反转 + 递归↔迭代** → 新增附录 C，完整说明位反转的定义（N=8 表格）、算法、为何需要、递归版隐式拆分 vs 迭代版显式置换的对应关系

✅ **修订说明已写入** `/Users/z/AURORA/docs/current/audit/sim_students/L38/professor_review.md`
