读完了。现在我用琳达的视角输出学习日志。

---

# 琳达 — L25 学习日志

## 我理解了什么

梯度下降就是用脚探探坡度，然后沿着"最陡下坡"方向走一小步，重复这个过程逼近谷底。用公式就是：`新位置 = 旧位置 - 学习率 × 梯度`。学习率太大会跳过目标来回震荡，太小会蠢蠢地挪动。

## 遇到的困难和问题

### 【推演】公比 |1-2·lr| 的收敛条件怎么来的？

> 卡在：L25 第一部分"lr 的边界条件"段落

课程说"每步误差乘以公比 `(1 - 2·lr)` = 0.8"，然后给了 lr 的三个临界值。但这个 1-2·lr 公式是怎么推导出来的？我跟着手算：
- x₁ = 0 - 0.1 × (-6) = 0.6 ✓
- x₂ = 0.6 - 0.1 × (-4.8) = 1.08 ✓

但我看不出为什么最终错误量 (x_n - 3) 与 (x_{n-1} - 3) 的关系就是 "乘以 (1-2·lr)" 这个形式。能补充一下这个推导吗？比如用极小值附近的近似，或者直接展开一次迭代来推？

### 【理解】中心差分公式 (f(x+h) - f(x-h))/(2h) 为什么是这个？

> 卡在：cc9d2e3e cell 数值梯度的计算

代码直接用 `slopes = (f(xs + h) - f(xs - h)) / (2 * h)` 计算近似梯度。我知道导数是 lim_{h→0} (f(x+h) - f(x))/h，但为什么这里是*前后各加减一个 h*，然后除以 2h？不是直接 (f(x+h) - f(x))/h 吗？

### 【理解】梯度在多维时怎么理解？突然跳到 2D 有点懵

> 卡在：问 4"f(x,y) = x² + y²，∇f(x,y) = ?"

前面全是一维的 f(x) = (x-3)²，讲的是"一条线上的坡度"。突然问 4 问二维，∇f(x,y) = [4, 8] 是个*向量*？这时候怎么"走一步"？是分别对 x、y 各走一小步吗？课程对这个概念跳跃没有铺垫。能简单说一下"多维梯度"是怎么回事吗？

### 【实践】线性回归代码里的梯度公式从哪推的？

> 卡在：3f5b9cce cell 实战预告代码

```python
w -= lr * np.mean(err * X) * 2     # 对 MSE 的梯度
b -= lr * np.mean(err) * 2
```

课程说这是"对 MSE 的梯度"，但没有给出 MSE 的定义或梯度推导。为什么 w 的梯度是 `err * X`，b 的梯度是 `err`？为什么都乘以 2？这是从什么损失函数怎么求导得来的？这段代码和前面 f(x)=(x-3)² 的单变量 GD 一点接不上。

### 【计算】关于 lr=0.5 时公比为 0 的理解

> 卡在：L25 第一部分"lr = 0.5：一步到达"

课程说"lr = 0.5 时公比恰为 0，x₁ = 3"。我手算了一遍：x₁ = 0 - 0.5 × (-6) = 3，确实一步到达。但这是怎么和"公比 1-2·lr = 1-1 = 0"这个数学关系联系起来的？为什么一旦公比为 0，就意味着"一步到底"？这个逻辑能再讲清楚吗？

---
