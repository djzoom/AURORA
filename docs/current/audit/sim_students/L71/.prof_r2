Second-round revision complete: addressed the one outstanding issue (EOT parameter justification for "4 steps" / "+2.0") by expanding the `fake_lm` docstring, inserting a new markdown cell ("插曲：EOT 是什么...") and a runnable demo code cell right after the `fake_lm` definition (before Task 1) that varies the parameters and shows the algorithm is unaffected. Notebook validated with `nbformat.read` and the new code cell executed successfully; revision notes appended to `professor_review.md`.
