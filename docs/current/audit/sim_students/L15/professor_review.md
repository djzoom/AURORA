# L15《线性方程组》修订说明 — 教授回应学生问题

依据 `laochen.md`、`linda.md`、`xiaoyu.md` 三份学习日志，对
`notebooks/2_linear_algebra/L15_linear_systems.ipynb` 做了增补式修订（只增不删，
原有 cell 全部保留）。以下逐条列出「学生问题 → 改动位置与内容」。修订后
notebook 已通过 `nbformat` 合法性校验，并用 `nbclient` 完整执行一遍确认新增
代码 cell 全部可运行（唯一在末尾"本课自评"处按设计因未填写而 assert 失败，
这是原有行为，与本次修订无关）。

---

## 1. 秩(rank)的定义缺失
- **提出者**：linda（【理解】秩(rank)的定义和含义缺失）
- **改动位置**：插入在 `cell-l15-0120`（剧情 cell）之后，新 markdown cell
  （id `1d010b2e`）+ 配套代码 cell（id `2bd064d9`）。
- **内容**：用"三个朋友指路，其中两条路线其实是同一条"的类比引出"秩=真正独立
  的信息条数"；用 `[[1,0],[0,1]]`（rank=2）和 `[[1,1],[1,1]]`（rank=1）两个最小
  例子把定义钉死。

## 2. 列空间(column space)概念突兀，无直观定义
- **提出者**：laochen（为什么秩变化反映"b是否在列空间"）、linda（"列空间"概念
  突兀）、xiaoyu（列空间的直观含义，希望用图/生活化比喻）
- **改动位置**：同上，`1d010b2e` + `2bd064d9`。
- **内容**：把列空间定义为"A 的列向量任意加权求和能到达的所有点"，配合
  "纸面/直线，走不出去"的生活化比喻；用 `A=[[1,1],[1,1]]`（laochen 指定的例子）
  演示列空间退化成一条直线，代码验证 `b=[2,3]`（不在线上→无解）和
  `b=[2,2]`（在线上→无穷多解）。

## 3. 为什么 rank(A) < rank([A|b]) 就意味着无解——推理链条缺失
- **提出者**：laochen（【理解】为什么秩的变化能反映…）、linda（【推演】为什么
  "rank(A) < rank([A|b])"意味着无解，增广矩阵为什么秩会变大）
- **改动位置**：插入在 `cell-l15-0126`（三种情形数值对照 cell）之后，新
  markdown cell（id `79f9261b`）。
- **内容**：给出"加一列秩最多+1"的直觉解释（b 是否带来"新方向"），逐一对照
  cell-l15-0126 打印出的唯一解/无穷解/无解三个例子，用列空间语言复盘每个例子
  秩为什么变或不变。

## 4. 初等行变换（尤其 ER3）为什么不改变方程组的解
- **提出者**：linda（【理解】初等行变换为何不改变方程组的解）、xiaoyu（问题2：
  初等行变换为什么不改变解，尤其 ER3 "改变了方程本身"的疑惑）
- **改动位置**：插入在 `0daf8385`（ER1/ER2/ER3 定义 cell）之后，新 markdown
  cell（id `2d02d8d9`）+ 代码 cell（id `836155b5`）。
- **内容**：逐条给出 ER1/ER2/ER3 保持解集不变的代数论证（ER3 部分展示"新方程
  是旧方程的加权重组，且该操作可逆"）；代码用二元方程组
  `x+y=3, 2x-y=0` 做一次真实的 ER3 变换，验证变换前后解完全一致。

## 5. 消元系数（2、3、5）怎么选，为什么不用分数
- **提出者**：xiaoyu（问题5：消元时 ER3 的系数怎么选，`2R2-3R1` vs
  `R2-1.5R1`）
- **改动位置**：插入在 `0352c35f`（手动消元 cell）之后，新 markdown cell
  （id `40421b73`）+ 代码 cell（id `78c7ac84`）。
- **内容**：解释整数系数（方法A）vs 分数系数（方法B）两种消法都合法、都不
  改变解集，纯粹是"避免小数累积误差 vs 步骤更少"的工程取舍；代码用方法B
  重新消一遍，验证与方法A结果等价。

## 6. 行阶梯形是什么、消到什么程度，以及倒代入过程完全是黑盒
- **提出者**：laochen（【推演】什么叫"行阶梯形"…、【推演】倒代入的过程没有
  展示）
- **改动位置**：接上一条之后，新 markdown cell（id `3e25fb9b`）+ 代码 cell
  （id `9bfd576e`）。
- **内容**：定义行阶梯形（主元阶梯状排列），把 notebook 原本只消了第1列的
  矩阵继续消完第2列，得到真正的 REF；然后手写倒代入三步（先解 z，代入解 y，
  再代入解 x），逐行打印方程和中间结果，最终与 `np.linalg.solve` 的答案
  （x=1, y=2, z=-3）核对一致。

## 7. 条件数(condition number)是什么，为什么大就不稳定
- **提出者**：laochen（【概念】条件数是什么…）、xiaoyu（问题3：参数实验中的
  "条件数"太陌生，能否给出 κ 很大时具体会发生什么的例子）
- **改动位置**：插入在 `a26c4eab`（可视化 cell）之后、原 `cell-l15-0133`
  （eps 实验）之前，新 markdown cell（id `f68c3ba5`）；并在 `cell-l15-0133`
  之后插入新代码 cell（id `73e72538`）。
- **内容**：用"钝剪刀 vs 利剪刀"类比条件数对输入抖动的放大效应；给出正式定义
  `κ(A)=‖A‖·‖A⁻¹‖ = σmax/σmin`；新代码对比 `κ≈1` 的单位矩阵和 `κ` 很大的
  近奇异矩阵，在同样大小的 b 扰动下 x 的变化量差多少个数量级，直接回应
  "κ大到底会怎样"的疑问。

## 8. 行列式(determinant)和"奇异矩阵"的定义缺失
- **提出者**：linda（【理解】行列式和奇异矩阵的定义）
- **改动位置**：插入在条件数演示代码（`73e72538`）之后、原 `cell-l15-0134`
  （奇异矩阵实验）之前，新 markdown cell（id `f9e566e2`）+ 代码 cell
  （id `ee721bc8`）。
- **内容**：给出 2×2 行列式公式 `ad-bc` 及其"平行四边形面积"几何含义，3×3
  对应体积；解释 `det=0` ⇔ 空间被压扁 ⇔ 列线性相关 ⇔ 不可逆 ⇔ singular；
  代码手算并用 `np.linalg.det` 验证 `A=[[1,2],[2,4]]` 的行列式为 0。

## 9. `np.linalg.lstsq` 返回的 4 个值、"最小范数解"/"最小残差解"术语未定义
- **提出者**：laochen（【实践】lstsq 返回的4个值是什么意思、【使用】"最小范数"
  和"最小残差"术语没有定义）、linda（【理解】lstsq 返回的 singular values
  是什么，与 rank 的关系）、xiaoyu（问题6：solve/lstsq 使用边界不清，"最小
  范数/残差解"的几何含义）
- **改动位置**：插入在 `cell-l15-0135`（solve/lstsq 对比 cell）之后，新
  markdown cell（id `5cdbe949`）+ 代码 cell（id `c6b7ab45`）。
- **内容**：逐一解释 `x_ls`、`res`、`rank`、`sv` 四个返回值的含义，指出非零
  奇异值个数=rank；用"选最省力的答案/离目标最近的投影点"类比解释"最小范数解"
  （无穷多解时）和"最小残差解"（无解时）的选择动机；代码打印一致方程与矛盾
  方程各自的 `sv`、`res`，直观展示两者差异。

## 10. solve 与 lstsq 的使用边界、决策规则不明确
- **提出者**：xiaoyu（问题6，同上，"是说 classify_system 返回 none 就用
  lstsq 吗"）
- **改动位置**：紧接第9条之后，新 markdown cell（id `7b5e02c5`）。
- **内容**：给出明确的四步决策流程：先跑 `classify_system` → unique 用
  solve → many/none 用 lstsq，并说明 many/none 情形下 lstsq 分别返回什么
  意义的解，呼应 Aurora LPC 场景。

## 11. 为什么超定系统（方程多于未知数）还会有唯一解
- **提出者**：laochen（【理解】为什么超定系统还会有唯一解）、xiaoyu（问题4：
  超定系统中 rank=n 却有 4×2 矩阵的矛盾感，"最小二乘意义的唯一解"是什么）
- **改动位置**：插入在 `2aeb143d`（Aurora 连接 cell）之后、图示 cell
  （`702d8e09`）之前，新 markdown cell（id `19e44bbd`）+ 代码 cell
  （id `0582240e`）。
- **内容**：把 `OD` 的四个方程翻译成人话（x=1, y=1, x+y=2, 2x+y=3），逐行验证
  第3、4行分别是第1、2行的线性组合（对 A 和 b 同时成立），因此 `rank(A)` 仍
  只有2，不是4，从而"唯一解"合理；进一步区分这是"特意设计的温顺超定系统"，
  并新造一个带噪声、真正矛盾的超定例子，展示 `rank(A)<rank([A|b])`（严格无解）
  时 `lstsq` 给出的"最小二乘意义唯一解"，呼应 Aurora LPC 的真实场景。

---

**执行校验**：`python3 -c "import nbformat; nbformat.read(..., as_version=4)"`
通过；`nbclient` 端到端执行整本 notebook，除原有"本课自评"cell（按设计要求
学生手填后才能通过 assert）外全部单元格无报错。
