# L06_euler.ipynb 修订说明（教授批注）

依据 `laochen.md`、`linda.md`、`xiaoyu.md` 三位学生的学习日志，对 `notebooks/1_complex_trig/L06_euler.ipynb` 做了只增不减的修订。原有内容全部保留，新增内容以 markdown/code cell 插入、或在原 cell 内追加的方式完成。以下按学生问题逐条对应改动位置。

## 老陈

1. **问题1（e 的虚数次方是什么意思，公式是"自证"的）**
   位置：`0b2c1013`（"1. 手动拼 e^{iθ}"）内追加一句forward pointer + 新插入 markdown cell「插播：e^{iθ} 到底是"证明"出来的，还是"定义"出来的？」（紧跟其后）。
   改动：给出不依赖泰勒展开的"定义视角"（数学家为保留指数律 `e^a·e^b=e^{a+b}` 而把 e^{iθ} 定义为单位圆上的旋转点），并直接回应"用 np.exp 验证是否自证"的疑虑——承认这一步确实是"确认 numpy 遵守约定"而非从零证明。

2. **问题2（"乘法=旋转"只给结论，推不出）**
   位置：`cell-l06-0028`（"本课剧情"）追加 forward pointer；紧随其后插入 markdown cell「为什么"乘法=旋转"成立？——用和角公式收尾」+ code cell（30°×60°=90° 数值验证）。
   改动：逐步展开 `(cosα+isinα)(cosβ+isinβ)`，回顾和角公式，推导出 `cos(α+β)+i sin(α+β)`；随后跑代码验证 30°+60°=90°。

3. **问题3（"频率索引""投影""DC分量"超前，时钟比喻只有半句）**
   位置：`pitfall-l06-twiddle`（常见误解格）内追加时钟比喻完整版；`cell-l06-0033` 之后插入 markdown「写代码前的"体感"：twiddle 到底是个什么东西？」+ N=4 迷你例子 code cell + 探照灯比喻 markdown cell。
   改动：把 N/k/n 讲成"表盘刻度总数/转速档位/走了几步"，并明确"投影/内积/DC分量留给 L37"，给出探照灯类比。

4. **问题4（白板手算负角 cos/sin 符号卡住）**
   位置：`cell-l06-0037`（本课收束）之后插入 markdown「白板计算前，先看一眼"负角速查表"」+ 单位圆四点标注图 code cell；`1be513e4`（白板挑战）内追加极坐标写法示例及折叠提示中 n=2/n=3 的符号提示。
   改动：给出 `cos(−θ)=cosθ`、`sin(−θ)=−sinθ` 规律 + 手表类比，并画出 0°/-45°/-90°/-135° 四点坐标图供对照。

5. **问题5（相位打印在 n=5 处"跳变"，unwrap 无解释）**
   位置：`cell-l06-0035` 之后插入 markdown「预告：为什么打印出来的角度会突然"跳"一下？」+ 演示 code cell；`cell-l06-0036`（参数实验）内追加提醒段落。
   改动：用"12点钟表绕回"类比解释 `np.angle` 的 (−π,π] 读数范围、"相位绕回"现象，说明 `np.unwrap`/`np.diff` 各自的作用，并提前预告 n=5 附近的假跳变。

## 琳达

1. **问题1（e 的虚数次方唯一解释是泰勒展开，看不懂；乘法=旋转套用实数指数律没交代）**
   同老陈问题1、2的改动位置（`0b2c1013` 后插入的"插播"cell 处理虚数次方；`cell-l06-0028` 后插入的和角公式推导 cell 同时回应"虚数指数为何还满足指数律"）。

2. **问题2（k·n 为什么要相乘，缺少非DFT的迷你例子）**
   位置：`cell-l06-0033` 后插入的「写代码前的"体感"」markdown + N=4 迷你例子 code cell（同老陈问题3位置）。
   改动：给出具体 N=4, k=1 的迷你示例，打印 n=0..3 应转到的角度，不使用 DFT 术语。

3. **问题3（相位实验与"均匀递减"表面矛盾，unwrap 未解释）**
   同老陈问题5的改动位置（`cell-l06-0035` 后插入的 unwrap 预告 cell + 演示代码）。

4. **问题4（"频率分辨率""DC分量"两个术语无定义）**
   位置：`cell-l06-0036`（参数实验）内追加"频率分辨率"定义段落；`1be513e4`（白板挑战问4）内追加 DC 分量的括号注释（Direct Current / 直流，对应"转速为0的指针"）。

## 小雨

1. **问题1（e 的 i 次方 / 泰勒展开看不懂）**
   同老陈问题1的改动位置（`0b2c1013` 后插入的"插播"cell）。

2. **问题2（投影/内积/DC分量无生活化比喻）**
   位置：`cell-l06-0033` 后插入的探照灯比喻 markdown cell（同老陈问题3位置）。
   改动：用"不同转速探照灯照信号，哪盏灯下信号站得最齐"类比投影/内积，DC分量类比"不转的那盏灯"。

3. **问题3（"乘法=旋转"想要图/数值例子）**
   同老陈问题2的改动位置（`cell-l06-0028` 后插入的和角公式推导 + 30°/60°/90° 数值验证 code cell）。

4. **问题4（相位打印跳变，np.diff/np.unwrap 都没学过）**
   同老陈问题5的改动位置（`cell-l06-0035` 后插入的 unwrap 预告 cell + 演示代码，其中对 `np.diff` 也做了"逐项相减"的定义）。

5. **问题5（白板 n=2/n=3 负角符号算错，想要单位圆标注图）**
   同老陈问题4的改动位置（`cell-l06-0037` 后插入的"负角速查表" markdown + 四点单位圆标注图 code cell）。

## 校验

- `python3 -c "import nbformat; nbformat.read(...)"` 通过（37 个 cell，合法 nbformat）。
- 用 `nbclient` 完整执行全部 cell：仅剩原有的自评 cell（`5db4f2b0`）因字段未填写而按设计抛出 `AssertionError`，属预期行为；其余所有 cell（含全部新增 cell）均正常运行，无报错。
