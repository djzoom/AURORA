# L13 修订说明 — 教授审阅

依据三份学生学习日志（`laochen.md`、`linda.md`、`xiaoyu.md`）中提出的全部问题，对
`notebooks/2_linear_algebra/L13_special_matrices.ipynb` 做了纯增量修订（只增加、不删减原文）。
以下逐条列出"学生问题 → 改动内容与位置"。

## 1. 为什么 `QᵀQ = I` 就能推出 `QQᵀ = I`？（左逆=右逆）

**提出者**：老陈 Q1

**改动位置**：cell `5ae30ad5`（"1. 三兄弟：单位阵、转置、逆"）末尾追加"追问：`QᵀQ = I` 凭什么保证 `QQᵀ = I` 也成立？"一节。

**改动内容**：用"钥匙开锁/锁门是不是一回事"类比引出"方阵的左逆=右逆"这一性质；给出"n 个互相垂直的单位向量在 n 维空间里自动构成完整坐标系（正交基）"的直觉解释；诚实注明严格证明需要 L16–L18 的可逆性/行列式工具，当下先用代码验证。紧接着在新插入的 code cell（id `ac3795ce`）中用具体旋转矩阵同时验证 `QᵀQ` 和 `QQᵀ` 都约等于 `I`。

## 2. "列向量两两正交且单位长度" 如何具体推出 `QᵀQ = I`？（需要 2×2 手算示例）

**提出者**：老陈 Q2、琳达（"手算一个 2×2 例子"的明确要求）

**改动位置**：cell `cell-l13-0099`（正交性判据说明）追加"手算一遍：矩阵乘法的定义到底在算什么？"一节；随后插入 code cell（id `5f194e1d`）。

**改动内容**：显式写出 `(QᵀQ)[i,j] = qᵢ·qⱼ` 这条对应关系，说明它来自 L12 矩阵乘法定义本身；用 90° 旋转矩阵 `Q=[[0,-1],[1,0]]` 逐元素手算四个位置 `(QᵀQ)[0,0]…[1,1]`，拼出结果 `I`。新插入的 code cell 用 NumPy 手动取出两列、逐元素计算点积，和 `Q.T @ Q` 对比一致性，把手算过程变成可运行代码。

## 3. `(Ax)·y = x·(Aᵀy)` 这条内积恒等式没有推导过程

**提出者**：小雨

**改动位置**：cell `5ae30ad5` 追加"追问：`(Ax)·y = x·(Aᵀy)` 这个等式是怎么来的？"一节；code cell `ac3795ce` 前半段验证。

**改动内容**：把恒等式拆成"点积=转置相乘"+"乘积转置定律"（L12 已验证过）两步推导，说明不是新增假设。代码用具体数字验证等式两边相等。

## 4. 什么是"酉矩阵"？和正交矩阵、共轭转置是什么关系？

**提出者**：老陈 Q3、Q7，琳达，小雨（三人都提出）

**改动位置**：在 cell `2616ccb0`（"🔗 Aurora 连接"）之后插入新 markdown cell（id `183e25a0`）+ code cell（id `bf8348ac`）。

**改动内容**：解释复数版"长度不变"为何需要共轭转置 `Uᴴ`；给出共轭转置的计算规则（转置+每元素取共轭）；说明"若 U 是实矩阵，`Uᴴ=Uᵀ`"；明确"正交矩阵是酉矩阵在纯实数情形下的特例"；说明 DFT 矩阵含复数项所以属于酉矩阵。代码用一个简单对角酉矩阵演示共轭转置的计算和 `UᴴU=I`。

## 5. 正定矩阵定义 `xᵀAx > 0` 的几何/物理直觉、shape 推演

**提出者**：老陈 Q4，琳达（shape 疑问），小雨（二次型无推导）

**改动位置**：在 cell `95998a75`（图示）之后插入新 markdown cell（id `4270c928`）+ code cell（id `6f2f4238`），置于原 PD 定义 cell（`pd-definition-added`）之前。

**改动内容**：用弹簧势能 `E=(1/2)kx²` → 多弹簧系统 `E=(1/2)xᵀKx` 的类比引出"能量"直觉；逐步推导 `xᵀAx` 的 shape：`(1,n)@(n,n)@(n,1)=(1,1)` 标量。代码用具体矩阵和多个方向的向量验证能量恒为正。

## 6. 为什么 `MᵀM`（以及 `XᵀX`）天然半正定/对称？

**提出者**：老陈 Q5，小雨（Gram 矩阵问题）

**改动位置**：在原 `pd-definition-added` cell 之后插入新 markdown cell（id `f26c8631`）+ code cell（id `d094de54`）。

**改动内容**：两步证明——① `(MᵀM)ᵀ=MᵀM` 对称（转置定律）；② `xᵀ(MᵀM)x=(Mx)ᵀ(Mx)=|Mx|²≥0` 半正定。同时点出协方差矩阵 `C=(1/N)XᵀX` 就是该定理的特例，说明为什么它必须半正定。代码构造随机 `M`，验证 `A=MᵀM` 对称且特征值全部非负。

## 7. 为什么对称矩阵的特征值必为实数？

**提出者**：琳达、小雨

**改动位置**：在 cell `cell-l13-0097` 之后、`cell-l13-0098`（特征值代码）之前插入新 markdown cell（id `1f49ba55`）。

**改动内容**：先解释"特征值/特征向量"是什么（避免和新概念一起打懵），再说明一般矩阵特征值可能是复数、对称矩阵特征值必为实数是一条代数定理；诚实说明严谨证明超出本课范围，留到后续系统学习，同时给出 `eigvalsh` 更快更稳更能保证返回实数的实际理由。

## 8. Cholesky 分解 `A=LLᵀ` 的含义，为什么"分解成功"证明正定？

**提出者**：小雨

**改动位置**：在 code cell `d094de54` 之后、原 `cell-l13-0100` 之前插入新 markdown cell（id `ecf00323`）。

**改动内容**：用"正数才能开平方根"类比矩阵的 Cholesky 分解（L 是"矩阵的平方根"）；推导 `xᵀAx=|Lᵀx|²≥0`，并说明 Cholesky 算法要求对角线非零从而保证严格 `>0`；解释 `np.linalg.cholesky` 分解失败即等价于"要给负数开平方根"，因此"分解成功⟺正定"的判据可靠。

## 9. `try / except` 语法看不懂

**提出者**：小雨

**改动位置**：同上 markdown cell（id `ecf00323`）末尾 + 新插入 code cell（id `97f2e650`）。

**改动内容**：用"先冒险试一下，出问题接住不崩溃"解释 try/except 逻辑，对照 Cholesky 代码里 `LinAlgError` 的用法；新代码用更简单的 `math.sqrt` 负数报错场景演示同样的语法结构，可独立运行。

## 10. `np.allclose` 容差（atol/rtol）什么时候该调？

**提出者**：琳达、小雨

**改动位置**：在 cell `47eaf451`（正交性判断练习）之后插入新 markdown cell（id `67680022`）+ code cell（id `728a4f82`）。

**改动内容**：给出 `np.allclose` 判据公式 `|a-b| <= atol + rtol*|b|`，说明默认值 `rtol=1e-5`、`atol=1e-8`；解释本课后面出现的 `atol=1e-12` 是因为验证的是"理论上精确相等"的代数恒等式，需要逼近浮点精度极限。代码演示同一对数字在默认容差和调紧容差下的判断差异。

## 11. 对角矩阵为什么不是正交矩阵——容易只记得"垂直"忘了"单位长度"

**提出者**：老陈 Q6

**改动位置**：追加到 cell `cell-l13-0101`（参数实验：旋转角与对称矩阵构造）末尾。

**改动内容**：明确指出正交矩阵要求"两两垂直"*并且*"单位长度"，用 `np.diag([2,3])` 具体算出 `QᵀQ=[[4,0],[0,9]]≠I`，强调"垂直但不是单位长度"是最容易踩的坑。

## 12. 白板挑战为什么要手算，代码能立刻给答案

**提出者**：琳达

**改动位置**：在 `whiteboard-bridge` 之后、白板挑战题目（`ae66325b`）之前插入新 markdown cell（id `6a753171`）。

**改动内容**：说明手算的目的不是"验证对不对"而是"把矩阵乘法的每一步过一遍、建立直觉"；建议"先手推再运行代码核对，对不上再回头找错"的具体做法，呼应前面手算 `(QᵀQ)[i,j]` 的训练方式。

---

## 校验

- 全部新增/扩充内容遵循"先故事/类比、后公式"的科普写作风格，未使用"显然/易得/不难看出"。
- 所有新增 code cell 均已用 `jupyter nbconvert --execute` 端到端执行验证可运行（除原有的自评 gate cell 外，该 cell 需学生手动填写 `True/False`，属于既有设计，非本次改动引入）。
- `nbformat.read(..., as_version=4)` 校验通过，notebook 结构合法。
- 修订过程中发现并修复了 3 处编辑失误（cell `5ae30ad5`、`cell-l13-0099`、`cell-l13-0101` 中误粘贴了工具展示用的 `</cell>` 标签），已清除。
