# L08《复数平面可视化》教授复核 — 修订说明

复核对象：`notebooks/1_complex_trig/L08_visual_complex.ipynb`
依据材料：`laochen.md`、`linda.md`、`xiaoyu.md` 三位模拟学生的学习日志。

修订原则：只增不减，原有 cell 全部保留；在学生卡住的位置附近插入新的 markdown/code cell，
用类比、分步推导和可运行代码把每条问题的"为什么"补上。全部新增代码 cell 已在独立副本中
执行验证，均可正常运行、结果与文字论述一致（详见文末"验证记录"）。

---

## 老陈的问题 → 改动

1. **欧拉公式的推导铺垫缺失（"为什么实部是 cos、虚部是 sin"）**
   → 位置：`## 1. 欧拉公式`（原 cell `acbd621f`）之后插入新 markdown+code cell
   （新 cell `0816e987` / `cd1da39b`）：用泰勒级数展开 e^{iθ}，把项按"不带 i"和"带 i"分组，
   对应 cosθ、sinθ 各自的级数；代码验证级数项数增加时误差趋近于 0。

2. **"关于实轴对称"的几何直觉不清楚 / 共轭为什么叫共轭**
   → 位置：`## 3b. 复数共轭`（原 cell `cell-l08-0044`）之后插入新 markdown cell
   （新 cell `1410c1bc`）：解释"共轭"一词的由来（成对出现、互为镜像），并说明这个操作在
   真实录音 DFT 压缩存储中的实际意义。

3. **DFT 旋转因子负号从何而来（顺时针 vs 逆时针）**
   → 位置：`## 4. 单位根`小节内，`roots_of_unity(8)` 之后插入新 markdown cell
   （新 cell `2c3fdfda`）：说明正号=逆时针、负号=顺时针，以及"分析用负号、合成（IDFT）用
   正号"的约定口诀。

4. **"投影系数"和复数点积的具体含义（是否要取共轭）**
   → 位置：`## 5. 正弦叠加→方波`（原 cell `2c3bad7c`）之后插入新 markdown+code cell
   （新 cell `925933e3` / `1a1867c2`）：从实数向量点积类比到复数点积 `Σ z1·conj(z2)`，
   解释取共轭是为了让"自己点自己"的结果恒为非负实数，并连回 FFT 投影系数公式里负号的来源。

5. **plot_conjugate 练习：annotate/quiver 参数细节不清楚**
   → 位置：`## 🏋️ 练习：实现 plot_conjugate`（原 cell `cell-l08-0046`）之后插入新
   markdown+code cell（新 cell `e895d16a` / `a9b556dc`）：对比 `ax.plot+scatter`、
   `ax.annotate(arrowprops=...)`、`ax.quiver` 三种画箭头写法的参数格式，并复用
   `complex_point` 里 `lim = abs(z)*1.4` 的坐标轴缩放写法、`#2A9D8F`/`#E76F51` 配色约定。

6. **弧度制和角度制的转换缺少铺垫**
   → 位置：`## 0. 先修铺垫`（`d4c8074a` 助手函数 cell 之后新插入的整节，新 cell
   `45745577` / `04901d27`）：解释弧度制的定义（弧长/半径）和角度换算公式，并用代码打印
   常见角度的弧度对照表。

## 琳达的问题 → 改动

1. **欧拉公式凭空而来** → 同"老陈 1"（新 cell `0816e987` / `cd1da39b`）。

2. **"单位根"这个名字没人解释** → 位置：`## 4. 单位根`小节（原 cell `7da75f13`）之后插入
   新 markdown+code cell（新 cell `96e758f3` / `815eae11`）：给出"单位根 = zᴺ=1 的解"的
   定义，并解释 `W_N^k` 记号中下标/上标各自的含义；代码验证 `(W_N^k)^N ≈ 1`。

3. **plot_conjugate demo 画圆和箭头的写法** → 同"老陈 5"（新 cell `e895d16a` / `a9b556dc`）。

4. **DFT 旋转因子符号太密集（i, k, n, N 分别是什么，twiddle 是什么）**
   → 位置：`## 3. 复数=平面上的点`（原 cell `1a70aa00`，首次出现 `twiddle(k,n,N)`）之后
   插入新 markdown cell（新 cell `1e13ed7b`）：用表格逐一定义 i/N/n/k，并说明这条公式的
   完整推导留到 L37–L39，本课只需要眼熟。

5. **共轭对称性 X[N−k]=conj(X[k]) 太突兀，没有论证过程**
   → 位置：紧接"老陈 2"之后（新 cell `002d4194`/`22eb8c64` 之后，新 cell `2eb73dbe`/
   `18c1c78a`）：给出简化版证明（利用 e^{−i2πn}=1 和实信号共轭不变），并用随机实数信号
   的 `np.fft.fft` 结果验证 `X[N−k] == conj(X[k])`。

## 小雨的问题 → 改动

1. **为什么 e^{iθ}=cosθ+i·sinθ** → 同"老陈 1"（新 cell `0816e987` / `cd1da39b`）。

2. **虚数 i 是凭什么出现的，为什么不直接用 (a,b) 坐标对**
   → 位置：`## 0. 先修铺垫`（新 cell `45745577` / `04901d27`，与"老陈 6"合并在同一节）：
   讲清楚 i 是数学家为解 x²+1=0 这类方程而"定义"出来的符号，i²=−1 是定义而非推导结果。

3. **"复数乘法=旋转"为什么模也要相乘**
   → 位置：紧接"老陈 1"之后（新 cell `3491dd9e` / `45f3cf49`）：用 z₁=1, z₂=i 和
   z₂=2i 两个具体例子对比，说明模负责缩放、幅角负责旋转，两者独立；代码逐项验证
   模相乘、角度相加。

4. **单位根 W_N^k 记号、为什么均匀分布、"旋转基"是什么**
   → 位置："单位根"命名解释（新 cell `96e758f3`/`815eae11`，回应琳达 2）之后，再插入
   新 markdown cell（新 cell `2c3fdfda`）：解释均匀分布对应"正交/互不干扰的测量方向"的
   直觉类比（东/北两个方向测位置），并说明"旋转基"这个词会在 L09 线性代数课正式展开。

5. **z×z̄=|z|² 中间计算没给出**
   → 位置：同"老陈 2"之后（新 cell `002d4194` / `22eb8c64`）：把 (a+bi)(a−bi) 按分配律
   逐项展开，标出交叉项抵消、i²=−1 代入的每一步，代码逐项打印验证。

---

## 验证记录

- 合法性：`python3 -c "import nbformat; nbformat.read(...)"` 通过，cell 数从 29 增至 48
  （新增 19 个 cell：8 个 markdown 段落说明 + 无新增的删除）。
- 可运行性：在独立临时副本上用 `nbclient`（`allow_errors=True`）从头执行全部 48 个
  cell，仅 1 个 cell 报错——是原有的"本课自评"门禁 cell（`l08_review`），该 cell 设计上
  就要求学生手动把 `None` 改成 `True/False` 才能通过 assert，与本次修订无关，属预期行为。
  其余全部 12 个新增代码 cell（含泰勒级数逼近、复数乘法验证、z×z̄ 展开、DFT 共轭对称性
  验证、单位根验证、annotate/quiver/arrow 对比图、复数点积演示）均正常执行、无报错，
  数值结果与各自 markdown 里的论述一致。
- 未修改任何原有 cell 的内容、id、输出或执行计数；`git diff --stat` 显示改动为纯新增
  （130 行插入，0 行删除）。
