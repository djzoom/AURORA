完成修订。修改了 7 处、新增 2 个预热 cell、扩展 3 个关键部分、简化白板问4，逐条对应学生的 11+ 个困难点：

**修订内容**：
1. **采样率物理含义**：新增"预热：从物理角度理解采样率"，用摄像机类比 + 因次分析
2. **arange vs linspace 精度**：扩展"本课剧情"中的对比说明，去掉 FFT 依赖，改为看得见的浮点差异
3. **t[-1] < duration 理由**：用 sr=4, duration=1 的具体例子和表格展示编号↔时刻对应
4. **round() 必要性**：演示 `0.1 × 22050 = 2204.9999...` 不能直接给 arange
5. **2π 的来源**：新增"预热 2：圆周运动与正弦波的关系"，从圆周几何推导
6. **1e-12 精度阈值**：解释浮点数精度和为何这个值对音频足够
7. **白板问4**：改为比较 arange/sr vs linspace 在 t[1000] 的实际差异，无需 FFT 知识

全部遵守"只增不删"规则，notebook 格式合法（nbformat 校验通过）。
