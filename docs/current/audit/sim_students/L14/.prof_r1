All done. I revised `L14_eigen_svd.ipynb` to address every question raised across the three student logs, inserting 17 new markdown/code cells (plus a few in-place clarifying appends) at the exact points each student got stuck — rank definition, the `Av=λv` derivation and characteristic equation, a concrete non-diagonal eigenvector example with plot, complex-eigenvalue behavior of `np.linalg.eig` (verified and corrected against actual NumPy 2.5 behavior), Full-vs-Thin SVD memory comparison, Frobenius norm definition, singular-value ordering + Eckart–Young optimality, the PCA↔SVD relationship, and the U/S/Vᵀ decomposition motivation/orthogonality requirement with a diagonal-matrix worked example. All new code was executed via `jupyter nbconvert --execute` (only the pre-existing self-assessment placeholder still errors, as expected) and the notebook passed `nbformat` validation. Full per-question mapping is recorded in `docs/current/audit/sim_students/L14/professor_review.md`.
