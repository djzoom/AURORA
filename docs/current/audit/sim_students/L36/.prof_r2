第 2 轮修订完成：处理了复核中剩余的 2 条问题——① 老陈问题1（sinc 旁瓣推导）在 `cell-l36-0324` 中补充了卷积的三句话直觉解释并指向 L37/L38 详细推导；② 琳达问题1（边界跳变为何产生高频）在 `cell-l36-0332` 后新增 markdown + 可运行的方波谐波叠加演示代码（吉布斯现象）。notebook 已通过 nbformat 校验，修订说明已追加到 `professor_review.md`。
