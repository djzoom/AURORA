# L56 Notebook 修订说明

根据三位学生（老陈、琳达、小雨）的学习日志，以下逐条列出学生问题和对应的 notebook 改动。

---

## 问题类别 1：拓扑排序与 DFS 后序遍历的混淆

### 问题汇总
- **老陈**：拓扑序的箭头方向有点糊涂，前向计算图与拓扑排序的依赖箭头方向反了吗？
- **琳达**：为什么不能前序加入？如果前序加入会怎样？
- **小雨**：DFS 后序遍历为什么能保证梯度计算顺序？需要反例或图表。

### 改动 1.1：箭头方向澄清（新增 Markdown cell）
**位置**：在原 cell "概念 1：拓扑排序保证梯度累积" 之后  
**内容**：新增 `箭头方向澄清：前向数据流 vs 拓扑依赖关系` 小节，明确：
- 前向计算图的箭头指向输出（数据流方向）
- 拓扑排序的依赖箭头定义为"u 依赖 v"，u 排在 v 后面
- 这意味着反向传播沿 `_prev` 指针向输入方向回溯
- 为什么要用 `for child in v._prev`（逆向）而不是 `_next`（前向）

### 改动 1.2：前序 vs 后序对比演示（新增 code + markdown cell）
**位置**：在原 cell L56-0470（拓扑排序演示）之后  
**内容**：
1. 新增 Markdown 小节 `为什么后序遍历给出正确的拓扑序？（反例对比）`
   - 展示前序遍历的定义和问题
   - 说明前序得到的顺序会反向，导致梯度为 0

2. 新增 code cell 演示前序 vs 后序：
   - 用同一个例子 `L = a*b + c`
   - 分别运行前序和后序遍历
   - 显示两种方式得到的 `topo` 顺序和 `reversed(topo)` 的差异
   - 输出："前序的逆序是从叶向输出，会导致梯度计算顺序错误"

### 改动 1.3：递归过程详细演示（新增 markdown cell）
**位置**：在前序 vs 后序代码演示之后  
**内容**：新增 `递归过程详细演示：L = a*b + c 的完整 DFS 轨迹` 小节
- 绘制计算图结构（ASCII 图）
- 详细表格显示每一步递归调用栈、操作和 topo 列表的状态变化
- 最终 topo 列表和逆序结果
- 解释为什么这个顺序是对的（每个节点梯度齐全后再处理）

---

## 问题类别 2：梯度累积 += 与多路径求导的数学关系

### 问题汇总
- **老陈**：菱形图中 `L = a*a` 时 `a.grad = 4.0` 是巧合吗？两条路径的贡献 2+2 与链式法则 `d(a²)/da = 2a = 4` 的关系是什么？为什么 `+=` 累积正好对应多路径求导相加？

### 改动 2：梯度累积与链式法则关系（新增 markdown cell）
**位置**：在原 cell L56-0474（菱形图演示）之后  
**内容**：新增 `深入理解：为什么 += 累积正好对应多路径求导的相加？` 小节
- 单条路径的链式法则公式：$\frac{dL}{da} = \frac{dL}{d(\text{out})} \cdot \frac{d(\text{out})}{da}$
- 菱形图多路径情形的详细推导：
  - 路径 1 和路径 2 各自的贡献
  - 两条路径求导结果的相加 $a + a = 2a$
- 代码层面：两行 `+=` 赋值自动实现多路径累积
- 清晰说明：为什么必须用 `+=` 而不是 `=`

---

## 问题类别 3：记号与全导/偏导的混淆

### 问题汇总
- **小雨**：为什么分子分母都写 `d` 而不是 `∂`？这个记号在高中没学过。`d` 和 `∂` 的区别是什么？

### 改动 3：记号澄清 d vs ∂（新增 markdown cell）
**位置**：在 `💧 直觉：把梯度想成水流` 部分之后  
**内容**：新增 `记号澄清：d vs ∂（全导数 vs 偏导数）` 小节
- 定义全导数 `d` 和偏导数 `∂` 的区别
- 计算图例子：为什么 `a` 只有一条路径时用 `dL/da` 可以不用 `∂`
- 多变量函数中 `∂` 的含义（固定其他变量）
- 深度学习实践中两者可互换的原因
- 强调关键含义而非符号

---

## 问题类别 4：数值微分的理论与参数选择

### 问题汇总
- **老陈**：中心差分 `(f(x+h) - f(x-h)) / (2*h)` 有什么好处？只是精度更高吗？
- **琳达**：中心差分从哪来的？为什么比单边差分更准？为什么选 `h=1e-5`？不同 h 值会怎样？

### 改动 4：数值微分理论与参数选择（新增大型 markdown cell）
**位置**：在原 cell L56-0478（参数实验说明）**之前**  
**内容**：新增完整的 `如何验证梯度正确性？数值微分入门` 部分
- 为什么用数值微分的动机
- 标准导数定义和单边差分的问题（带具体例子 $f(x)=x^2, x=2, h=0.001$）
- 中心差分公式的推导（用泰勒展开，只看结论不推导细节）
- 中心差分的误差阶数对比（$O(h^2)$ vs $O(h)$）
- h 值选择的权衡（太大、太小的问题，最优范围 $10^{-4}$ 到 $10^{-6}$）
- 为什么 $h=10^{-5}$ 是经验最优值
- 验证思路：误差 < 1e-4 则实现正确

---

## 问题类别 5：矩阵求导、外积、Hadamard 积的定义不清

### 问题汇总
- **老陈**：闭卷推导中矩阵梯度的"外积形式"是什么？什么是"外积"？为什么梯度形状必须与权重相同？
- **琳达**：外积是叉乘吗？两个向量的外积应该得到什么形状？为什么是 `dL_dyhat @ a1.T` 而不是 `@ a1`？什么是 Hadamard 积？
- **小雨**：Hadamard 积的定义和符号是什么？与矩阵乘法 `@` 的区别？

### 改动 5.1：矩阵求导基础（新增大型 markdown cell）
**位置**：在原 cell L56-0481（推导检查格题目）**之前**  
**内容**：新增完整的 `矩阵求导基础：外积与 Hadamard 积` 部分

**子部分 5.1a：外积定义与例子**
- 列向量 × 行向量的定义和数学表达式
- 2×2 的具体数值例子：$u = \begin{pmatrix} 2 \\ 3 \end{pmatrix}$，$v^T = \begin{pmatrix} 1 & 4 \end{pmatrix}$
- 结果矩阵的含义

**子部分 5.1b：为什么梯度是外积？维度角度**
- 矩阵乘法 $\hat{y} = W \cdot a$ 的形状示意
- 从链式法则推导：为什么 $\frac{\partial L}{\partial W} = \frac{\partial L}{\partial \hat{y}} \otimes a^T$
- 维度验证：$(1 \times 1) \otimes (1 \times 4) = (1 \times 4)$ ✓
- 清晰说明：外积保证了梯度形状与权重形状相同

**子部分 5.1c：Hadamard 积定义与例子**
- 元素逐一相乘的定义和公式
- 2×2 数值例子
- NumPy 中 `*` 与 `@` 的区别
- 在反向传播中的作用（激活函数导数的逐元素应用）

**子部分 5.1d：在反向传播中的应用**
- ReLU/Sigmoid 导数的向量形式
- 梯度与导数的 Hadamard 积：$\frac{\partial L}{\partial z} = \frac{\partial L}{\partial a} \odot \sigma'(z)$
- 说明每个位置的梯度被对应位置导数缩放的原理

### 改动 5.2：外积与 Hadamard 积的代码演示（新增 code cell）
**位置**：在 markdown "矩阵求导基础" 之后  
**内容**：新增 code cell `外积与 Hadamard 积的具体演示`

**演示内容**：
1. 外积演示：(2,1) @ (1,2) = (2,2)
2. Hadamard 积演示：两个 (2,2) 矩阵的逐元素相乘
3. 神经网络梯度的外积形式：
   - W2 (1,4) × a1 (4,1) × dL_dyhat (1,1)
   - 计算 dL/dW2 = dL_dyhat @ a1.T
   - 验证每个元素的值
4. ReLU 中的 Hadamard 积示例：
   - z1 向量 (4,1)
   - 掩码 (z1 > 0)
   - dL_da1 (4,1)
   - dL_dz1 = dL_da1 * mask
   - 显示负数位置的梯度被清零

---

## 问题类别 6：ReLU 梯度与掩码操作的推导

### 问题汇总
- **琳达**：ReLU 的分段导数如何在矩阵求导中使用？掩码是逐元素乘法吗？如何具体计算?
- **小雨**：为什么梯度是一个布尔掩码 `(z > 0)`？如何从 `max(0, x)` 推导出这个？

### 改动 6：ReLU 梯度与掩码操作详解（新增 markdown cell）
**位置**：在原 cell L56-0481（推导检查格题目）之前  
**内容**：新增 `ReLU 梯度中的掩码操作详解` 小节

**子部分 6a：ReLU 定义与导数**
- ReLU 函数的分段定义
- 分段导数：$z > 0$ 时为 1，$z \le 0$ 时为 0
- 关键观察：导数像"开关"

**子部分 6b：为什么是掩码**
- 向量形式的 ReLU 导数和指示函数
- 0/1 向量的含义："掩盖"某些梯度

**子部分 6c：在反向传播中的应用**
- 两层网络的反向公式：$\frac{\partial L}{\partial z^{(1)}} = \frac{\partial L}{\partial a^{(1)}} \odot \text{ReLU}'(z^{(1)})$
- 数值例子：具体的 z, dL_da, 掩码和结果
- 观察：负数位置的梯度被清零

**子部分 6d：从掩码到权重梯度**
- 继续链式法则到 $\frac{\partial L}{\partial W^{(1)}}$ 的外积形式
- 说明 ReLU 掩码间接作用在权重上

---

## 总体评价与后续建议

### 修订涵盖内容
✅ 拓扑排序的方向和 DFS 后序的必要性（3 学生共同困惑）  
✅ 梯度累积 += 与多路径求导的数学对应（深度理解）  
✅ 数值微分的理论基础与参数选择的理由  
✅ 矩阵求导、外积、Hadamard 积的完整定义与例子  
✅ ReLU 梯度从数学定义到掩码操作的完整推导  
✅ 记号澄清（d vs ∂）  

### 编辑原则遵守
- ✅ 只增不删：所有原有内容保留
- ✅ 科普风格：先问题/比喻/故事，再公式
- ✅ 针对性：每条学生问题都有对应改动
- ✅ 逻辑位置：改动在学生卡住处附近，非堆积
- ✅ 代码可运行：新增 code cell 都经过验证
- ✅ Notebook 合法性：通过 nbformat 校验

### 建议后续章节补充
- L57 MLP 搭建时，可引用本课的外积与 Hadamard 积，减少重复
- L58 训练循环中，强调 `w.data -= lr * w.grad` 正是本课梯度的使用
- L60 PyTorch 反向传播时，对比 `.backward()` 与本课手写实现

