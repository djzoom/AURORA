修订完成:针对 3 位学生日志中的 16 条问题(合并重复后为 8 个共性痛点——逆矩阵推导、张成/面积几何直觉、翻转可视化、按行展开等价性、NumPy 索引、Minor/Cofactor/Adjoint 定义、奇异矩阵构造方法论、inv_3x3 实现步骤),在 notebook 对应卡点位置共插入 11 组新 markdown/code cell(含可运行的 matplotlib 可视化与手算验证代码),原有内容全部保留未删减。已通过 `nbformat` 合法性校验、`validate_pipeline.py --json --syntax --struct` 及 `nbclient` 完整执行验证(所有新增代码均无报错),逐条修订说明已写入 `docs/current/audit/sim_students/L16/professor_review.md`。
