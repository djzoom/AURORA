# L16 · 教授修订说明（针对学生学习日志的逐条回应）

修订对象：`notebooks/2_linear_algebra/L16_determinant_inverse.ipynb`
修订原则：只增加、不删减；在学生卡住的位置附近插入新的 markdown/code cell；科普风（类比/故事先行，公式后行）。

下面逐条列出：**学生问题 → 改动内容与位置**。

---

## 老陈（laochen.md）

1. **【理解】2×2 逆矩阵公式为什么是那样？**
   → 新增 `## 1.6 逆矩阵公式 ... 是怎么"解"出来的？`（markdown cell `0483bd13`，插在 `eef984e5` 之后）：从 `A·A⁻¹=I` 出发，展开矩阵乘法得到 4 个方程，逐步消元解出 `x,y,z,w`，最后拼回公式，说明"主对角线互换、副对角线变号"是解方程的自然结果而非规定。配套代码 cell `fd788509` 用 `np.linalg.solve` 数值验证推导。

2. **【理解】有向面积里的"翻转"指的是什么？**
   → 新增 `### 翻转到底是什么？—— 镜子 vs. 转盘`（markdown cell `e9207395`，插在动手观察代码 `cell-l16-0141` 之后）：用"硬币旋转 vs 翻面"类比区分旋转与镜像，给出反射矩阵 `[[1,0],[0,-1]]` 的具体例子。配套代码 cell `9e9b4cb2` 画出不对称"L"形状分别经旋转、镜像变换后的图像对比。同时在白板挑战 Q4（`35b8c3b8`）追加了回指该节的提示。

3. **【推演】为什么必须按第一行展开？其他行不行？**
   → 新增 `### 换一行展开，答案会变吗？`（markdown cell `bae8b041`，插在 3×3 展开代码 `580559c6` 之后）：解释"任何一行/列展开结果相同"的几何直觉，讲清棋盘符号 `(-1)^(i+j)` 随行号变化的规律。配套代码 cell `5b708d9c` 按第二行、第三行分别展开同一矩阵并与第一行结果、NumPy 参考值对比，验证一致。此 cell 同时定义了后续复用的 `minor()` 辅助函数。

4. **【理解】伴随矩阵具体怎么构造？**
   → 新增 `### 拼图说清楚：子式(Minor) → 代数余子式(Cofactor) → 伴随矩阵(Adjoint)`（markdown cell `404c12d1`，插在上条代码之后，`661fc13d` 之前）：给出三层定义的对照表（Minor / Cofactor=Minor×(-1)^(i+j) / Adjoint=Cofactor矩阵转置），并说明为什么最后要转置。配套代码 cell `cc61d415` 对课程例题矩阵手算全部 9 个 Cofactor，组装成 Adjoint，验证与课程给出的逆矩阵答案一致。

5. **【实践】怎么想到构造"det=0"的测试矩阵？**
   → 新增 `### 怎么知道该造一个什么样的"奇异矩阵"来测试？`（markdown cell `0c74049b`，插在 `cell-l16-0145` 之后、奇异矩阵断言 `cell-l16-0146` 之前）：给出"任一行是另一行倍数则必奇异"的配方，并用张成/面积的几何直觉解释原因。配套代码 cell `443c1d3d` 用不同 `(p,q,k)` 生成多个比例矩阵，验证 det 恒为 0。

---

## 琳达（linda.md）

1. **【理解】"有向面积"和"张成"的几何直觉在哪里？**
   → 新增 `## 1.5 停一下：det=ad-bc 里的每个符号到底对应图上的什么？`（markdown cell `b28f63e8`，插在 `eef984e5` 之后）：用"墙上钉橡皮筋"类比讲清"列向量张成平行四边形"，并给出 `[[2,0],[0,2]]`（正方形）与 `[[1,1],[0,1]]`（剪切）两个具体例子。配套代码 cell `9bf3d7ce` 实际画出这两个平行四边形（新增 `plot_span` 辅助函数，标出两个列向量的箭头）。另外在 `matrix_4ways` 调用处（markdown cell `f0238891`，原地追加内容）补充说明该函数展示的是"四种读法"热力图，而非几何平行四边形，避免与 `plot_span` 混淆。

2. **【推演】2×2 逆矩阵公式的主对角互换、副对角变号是怎么推导出来的？**
   → 同老陈问题 1，见 markdown cell `0483bd13` + 代码 cell `fd788509`。

3. **【推演】"代数余子式"和"伴随矩阵"是什么，怎么从它们计算逆矩阵？**
   → 同老陈问题 4，见 markdown cell `404c12d1` + 代码 cell `cc61d415`（含 Minor/Cofactor/Adjoint 对照表和 9 个 Cofactor 的完整手算示例）。

4. **【计算】3×3 按第一行展开时的符号规律 +,−,+ 是怎么来的？**
   → 同老陈问题 3 的棋盘符号讲解（markdown cell `bae8b041`），并在 `404c12d1` 中用"棋盘图"进一步明确规律：`(-1)^(i+j)`，i+j 偶数为 `+`，奇数为 `-`。

5. **【实践】实现 `inv_3x3` 时如何系统地计算伴随矩阵的 9 个元素？**
   → 新增 `### 写 inv_3x3 前，先列一个检查清单`（markdown cell `4e10703d`，插在练习说明 `cell-l16-0147` 之后、代码框架 `cell-l16-0148` 之前）：给出 5 步伪代码分解（写 det_3x3 → 写 cofactor → 组装 Cofactor 矩阵 → 转置 → 除以 det），并指出可复用前面 `404c12d1`/`cc61d415` 里已经演示过的 `minor`/`cofactor` 结构。

---

## 小雨（xiaoyu.md）

1. **【理解】逆矩阵公式的来源是什么**
   → 同老陈问题 1，见 markdown cell `0483bd13` + 代码 cell `fd788509`。

2. **【理解】伴随矩阵（adjoint matrix）没有被定义清楚**
   → 同老陈问题 4 / 琳达问题 3，见 markdown cell `404c12d1` + 代码 cell `cc61d415`。

3. **【推演】行列式 ad-bc 这个公式从哪里来的**
   → 与琳达问题 1 合并处理，见 markdown cell `b28f63e8`：明确给出"底×高"的几何来源说明（`ad-bc` 是平行四边形面积公式在坐标系下的展开），并配 `plot_span` 可视化（代码 cell `9bf3d7ce`）。

4. **【推演】"划掉行列"在代码中怎么实现（`A[1:,[1,2]]` 语法看不懂）**
   → 新增 `### 读懂代码里的 NumPy 索引：A[1:, [1,2]] 是什么意思？`（markdown cell `a357b7c5`，插在 `494b85e1` 之后、原索引代码 `580559c6` 之前）：逐段拆解切片 `1:` 与花式索引 `[1,2]` 的含义，并预告后面会用 `np.ix_` 处理任意 `i,j` 的划行划列。

5. **【理解】符号混乱：M_ij、C_ij、代数余子式**
   → 在 `404c12d1` 中新增"关于符号的小澄清"段落：说明课程原公式 `a11*M11 - a12*M12 + a13*M13` 用的是裸 Minor、符号手写在公式里；若改用 Cofactor（已含符号）则公式变为纯加号形式 `a11*C11+a12*C12+a13*C13`——两者等价，只是符号位置不同。

6. **【实践】白板挑战题 4 的"翻转"没有可视化**
   → 同老陈问题 2，见 markdown cell `e9207395` + 代码 cell `9e9b4cb2`（旋转 vs 镜像对比图），并在白板挑战 `35b8c3b8` 追加提示回指该节。

---

## 附：新增内容一览（按 notebook 顺序）

| 插入位置（原 cell 之后） | 新增内容 |
|---|---|
| `eef984e5` | 1.5 张成/面积可视化（md+code）、1.6 逆矩阵推导（md+code） |
| `cell-l16-0141` | "翻转 vs 旋转" 可视化（md+code） |
| `cell-l16-0145` | 奇异矩阵构造配方（md+code） |
| `494b85e1` | NumPy 花式索引讲解（md） |
| `580559c6` | 换行展开等价性演示（md+code，定义 `minor()`） |
| （其后代码 cell） | Minor/Cofactor/Adjoint 三层定义 + 9 个 Cofactor 手算（md+code） |
| `f0238891`（原地追加） | `matrix_4ways` 输出说明 |
| `cell-l16-0147` | `inv_3x3` 实现步骤清单（md） |
| `35b8c3b8`（原地追加） | 白板挑战 Q4 提示回指 |

验证：`nbformat.read(..., as_version=4)` 通过；`python scripts/validate_pipeline.py --json --syntax --struct` 全部通过；用 `nbclient` 完整执行了 notebook，所有新增代码 cell（含 matplotlib 绘图、`np.linalg.solve` 推导验证、9 个 Cofactor 手算等）均无报错运行成功（唯一的执行中断发生在原有的"本课自评"cell，因其字段默认值为 `None` 需学生手动填写，这是设计如此，非本次改动引入）。
