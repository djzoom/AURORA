Notebook validated (nbformat + full `jupyter nbconvert --execute` run, only the pre-existing student self-eval cell fails as expected) and review notes appended.

处理了第 2 轮复核标记的 3 条未解决问题，全部通过插入新 cell（不删减原文）解决：老陈问题4在 Value 最小示例演示后新增图解 cell（`d9078b67`/`f99309a2`），用 ASCII 图 + 可运行代码对比"孤立节点 vs 完整节点"的梯度回流差异；琳达问题3在 MLP class TODO 前新增 cell（`bb6e53fb`/`638bf782`），用类比讲清"统一包装为列表"的原因及其在 L58 训练循环中的作用；小雨问题6在 Layer 演示后新增 cell（`d4ede196`/`672c0a84`/`66fa5996`），用可运行代码实际触发并捕获中间层 nout=1 导致的 `TypeError`，说明 MLP 如何（以及未如何）处理类型差异。修订说明已追加到 `professor_review.md`。
