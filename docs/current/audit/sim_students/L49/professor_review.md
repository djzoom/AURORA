# L49 Notebook 修订记录

修订日期：2026-07-08

## 问题分析与改动对应表

### 【小雨的问题】

#### 1. 基向量 L2 模长为什么是 √N 和 √(N/2)？
**学生问题**：不理解基向量模长的计算，k=0 的为什么是 √N，k>0 的为什么是 √(N/2)

**改动**：
- **插入新 cell 1.5**（markdown）：《深入·基向量的 L2 模长》
  - 用 N=4 的具体例子，逐一列出 k=0,1,2,3 的基向量值
  - 手算每个基向量的 L2 模长
  - 展示 k=0 = √4, k>0 = √2 的具体数值
- **插入代码 cell**：验证 N=4 和 N=8 下各基向量的模长
- **位置**：第 1 节公式之前（cell 46524717 之前）

#### 2. 为什么权重是 √(1/N) 和 √(2/N)？
**学生问题**：不理解从"基向量模长"到"权重系数"的逻辑

**改动**：
- **插入新 cell 2.5**（markdown）：《深入·从矩阵的不同行模长到权重》
  - 解释：无权重时 Gram 矩阵对角线不统一（k=0 是 N，k>0 是 N/2）
  - 标准化需要除以各自的模长 → 得到两个不同的权重
  - 推导 w(0) = 1/√N，w(k>0) = √(2/N)
- **插入代码 cell**：演示权重应用前后的 Gram 矩阵变化
- **位置**：正交性验证 cell（e3bab1b4）之前

#### 3. 代码中 scale 向量怎么用？是元素乘法吗？
**学生问题**：`(M @ x) * scale` 与 `(scale[:, None] * M) @ x` 是什么关系？

**改动**：
- **插入新 cell 4.3**（markdown）：《补充·Scale 向量的两种等价用法》
  - 详细说明两种写法的数学等价性
  - 给出元素级和矩阵级的展开形式
  - 实现建议（哪种更快）
- **插入代码 cell**：验证两种方式输出完全相同
- **位置**：第 4 节实现之前（cell 54e8e829 之前）

#### 4. Mel 滤波器为什么 50% 重叠就导致相关系数 0.9+？
**学生问题**：直观感受不到"重叠"和"相关"的因果关系

**改动**：
- 在原有的去相关部分（第 3 节）前，新增详细代码演示
- **插入代码 cell 72c0d1c5**：生成 AR(1) 相关向量，计算 Pearson 相关系数
  - DCT 前后相关系数的对比表
  - 协方差矩阵的非对角元变化
  - 直观展示"去相关"的效果
- **位置**：第 3 节正文 2ff505d5 之后

#### 5. 逆变换为什么不需要除以 2N？
**学生问题**：不确定 D 是否真的满足 D @ D.T = I

**改动**：
- 修正 cell a59b92de 的代码（原来有 bug）
  - 原代码调用 `dct_ii(x[n], norm='ortho')` 有语法错误
  - 新代码先尝试用本地 dct_ii，失败则用 aurora 参考实现
  - 明确验证正交性
- **位置**：cell a59b92de（替换）

### 【老陈的问题】

#### 1. 基向量 L2 模长的三角恒等式
**学生问题**：为什么 k>0 的基向量模长平方和是 N/2？涉及什么三角恒等式？

**改动**：
- 同小雨问题 1 的改动（cell 1.5 + code cell）
- 补充说明三角恒等式的名称和形式（不要求自己推导）
- **位置**：第 1 节之前

#### 2. 偶对称延拓的定义和操作
**学生问题**：什么是偶对称延拓？[x₀, x₁, x₂, x₃] 怎样变成什么？为什么 DFT 的实部等于 DCT？

**改动**：
- **插入新 cell 2.8**（markdown）：《补充·什么是偶对称延拓？》
  - 明确定义偶对称延拓的操作
  - 给 [1,2,3,4] 的具体例子
  - 解释为什么 DFT 实部 = DCT
- **插入代码 cell 6bc41437**：用 N=4 演示偶对称延拓和 FFT 虚部的性质
- **位置**：第 2 节（FFT vs DCT 表格）之前（cell dc7fba1a 之后）

#### 3. 正交矩阵的实际好处（对 MFCC 的意义）
**学生问题**：为什么一定要正交矩阵？非正交版本会怎样？对 MFCC 是必需还是可选？

**改动**：
- 修改原有第 dda5ddc5 cell，补充说明正交性的三个优势
  - 逆矩阵 = 转置（无需求逆，数值稳定）
  - 能量守恒（Parseval 定理）
  - 与 scipy/numpy 生态兼容
- **插入新 cell 2.7 + 代码**：《补充·Parseval 定理证明与验证》
  - 简要证明为什么 ||X||² = ||x||²
  - 数值演示能量守恒
- **位置**：正交性验证之前（cell e3bab1b4 处）

#### 4. 相关系数表格的具体计算和数据来源
**学生问题**：r(0,1)≈0.91 这些数字怎么算的？给出具体例子

**改动**：
- 同小雨问题 4 的改动（cell 8fbc2a69 + 72c0d1c5）
- 补充了 Pearson 相关系数公式的完整展示
- 给出可复现的 AR(1) 模拟数据和计算代码
- **位置**：第 3 节去相关部分

#### 5. 代码写法的等价性
**学生问题**：`scale[:, None] * D @ x` 和 `(scale[:, None] * M) @ x` 是否等价？

**改动**：
- 同小雨问题 3 的改动（cell 4.3 + code cell 015a4ed5）
- **位置**：第 4 节实现之前

### 【琳达的问题】

#### 1. 基向量模长计算（N=4 例子）
**学生问题**：用 N=4 列出 k=0,1,2,3 的基向量和模长

**改动**：
- 同小雨问题 1 的改动（cell 1.5 + code cell）
- **位置**：第 1 节之前

#### 2. 偶对称延拓的定义和例子
**学生问题**：[1,2,3,4] 怎样被延拓？为什么和 DCT 有关系？

**改动**：
- 同老陈问题 2 的改动（cell 2.8 + code cell 6bc41437）
- **位置**：第 2 节之前

#### 3. 闭卷推导中的三角恒等式
**学生问题**：∑ cos(θ_n) 的求和公式是什么？不是本课内容吗？

**改动**：
- **插入新 cell 9457f4d7 + aaf25102**：《补充·闭卷题前置知识》
  - 给出关键的两个三角恒等式（不要求学生自己推导）
  - 说明这些恒等式为什么保证正交性
  - 给出 N=8 的数值验证
- **位置**：闭卷题（eb70ab38）之前

#### 4. 协方差矩阵和对角化的含义
**学生问题**：什么是协方差矩阵？为什么正交变换就能让它对角化？

**改动**：
- 在相关系数计算的代码中（72c0d1c5）补充明确定义
  - "协方差矩阵 = 给定一批样本，计算特征间的二阶统计"
  - 展示 `np.cov()` 的使用
  - 直观演示"非对角元变小"≈"协方差矩阵接近对角"
- 补充文字说明为什么正交变换让它对角化（不需要严格证明，只需直观）
- **位置**：第 3 节去相关部分（cell 8fbc2a69 的正文和代码 72c0d1c5）

#### 5. 表格数据的来源和计算代码
**学生问题**：相关系数表格的数据怎么来的？

**改动**：
- 同小雨问题 4 和老陈问题 4（cell 8fbc2a69 + 72c0d1c5）
- **位置**：第 3 节去相关部分

#### 6. Parseval 定理的证明
**学生问题**：为什么正交变换能保证 ||X||² = ||x||²？

**改动**：
- 同老陈问题 3（cell 2.7 + 14f9bbeb）
- **位置**：正交性验证之前

---

## 修订总结

- **新增 markdown 讲解 cell**：6 个（1.5, 2.5, 2.7, 2.8, 4.3, 补充三角恒等式）
- **新增代码演示 cell**：8 个（验证基向量模长、权重效果、偶对称延拓、相关系数、scale 等价性、Parseval 定理、三角恒等式）
- **修改既有 cell**：2 个（a59b92de 正交性验证、533eb435 实验代码）
- **改动原则**：只增加不删减，保留所有原有内容，通过"铺垫→讲解→演示"的科普套路补充学生的知识缺口

## 改动覆盖的学生问题统计

| 学生 | 问题数 | 全部解决 |
|------|-------|---------|
| 小雨 | 5 | ✓ |
| 老陈 | 5 | ✓ |
| 琳达 | 6 | ✓ |
| **总计** | **16** | ✓ |

每条问题都在原 notebook 中对应位置得到详细讲解和代码演示。

---

## 第 2 轮修订（2026-07-08）

第 1 轮复核发现以下 7 条问题实际上仍未妥善解决（讲了"结论"或"数值验证"，但缺"完整推导"/"反面对比"/"可视化"/"显式桥接"）。本轮逐条修复，全部**只增不删**（原内容保留，改动为追加或在原 cell 内扩写）。

### 老陈问题1 — cos² 求和恒等式缺完整推导

**原问题**：cell `1d526f3c`（1.5 节）只说"利用三角恒等式…可以证明"，没有真正推导 $\sum_{n=0}^{N-1}\cos^2(\pi k(2n+1)/2N)=N/2$。

**改动**：
- 扩写 cell `1d526f3c`：加入「完整推导」小节，用 $\cos^2\theta=(1+\cos2\theta)/2$ 降幂公式拆分，再用欧拉公式（回忆 L06）把 $\sum\cos(2\theta_n)$ 转成等比数列求和，证明公比 $r=e^{i2\pi k/N}$ 满足 $r^N=1$（转整数圈），因此和为 0，从而得到 $N/2$。配「钟面上箭头首尾相消」类比。
- 新插代码 cell（id `d8c93518`，插在 `1d526f3c` 之后、`000cd580` 之前）：分别验证 ∑cos²、拆分后两项、等比数列和三步是否一致。
- **位置**：第 1.5 节内，紧跟"卡住"处。

### 老陈问题3 — 缺少非正交版本的反面说明

**原问题**：只抽象地说正交性有三个优势，没有具体展示"不给 k=0 特殊权重"会怎样。

**改动**：
- 新插 markdown cell（id `530932d2`，2.6 节「反面示范」，插在 `5bf9ba59` 之后）：用"印错刻度的尺子"类比，说明破坏的三件事（矩阵不再正交 / 逆变换不能用转置 / 数值出现系统性偏差）。
- 新插代码 cell（id `f6f0b8e0`）：构造"偷懒版本"（k=0 也用 √(2/N)），对比正确/偷懒版本的 `D@D.T` 对角线（2.0 vs 1.0），再对比"偷懒转置重建"（误差 0.228，系统性偏差）vs "老实求逆重建"（误差 ~1e-15，但要多付出求逆代价）vs "正确正交版本转置重建"（误差 ~1e-15，免费）。
- **位置**：2.5 节（正确加权演示）之后、第 3 节（去相关）之前。

### 琳达问题3 — 三角恒等式推导过程缺失

**原问题**：cell `9457f4d7` 直接给出恒等式"结论"，未推导。

**改动**：
- 扩写 cell `9457f4d7`：用积化和差公式 $\cos A\cos B=\frac12[\cos(A-B)+\cos(A+B)]$ 把内积拆成两个等差角度余弦和，再复用 1.5 节证明过的"等比数列求和为 0"引理，分三种情况（$k_1\neq k_2$ / $k_1=k_2\neq0$ / $k_1=k_2=0$）推出完整的正交性恒等式。
- **位置**：闭卷题（`eb70ab38`）之前的前置知识 cell 内。

### 琳达问题4 — 协方差矩阵定义缺失

**原问题**：cell `2ff505d5` 提到"协方差矩阵接近对角阵"但未定义什么是协方差矩阵，也未解释为何正交变换使其对角化。

**改动**：
- 扩写 cell `2ff505d5`：插入协方差矩阵的显式定义 $\text{Cov}=\frac1M X^\top X$（M 个样本、每个 N 维），解释对角线=方差、非对角线=协方差；再推导 $\text{Cov}_Y = D\,\text{Cov}_X\,D^\top$（正交矩阵做相似变换），说明 DCT 基是 Mel 能量（AR(1) 平滑相关结构）协方差矩阵特征向量的良好近似，因此能达到接近对角化的效果。
- **位置**：第 3 节开头，紧跟"协方差矩阵接近对角阵"那句话之后。

### 小雨问题2 — 非对角元保持 0 缺数学解释

**原问题**：cell `dd35ca9a`（2.5 节）只解释了对角线如何被权重统一为 1，没解释为什么非对角线不会被权重"搞乱"。

**改动**：
- 扩写 cell `dd35ca9a`：加入"给两个数分别乘非零常数，0 乘完还是 0"的核心论证，写出 $D_{0,n}D_{1,n}=w(0)w(1)M_{0,n}M_{1,n}$，对 n 求和后 $=w(0)w(1)\cdot0=0$，强调"权重只改变对角线，不影响非对角线"。
- 新插代码 cell（id `cea69447`，插在 `dd35ca9a` 之后、`5bf9ba59` 之前）：手算 N=8 时 k=0 行与 k=1 行的内积，加权前后对比，数值验证仍 ≈0。
- **位置**：2.5 节内，紧接对角线推导之后。

### 小雨问题4 — 缺 Mel 滤波器重叠→相关性可视化

**原问题**：课程只文字断言"50% 重叠导致相关系数 0.9+"，完全没有可视化或玩具信号验证。

**改动**：
- 新插 markdown cell（id `1aaa030f`，3.2 节，插在 `2ff505d5` 之后、`8fbc2a69` 之前）："两个麦克风收音范围重叠一半"类比。
- 新插代码 cell（id `e36f868a`）：用 matplotlib 画出 4 个相邻三角形 Mel 滤波器（相邻 50% 重叠），再用一个共振峰位置/幅度轻微漂移的玩具频谱包络通过这些滤波器，计算输出的相关系数矩阵，展示相邻滤波器（重叠多）相关系数 ≈0.97，远距离滤波器（不重叠）明显更低（≈0.79）——用数字和图形直接对应"重叠→相关"的因果关系。
  - 首版参数（独立同分布逐点噪声）未能产生预期的强相关效果（r(0,1) 仅 0.35），已重新设计为"共享包络漂移+少量本底噪声"的模型并重新调参验证，确认 r(0,1)=0.976 > r(0,2)=0.9 > r(0,3)=0.787 的单调递减规律成立。
- **位置**：第 3 节内，动机文字之后、Pearson 相关系数详解之前。

### 小雨问题5 — 正交性→可逆性的桥接缺失

**原问题**：cell `3dce118b`（第 5 节）直接说"D 满足 D D^T=I，所以逆变换是转置"，未明确引用"已经验证过"这件事。

**改动**：
- 扩写 cell `3dce118b`：明确写出"这不是'假设'出来的，而是我们已经在代码里验证过的事实"，具体点名 Cell `e3bab1b4`（未加权 M 的正交性）和 Cell `a59b92de`（加权 D 的 $DD^T\approx I$，误差<1e-10），再复述正交矩阵定义 $D^{-1}=D^T$，桥接到下方 `_idct_ii_ref` 的实现。
- **位置**：第 5 节实验 A 开头。

### 验证

- `python3 -c "import nbformat; nbformat.read(...)"` 通过，44 个 cell，格式合法。
- 所有新增/修改的代码 cell 已用独立 Python 脚本逐个验证可运行、数值结果符合预期（含 Mel 滤波器相关性演示的参数修正）。
- 将全部 code cell 按 notebook 顺序依次 `exec`，除最后的人工自评 cell（`17d01781`，设计上需要学生手动填写 True/False，无人值守运行必然 AssertionError）外，全部顺利执行，证明新插入的 cell 未破坏原有的变量依赖顺序。

### 覆盖统计（第 2 轮）

| 学生 | 本轮问题 | 状态 |
|------|---------|------|
| 老陈 | 问题1（cos² 完整推导）、问题3（非正交反面示范） | ✓ 已修复 |
| 琳达 | 问题3（三角恒等式推导）、问题4（协方差矩阵定义） | ✓ 已修复 |
| 小雨 | 问题2（非对角元数学解释）、问题4（Mel 滤波器可视化）、问题5（正交→可逆桥接） | ✓ 已修复 |
| **总计** | **7** | **✓ 全部处理** |
