Fixed both remaining round-2 issues plus one incidental bug: (1) added an optional "how to hand-calc a non-peak point" walkthrough + verification code to the white-board challenge (`2b3e1348`/`432168c1`, addressing 问7); (2) added a "抽样变异/sampling variation" explanation to the 大数定律 section (`e3ade90b`) plus a new runnable demo cell showing outlier-count fluctuation across 10 seeds, and a callback note in the Q5 answer (addressing 问8/小雨问8); (3) fixed a pre-existing f-string syntax bug in the Q5 answer cell that would have crashed on execution. Notebook validated via `nbformat` structural check, per-cell `compile()`, and a full `nbclient` end-to-end execution — all pass. Revision notes appended to `professor_review.md`.
