# L23_gradients.ipynb 修订说明（教授审阅）

三位模拟学生（老陈、琳达、小雨）的学习日志已全部读完。以下逐条列出**学生问题 → notebook 中的改动与位置**。原则：只增不减，原文全部保留，新增内容插在学生实际卡住的位置附近。

## 1. 符号陌生：∂ 和 ∇ 是什么？

- **提出者**：小雨（"符号 ∂ 是什么？为什么不写 d？"、"∇ 这个倒三角是什么符号？"）
- **改动位置**：在剧情 cell（`cell-l23-0209`，∇f 公式首次出现处）之后插入新 markdown cell「先认识两个新符号：∂ 和 ∇」。
- **内容**：解释 `∂`（"偏导符号"/partial，与一元导数的 `d` 区别在于"是否有其他变量被固定"）；解释 `∇`（读作 "nabla"/"del"，只是"把所有偏导数拼成向量"的简写，不是新运算）。

## 2. 把 y 当常数是否合法？

- **提出者**：琳达（"为什么可以把 y 当常数处理？和一元函数求导有什么区别？"）
- **改动位置**：在偏导数介绍 cell（`be201745`）之后插入「把 y 焊死再求导——这样做真的合法吗？」。
- **内容**：用"地形图剖面线"类比 + 形式化定义 `g(x)=f(x,y₀)`，证明偏导数就是"焊死其他变量后坍缩成一元函数"再套用 L22 的普通导数定义，说明合法性来源。

## 3. ∂f/∂x = 2x 的完整推导

- **提出者**：琳达（"课程直接给出了答案，没有展示推导步骤"）
- **改动位置**：紧接上一条之后插入「手把手推导一遍：∂f/∂x = 2x 到底怎么来的」。
- **内容**：把中心差分定义代入 `f(x,y)=x²+y²`，逐步展开 `(x+h)²`、`(x-h)²`、相减、约分，完整推出 `2x`，不省略步骤。

## 4. 中心差分为什么除以 2h（单侧差分不行吗）

- **提出者**：老陈（"用 (f(x+h)-f(x))/h 和中心差分哪个误差更小？原因是什么？"）、琳达（"课程没有明确说明与向前/向后差分的比较，感觉和 L22 断层"）、小雨（"h 怎么选，太大太小会怎样？"）
- **改动位置**：紧接上文插入「复习一下：为什么偏导数也用"除以 2h"的中心差分？」markdown + 一个可运行代码 cell（对比前向/中心差分在 `(3,4)` 处的误差随 h 缩小的变化）。
- **内容**：三种差分公式对照表、`2h` 的几何含义（两点间水平距离）、指回 L22 的泰勒展开推导、并用代码复现"中心差分误差下降更快"的现象。

## 5. "等高线"是什么？为什么是同心圆？

- **提出者**：老陈（"等高线是什么？为什么要谈它？"）、小雨（"等高线和法向量是什么？f(x,y)=x²+y² 的等高线为什么是同心圆？"）
- **改动位置**：插入「等高线到底是什么？」markdown + 一个 `matplotlib.contour` 可视化代码 cell（画出等高线圆、梯度箭头、切线箭头）。
- **内容**：从地图类比引入等高线定义 `f(x,y)=c`；用"到原点距离恒定"代数推导出 `x²+y²=c` 是圆；图示直接展示梯度垂直于等高线的几何关系。

## 6. 切线方向 (-4,3) / (-2,1) 怎么算出来的

- **提出者**：小雨（"点 (1,2) 处的等高线切线方向是 (-2,1)，这怎么算？是否有公式？"）
- **改动位置**：紧接等高线图之后插入「切线方向 (-4,3) 是怎么算出来的？——旋转 90° 的小技巧」。
- **内容**：给出通用的 2D 向量旋转 90° 公式 `(a,b)→(-b,a)`，用点积恒等式 `-ab+ab=0` 证明恒垂直；顺带解释了 `cell-l23-0217` 代码注释里"该检验是代数恒等式、测不出 bug"这句话的由来。

## 7. 为什么"垂直于等高线"就是"最陡"

- **提出者**：老陈（"没理解为什么垂直就意味着最陡，需要几何解释"）、琳达（"为什么垂直方向比其他所有方向都陡？数学上怎么证明？"）
- **改动位置**：紧接上一条插入「为什么"垂直于等高线"就意味着"最陡"？」。
- **内容**：引入方向导数 `D_u f = ∇f·u = ‖∇f‖cosθ`，说明变化率在 `θ=0`（与梯度同向）时最大、在 `θ=90°`（垂直/沿等高线）时为零，从而把"最陡"和"垂直"统一到同一个公式里。

## 8. 单位扰动向量 e 的构造：为什么不直接写列表

- **提出者**：老陈（"为什么不直接写 [0,...,h,...,0]，单位向量乘 h 比直接构造列表复杂啊"）、琳达（"3 维或更高维时，单位向量的几何直观是什么？"）、小雨（"为什么叫'单位'向量？"）
- **改动位置**：在「写代码前，先把变量表补完整」（`cell-l23-0215`）之后插入「"单位扰动向量" e 到底是什么，为什么不嫌麻烦地构造它？」。
- **内容**：用 3D 坐标轴基向量 `e_x/e_y/e_z` 建立"单位向量=长度1+只有一个分量非零"的直观；解释"方向（e）与步长（h）分离"带来的维度通用性——不需要为每个维度手写字面量列表。

## 9. dtype=float 的必要性

- **提出者**：小雨（"point = np.asarray(point, dtype=float)，如果已经是数组，为什么还要转换？"）
- **改动位置**：紧接上一条插入「dtype=float 是多此一举，还是必要的？」markdown + 一个可运行代码 cell，实际演示整数数组导致偏导数被截断成整数的 bug；随后补一段落把这个 demo 与 `gradient()` 函数首行代码对应起来。
- **内容**：用 `np.zeros_like` 会继承输入 dtype 的事实，演示不加 `dtype=float` 时 `6.2831` 被静默截断成 `6` 的真实 bug，说明这是"跑起来不报错但结果错"的典型陷阱。

## 10. 梯度范数是什么，为什么叫"步长缩放因子"

- **提出者**：老陈（"梯度范数是什么意思？为什么称为步长缩放因子？和梯度下降学习率有什么关系？"）
- **改动位置**：在三元函数梯度验证 cell（`cell-l23-0218`）之后插入「梯度范数（norm）是什么？为什么叫"步长缩放因子"？」。
- **内容**：回顾 L11 的欧几里得范数公式；用梯度下降更新规则 `新位置=旧位置-lr×梯度` 说明"一步实际移动距离=lr×‖∇f‖"，解释梯度长度如何在坡缓处自动让步子变小，预告 L25。

---

## 校验

- 所有新增 markdown 均遵循"类比/问题先行，公式后行"的科普风格，未使用"显然/易得/不难看出"。
- 新增代码 cell（前向 vs 中心差分对比、等高线+梯度可视化、dtype 截断演示）已用 `nbclient` 端到端执行验证，均可正常运行、无报错（notebook 原有的"本课自评"占位 cell 因 `l23_review` 字段未手动填写而断言失败，属预期的原有行为，与本次修订无关）。
- `nbformat.read(..., as_version=4)` 校验通过，notebook 结构合法。

---

## 复审补记（第二轮）

复查本文档记录的全部改动是否已实际落在 notebook 里、内容是否遮住了每一条学生问题。结论：前 10 条改动均已存在于 notebook 且逐条对应学生原话，代码 cell 重新用 `nbclient` 整本执行一遍确认无报错（同一个"本课自评"占位 cell 之外）。

发现一处遗漏并已修复：

### 11. "法向量"这个词本身没有被定义

- **提出者**：小雨（"'法向量'更是第一次听。课程需要配一张图或类比：为什么圆的法向量是径向？"）
- **原状态**：条目 5、6、7 已经把"梯度⊥切线"的几何关系和"为什么垂直=最陡"讲透了，但全程只在标题/注释里提过"法向量"这个词（如 `cell-l23-0216` 标题"梯度方向即等高线法向量"），从未正面解释这个术语本身的含义——对第一次见到"法向量"三个字的学生，这个词仍然是空的。
- **改动位置**：在 `e758c6b1`（即条目 6、7 所在的 cell）末尾追加「顺便认识一个名字：什么是"法向量"？」小节。
- **内容**：给出定义——曲线（或曲面）上某一点，垂直于该点切线方向的向量叫"法向量"；用圆类比（切线=贴着圆走，法向量=垂直于切线=沿半径方向）建立直觉；最后把"梯度方向 = 函数值上升最快方向 = 等高线法向量"三种说法统一起来，并说明对 `x²+y²` 这种以原点为圆心的等高线，法向量恰好等于径向，解释了为什么之前算出的梯度 `(6,8)` 会和位置向量 `(3,4)` 同向。
- **校验**：`nbformat` 合法性校验通过；重新执行整本 notebook，新增段落所在 cell 无代码、不影响执行；未使用"显然/易得/不难看出"（复审中把初稿里的"不难验证"也替换掉了，同属应避免的手摆式措辞）。
